# QA batch gate — #252 (remote idle-expire + PWA liveness)

**Date:** 2026-08-16
**Commits gated:** 1a1dd52 (backend idle-expire), 2b5edf0 (PWA liveness 5xx), 0822076 (PWA bridge-timeout JSON vs edge text/plain split)
**Merged into main via:** 41dd757, dfca6dd

## Result: PASS (GO)

## 1. Full test suite
`.venv/Scripts/python.exe -m pytest` (editable install, repo .venv — not system python/PYTHONPATH):

```
6559 passed, 7 skipped in 537.76s (0:08:57)
EXIT:0
```

0 failures.

## 2. Lint / static gates
- `ruff check src/ tests/` → All checks passed.
- `ruff format --check src/ tests/` → 451 files already formatted.
- `lint-imports` (import-linter) → **25/25 contracts KEPT**, 0 broken (was 23-24 in prior gates — `remote-bolt-on-isolation` and others still hold; no new violations introduced by this change).

## 3. Behavioral regression review (diff read: `git show 1a1dd52`, `2b5edf0`, `0822076`)

| Required behavior | Verified in code | Verified in tests |
|---|---|---|
| Idle-expire still stops server/tunnel for real (security unchanged) | `remote/__init__.py::_check_idle_expire` still calls `self.stop()` unconditionally | `test_remote_scaffold.py::test_idle_expire_stops_server_but_leaves_enabled_true_on_disk` |
| Idle-expire no longer writes `config.enabled=False` to disk | Old `self.config.enabled = False; self.config.save()` removed, replaced with `self.auto_suspended = True` + `on_auto_suspend()` callback | same test above (asserts config unchanged on disk) |
| `maybe_start()` self-resumes after a simulated reboot when `enabled=True` was left on disk | `maybe_start()` gates purely on `config.enabled` from disk, unrelated to `auto_suspended` (in-memory only) | `test_remote_scaffold.py::test_reboot_after_idle_suspend_starts_remote_control_again` |
| `on_auto_suspend` callback wired end-to-end (MainWindow chip repaint + headless no-op) | `main_window.py`/`user_actions.py::_on_remote_auto_suspended` (repaints chip, status message) + `headless_window.py::_on_remote_auto_suspended` (drops handle only) | `test_remote_scaffold.py::test_idle_expire_calls_on_auto_suspend_callback`, `test_a_failing_callback_does_not_escape_the_qtimer_slot` (QTimer-slot exception safety preserved) |
| Re-enable via Settings reuses existing `secret_path`/`token` by default | `settings_dialog.py::_on_toggle` passes `self._current.secret_path`/`token` through to `build_config()` unless rotate checkbox is checked | `test_remote_settings_dialog.py::test_reenable_reuses_existing_secret_path_and_token_by_default` |
| Explicit rotate checkbox mints a fresh pairing identity | `rotate = self._rotate_pairing_check.isChecked()` → passes `""` to force `RemoteControl._start()` to mint | `test_rotate_checkbox_mints_fresh_identity`, `test_rotate_checkbox_resets_after_disable` |
| `idle_expire_min` now a user-editable QSpinBox (was hardcoded 240), survives re-enable round trip | `settings_dialog.py` new `QSpinBox` (range 0-1440, special value "off") wired into `build_config()` | `test_spinbox_prefills_from_current_config`, `test_changed_value_is_forwarded_to_on_apply`, `test_value_survives_a_reenable_round_trip` |
| `idle_expire_min = 0` → never auto-suspend | `AuthGate.idle_expired()` unchanged contract: `if idle_min <= 0: return False` | `test_remote_auth.py` line 280-282: `_gate(idle_expire_min=0).idle_expired() is False` (pre-existing contract, not regressed) |
| PWA: 5xx **text/plain** (edge/tunnel outage) → Offline + tunnel-down message | `app.js` `res.status >= 500` + `Content-Type` not `application/json` → `setOffline(true)`, throws `cockpit_unreachable` | `test_remote_pwa_liveness.py::test_5xx_treated_as_unreachable_before_setoffline_false` |
| PWA: 5xx **application/json** (cockpit's own 504 bridge-timeout) → stays Online + "ตอบช้า" message | Same block: JSON content-type → `setOffline(false)`, throws `cockpit_unresponsive` (distinct error) | `test_5xx_with_our_json_content_type_is_not_unreachable`, `test_5xx_with_our_json_content_type_clears_offline_before_throw`, `test_bridge_timeout_message_is_distinct_from_conn_error` |
| 404 (dead token → pairing) / 403 (`password_required` → password prompt) untouched | No changes to those branches in diff | `test_404_and_403_paths_untouched` |
| Every user-facing `apiFetch` call site tells conn/bridge errors apart from generic errors | `errMsg(err, fallback)` helper collapses 7 previous nested-ternary call sites | `test_load_projects_uses_isconnerror`, `..._verify_password_...`, `..._open_project_...`, `..._close_project_...`, `..._send_lead_message_...`, `..._send_lead_image_...`, `..._confirm_resume_...`, `test_errmsg_defined_and_prioritizes_conn_then_bridge_then_fallback` |

All required behaviors from the task spec are implemented and have direct regression test coverage. No gaps found.

## 4. Issues found
None blocking. No production code was touched by QA (per role policy).

## 5. Verdict
**GO** — merge is safe. Full suite green (6559/6559 non-skipped), lint/format/import-boundary gates green, and every behavioral requirement in the task spec traces to both a code change and a passing regression test.
