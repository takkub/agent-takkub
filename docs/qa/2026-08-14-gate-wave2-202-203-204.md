# Release gate wave 2 — #202 / #203 / #204

**HEAD:** 79374d9 · **Verdict: GO**

## 0. Environment check

`.venv\Scripts\python.exe -c "import agent_takkub; print(agent_takkub.__file__)"` →
`C:\Users\monch\WebstormProjects\agent-takkub\src\agent_takkub\__init__.py` — correct repo venv, proceeded.

## 1. Full suite

`.venv\Scripts\python.exe -m pytest` → **6044 passed, 7 skipped, 0 failed** (415.83s).

## 2. #202 — pip-editable pane guard + venv-safe repair

- `pane_guard.classify()` probed directly for all three flagged variants plus
  a plain install: `pip install -e .`, `pip3 install --editable .`,
  `python -m pip install -e .`, and `pip install -e ./somepath` all return
  `allowed=False, rule=pip_editable:pip-install`; `pip install requests`
  (and multi-space variants) return `allowed=True`. No allowlist by role,
  confirmed against `backend`.
- `tests/test_pane_guard.py -k pip` → 23/23 passed.
- `worktree_manager.repair_editable_pth_if_stale()` resolves the reinstall
  interpreter via `_dev_venv_python(git_root)` (`<git_root>/.venv/Scripts/python.exe`
  or POSIX `bin/python`) — **never** `sys.executable` — and returns a
  no-repair-possible message (does not run anything) when that interpreter
  doesn't exist. Read source to confirm; `tests/test_worktree_manager.py -k
  "repair or editable or pth"` → 7/7 passed.
  - Note: `doctor.check_editable_install()`'s own `_reinstall()` still uses
    `sys.executable` directly, but it is correctly gated behind `DATA_HOME
    == REPO_ROOT` (dev-checkout self-repair only — the process's own
    interpreter *is* that venv's interpreter in that case). This is a
    different, narrower codepath than the cross-instance repair
    `repair_editable_pth_if_stale` exists for (a prod cockpit repairing a
    dev checkout's venv after a worktree removal) — not a regression, no
    action needed.
- Role-file prose sync (`tests/test_agent_role_files_have_pip_editable_guard.py`,
  64 tests / 16 role files): all green. Verified the test actually catches
  drift — temporarily mutated `.claude/agents/qa.md`'s guard header
  (`ห้าม pip install -e` → `XXX pip install -e`), confirmed
  `test_has_section[qa.md]` failed with a clear assertion, then restored the
  file from a backup and re-ran to confirm clean (`git status --short` empty
  afterward).

## 3. #203 — real cockpit config dir + staleness

- `provider_usage._resolve_claude_config_dir()` on this dev-checkout QA pane
  resolves through `config.active_project()` → `user_profile.config_dir_for("agent-takkub")`
  → `C:\Users\monch\.claude-work` — **not** the stale `~/.claude` (which sits
  frozen at the 2026-07-23 snapshot per the task brief). Confirmed live:
  `C:\Users\monch\.claude-work\takkub-usage-state.json` has `fetched_at:
  2026-08-14T15:21:55Z` (fresh, same day) with real 5h/7d utilization —
  matches what this instance is actually running under.
- Age-based staleness (`_CLAUDE_STALE_THRESHOLD_S = 24h`) and the
  resolution path both have dedicated coverage already:
  `tests/test_provider_usage.py -k "stale or config_dir or resolve"` → 7/7
  passed (`TestClaudeConfigDirResolution`,
  `test_stale_by_age_even_when_status_ok`,
  `test_missing_fetched_at_is_stale_not_active`). No gap found, nothing
  added.

## 4. #204 — 5h + 7d (+ codex primary/secondary) on mobile — Playwright, 390×844

Real `remote/static/app.js`/`index.html` served unmodified from a scratch
static-file harness (`python -m http.server`, copy of the static dir with a
`mock.js` shim inserted before `app.js` that only overrides `window.fetch`
for `api/bootstrap` / `api/usage` / `api/projects` with synthetic
`ProviderUsage.windows` fixtures — same shape as `usage_to_dict()`/`api.usage()`
produce; the app's real rendering code ran unmodified). Harness lived only
under the session scratchpad, never touched the repo; server process was
stopped by its own listening PID (`netstat` → `taskkull /PID`), not by name.

- **(a)** Claude card shows `5 ชม.` and `7 วัน` as two separate rows, each
  with its own % and "รีเซ็ตใน …" countdown — `204-usage-main.png`.
- **(b)** A window that exists but has no data (`codex.secondary`,
  `utilization: null`) renders `—`, never `0%` — same screenshot. Verified
  the single-window/no-quota path (`opencode`, spend-only) still renders
  its own distinct spend line, not a blended % bar —
  `204-usage-main-scrolled.png`.
- **(c)** Stale claude data (`fetched_at` 30h old) shows the gold
  `ข้อมูลอาจไม่ใหม่` badge next to "อัปเดตเมื่อ 30 ชม.ก่อน" —
  `204-usage-stale.png`.
- **(d)** Codex renders `หลัก` (primary, 44%) and `รายสัปดาห์` (secondary,
  `—`) without breaking layout — same screenshot as (a)/(b).
- Console: only the harness's own expected `sw.js` 404 (no `sw.js` shipped
  in the scratch copy) — no app errors.

Screenshots: `$TAKKUB_ARTIFACTS_DIR/screenshots/204-usage-main.png`,
`204-usage-main-scrolled.png`, `204-usage-stale.png`
(`runtime/exports/2026-08-14/agent-takkub/screenshots/`).

## 5. Regression — #198 scroll-pin / #200 Pulse

`git diff 9030ce1^ 9030ce1 -- src/agent_takkub/remote/static/app.js` — all
four hunks fall inside lines 2167–2337, entirely within the new
`USAGE_WINDOW_LABELS`/`buildUsageWindowRow`/`buildUsageCard` code (84
insertions / 5 deletions, additive). No lines outside the usage-card
rendering path were touched, so #198's scroll-pin and #200's Pulse view
logic (elsewhere in the ~2500-line file) carry zero risk from this change
by direct inspection — not re-screenshotted separately since there is no
code-path overlap to regress.

## Verdict: GO

Full suite green, #202/#203/#204 each verified against real code (unit
tests + direct probes + real-file inspection + live browser render), no
regression surface found for #198/#200.
