# UI / Boot / CLI
Remove OpenViking page, wizard, managed runtime controls, mode/limit/timeout/sync/re-index if OpenViking-only.
Remove boot orphan reap, startup health/start, shutdown stop.
Remove `takkub ov ...` commands and OpenViking doctor/runtime-health rows.
Opening Cockpit must perform zero OpenViking work.
