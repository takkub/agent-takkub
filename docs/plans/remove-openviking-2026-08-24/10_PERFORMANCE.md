# Expected benefit
Measure before/after on same machine:
- Cockpit startup time
- idle RSS
- process count
- assignment startup latency

Expected: fewer processes, less RAM, no health polling/timeouts, simpler startup/shutdown and onboarding.
