# Runtime removal
Delete `src/agent_takkub/openviking/` after checking imports.
Move only genuinely generic helpers to neutral modules if still used elsewhere.
Do not leave OpenViking abstractions "just in case".
