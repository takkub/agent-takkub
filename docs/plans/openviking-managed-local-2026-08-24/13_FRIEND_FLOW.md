# Friend/New User Flow

```text
Install Takkub
→ everything works

Optional:
Settings → OpenViking → Install & Enable
→ one-time setup
→ future launches auto-start
```

No Docker knowledge required.
No manual command required after setup.
