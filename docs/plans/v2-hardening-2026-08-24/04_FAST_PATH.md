# Fast Path

SMALL:
task -> current file/selection -> minimal rules -> one agent -> verify

Do not auto-call:
OpenViking, broad Brain recall, Figma, 21st, reviewer, multi-agent planner.

MEDIUM:
Brain scoped + Graft + relevant files + one agent + tests.

LARGE/HIGH-RISK:
Planner + Brain + Graft + OpenViking/resources + design tools if relevant
+ multi-agent/reviewer only when useful.
