# Safe Cache

Cache:
Graft queries, file metadata, OpenViking health, short-TTL retrieval,
design integration metadata.

Invalidate on:
relevant file changes, project switch, knowledge update, config change.

Never reuse project-scoped cache across projects.
Never cache secrets in trace.
