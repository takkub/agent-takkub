# QA Gate — graft 1.0.49 (final gate, 2026-08-06)

## Verdict: PASS

## What was gated (10 files, ~1,000 lines diff)
- `src/agent_takkub/graft_autobuild.py` — skip/failed tri-state build result
- `src/agent_takkub/status_header.py` — chip/tooltip/dialog wording split
- `src/agent_takkub/role_memory.py` — self-distill one-shot flag
- `src/agent_takkub/spawn_engine.py` — self-distill nudge injection
- `src/agent_takkub/main_window.py` — wiring
- `tests/conftest.py`, `tests/test_graft_autobuild.py`, `tests/test_graft_chip.py`,
  `tests/test_role_memory.py`, `tests/test_spawn_task_delivery.py`

## 1. Full suite
`.venv/Scripts/python.exe -m pytest -q` — ran twice (before/after external-repo
side-effect check). Both runs: exit code 0, all dots/skips, zero `F`/`E`. No
regression, no new fail.

## 2. docs-verify
`.venv/Scripts/python.exe -m agent_takkub.docs_verify` — exit code 0, no output.

## 3. Skip-vs-failed chip — verified against real machine state
`_git_nonignored_files()` (the real classifier `_run_build` uses) was run
directly against every distinct path across all 27 projects in this machine's
`projects.json` (46 distinct dirs total):

```
git 31 nongit(skip) 15 total 46
```

**15 dirs classified as skip — matches the gate spec exactly.** Code review of
`get_build_status()` / `status_header.py` confirms:
- `failed` and `skipped` are two separate dicts/lists, never merged (graft_autobuild.py:709-713)
- `_eligible_total = total - len(skipped)` — skip dirs don't count toward the denominator (status_header.py:734-735)
- Chip text only ever says "N failed" when `failed` is non-empty; a skip-only
  state never renders the word "failed"/"Failed" anywhere in chip, tooltip, or
  dialog (status_header.py:886-901, 1090-1124) — dialog `is_warning` is
  `bool(failed)` only, so a skip-only run stays informational tone, not a
  warning.
- Both buckets have independent TTL-based pruning so a skip entry can never
  drown a later real failure or vice versa.

## 4. Self-distill nudge
- Flag set (`_mark_distill_pending`) only inside `_archive_entries`, i.e. only
  when L1→L2 archival actually happens (role_memory.py:493-495).
- Consumed one-shot via `consume_distill_pending()` — unlinks the sentinel file
  on read, so it fires exactly once per overflow event, not every spawn
  (role_memory.py:466-479).
- `spawn_engine.py` only appends the nudge markdown block to `_appendix` when
  `consume_distill_pending()` returns `True` — confirmed by reading the diff;
  no unconditional path. Failure to check the flag is caught and logged, spawn
  still proceeds without the nudge (fail-open, correct).

## 5. Guard fix — `creationflags` literal
`grep -n creationflags` in `graft_autobuild.py`: all 3 subprocess call sites
(`Popen`/`run`) use the `SUBPROCESS_NO_WINDOW` constant imported from
`._win_console`, no raw platform-specific literal left.

## 6. Test-junk cleanup + root cause
- `runtime/role-memory/` currently contains only real project names (pms,
  unirecon, tak-game, ...) — no `proj_a/`, `proj/`, `spawn-task-test/` or other
  synthetic dirs.
- Root cause closed in `tests/conftest.py`: `role_memory.ROLE_MEMORY_DIR` is
  computed once at import time (not a lazy join), so the existing
  `_isolate_runtime` fixture's `RUNTIME_DIR` patch didn't reach it. Fixture now
  explicitly `monkeypatch.setattr(rmem, "ROLE_MEMORY_DIR", ...)`.
- Verified: ran the full suite twice; `runtime/role-memory/` directory listing
  identical before/after both runs, no new dirs.

## 7. External real-repo cleanliness (2 runs, before/after full suite)
Checked `C:/Users/monch/WebstormProjects/pms/pms-web` (real git repo, not
agent-takkub) using `.gitignore` sha256 + `git status --ignored` (per gate
instruction — not bare `git status`):
- `.gitignore` sha256: `b6883bdb...` — identical before and after.
- `git status --ignored` output: byte-identical before/after (same modified
  file, same untracked screenshots/graphify-out, same ignored list). No test
  run wrote into or touched this external repo.
- `agent-takkub`'s own `git status --ignored` also unchanged across both runs
  (same ignored-file list, no new untracked entries).

## Not re-verified (per Lead's note — already confirmed by Lead)
- ruff check + format (382 files), import-linter (23/23 contracts) — skipped
  per task instructions.
- No stray graft/viz process found running (`tasklist` check clean).

## Optional items (time-boxed, not attempted)
Search box + node-click detail in the viewer were not exercised this round —
prioritized the mandatory gate items above given scope. No blocker implied;
flagging as untested, not as failing.

## Bugs found
None. No new fails, no regression, no scope drift.
