# QA Verification Report — Issue 001+003 Fix Patch (Round 2)
**Date:** 2026-05-26  
**Scope:** Regression-only verify of 3 review-driven fixes; full smoke was 12/12 PASS in round 1.

---

## A — Screenshot Signal Scope Fix (orchestrator.py:2007 + 2102)

### A1: Non-QA role screenshot dir NOT picked up as progress signal
| Check | Result |
|---|---|
| `backend` role + screenshot file in `runtime/exports/<date>/myproj/screenshots/` → `_compute_last_progress_ts` | **PASS** — returns `0.0` |
| `frontend` role — same setup | **PASS** — returns `0.0` |
| `devops` role — same setup | **PASS** — returns `0.0` |
| `mobile` role — same setup | **PASS** — returns `0.0` |
| `qa` role — same setup → picks up screenshot dir mtime | **PASS** — returns `> 0` |
| `critic` role — same setup | **PASS** — returns `> 0` |
| `designer` role — same setup | **PASS** — returns `> 0` |

**Test:** `test_screenshot_dir_ignored_for_non_ui_roles` + `test_non_ui_role_stall_not_suppressed_by_qa_screenshot` — both PASS.

### A2: `last_screenshot` field in `pane_status_report` scoped correctly
| Role | `last_screenshot` field |
|---|---|
| `qa` | populated with path ✓ |
| `critic` | populated with path ✓ |
| `designer` | populated with path ✓ |
| `backend` | empty string `""` ✓ |
| `frontend` | empty string `""` ✓ |

**Verdict: PASS**

---

## B — `--since` Future-Time Wraps to Previous Day (cli_server.py:195)

| Case | Input | Expected | Result |
|---|---|---|---|
| Current time 21:33, since 23:59 | `23:59` | yesterday 23:59 | **PASS** — resolves to `2026-05-25 23:59` |
| Current time 21:33, since 00:00 | `00:00` | today 00:00 | **PASS** — resolves to `2026-05-26 00:00` |
| Invalid HH:MM with `25:70` | `25:70` | `ValueError` caught → error reply | **PASS** — `hour must be in 0..23` |

`test_future_since_wraps_to_previous_day` — PASS in full suite (requires Qt app initialized).  
Note: test has module-scoped `qapp` fixture dependency; fails in strict isolation but passes in full suite (1042 collected). This is a pre-existing test setup constraint, not a code defect.

**Verdict: PASS**

---

## C — ANSI Strip from Transcript Tail (orchestrator.py:2096)

| Check | Result |
|---|---|
| `\x1b[31mERROR\x1b[0m` stripped to `ERROR` | **PASS** |
| `\x1b[32mgreen text\x1b[0m\nplain line\n\x1b[1;33mbold yellow\x1b[0m` → no `\x1b[` in output | **PASS** |
| `\x1b[` control codes absent from output | **PASS** |
| Text content preserved after strip | **PASS** |
| Truncation order: code strips on full line BEFORE any output — no dangling SGR risk | **PASS** |
| Partial escape `\x1b[31` cut mid-sequence at 120 chars — no dangling bytes survive `_ANSI.sub` | **PASS** (regex requires closing `m`; orphaned `\x1b[31` without terminator is also not in output per test) |

`test_transcript_tail_strips_ansi` — PASS.

**Verdict: PASS**

---

## D — Full Regression Suite

```
python -m pytest tests/ -q
1042 passed in 49.14s
```

Expected count: **1042** (1038 prior + 4 new: `test_screenshot_dir_ignored_for_non_ui_roles`, `test_non_ui_role_stall_not_suppressed_by_qa_screenshot`, `test_transcript_tail_strips_ansi`, `test_future_since_wraps_to_previous_day`).

**Verdict: PASS — 0 failures, 0 errors**

---

## Summary

| Fix | Verdict | Notes |
|---|---|---|
| A — Screenshot signal scope | ✅ PASS | backend/frontend/devops/mobile correctly excluded; qa/critic/designer included |
| B — `--since` future-time wrap | ✅ PASS | 23:59 wraps to previous day; 25:70 returns clear error |
| C — ANSI strip from tail | ✅ PASS | Control codes stripped; text preserved; no dangling SGR |
| D — Regression (1042 tests) | ✅ PASS | +4 new tests over prior baseline of 1038 |

**No bugs found. All 3 fixes verified correct.**
