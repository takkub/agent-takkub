# QA batch gate — #151 (role-memory L2 archive) + #152 (BM25 search) — 2026-08-05

## 1. Full suite

| Check | Result |
|---|---|
| `.venv/Scripts/python.exe -m pytest -q` | ✅ exit 0, all pass, 0 failures/errors (custom conftest suppresses the final summary line, but no `F`/`E` markers appeared across the full dot run) |
| `ruff check src tests` | ✅ "Ruff: No issues found" |
| `ruff format --check src tests` | ✅ "375 files already formatted" |
| `lint-imports` (via `.venv/Scripts/lint-imports.exe` — the bare `lint-imports` on PATH resolves to a different Python without the package installed, so it 404s on `agent_takkub`; venv binary is authoritative) | ✅ "Contracts: 23 kept, 0 broken" |

## 2. Smoke — `takkub search` (#152)

- `takkub search "spawn queue" --days 3` → ✅ ranked results, each line prefixed with a `[score]` (e.g. `[11.71]`), footer `ok: 20 match(es) (bm25) (limit reached)`. Top hits are on-topic (#139 spawn-queue-wedge material).
- `takkub search "spawn queue" --days 3 --grep` → ✅ legacy mode confirmed: no score prefix, plain msg-terminated lines, same result shape as pre-#152 behavior.
- `takkub search "ค้างทั้งระบบ" --days 3` (Thai-only query) → ✅ exit 0, no crash, ranked Thai-language hits returned (top score 70.99, all results semantically on-topic for "ค้างทั้งระบบ").

## 3. Smoke — role-memory L2 archive (#151)

Method: called `role_memory._curate_text()` + `role_memory._archive_entries()` directly against a fixture memory file (isolated `ROLE_MEMORY_DIR` under scratchpad, not the repo), since this exercises the exact curation/archive code path without depending on a live spawn.

- Wrote a 647-char bullet entry (over the 600-char `_MEM_MAX_ENTRY_CHARS` cap) into a fixture `p/backend.md`.
- `_curate_text()` → `changed=True`, 1 entry archived, and the archived text matches the original 647-char entry byte-for-byte.
- `_archive_entries()` → creates `p/backend-archive.md`; the full original 647-char text is present verbatim in the archive file.
- L1 (`backend.md`) curated output is shorter than the original (truncated), confirming L1 stays capped while L2 retains the full text.
- Cross-checked against existing unit coverage in `tests/test_role_memory.py` (all passing in the full-suite run above): full-text preservation on truncate/trim, archive not injected into spawn content, no archive file created when nothing was cut, single-line ISO timestamp format, and oldest-first cap/rotation.

## Verdict

No regressions, no blockers. #151 and #152 are gate-clean.
