# Cross-Platform Path/Resume Review - codex

Scope: path encoding/decoding, session/resume, config path resolution, user profile config dirs, worktree paths. Read-only audit; no code changes.

| file:line | severity | อธิบาย | fix |
|---|---|---|---|
| `src/agent_takkub/chatlog_scanner.py:50`, `src/agent_takkub/chatlog_scanner.py:61`, `src/agent_takkub/chatlog_scanner.py:66`, `tests/test_token_meter.py:61`, `tests/test_resume_session_picker.py:52` | breaks-both | `decode_project_dir()` assumes only drive/separators became `-`, but the repo's own Claude encoder test documents that Claude maps every non-alphanumeric character to `-` (`agent-takkub`, `_`, `.`, spaces all lose information). The resume tests explicitly avoid hyphenated temp dirs because the round-trip is lossy. In a real project named `agent-takkub`, `C--Users-alice-WebstormProjects-agent-takkub` decodes to `C:/Users/alice/WebstormProjects/agent/takkub`, so exact `.resolve()` comparisons against the real cwd fail on Windows and macOS/Linux. | Do not decode lossy folder names for equality. Add a shared `encode_path_for_claude(cwd)` helper (or reuse/move `token_meter.encode_path_for_claude`) and compare `d.name == encode_path_for_claude(cwd_resolved)`. Keep `decode_project_dir()` only for display/search best-effort, not for identity. Add regression tests with `agent-takkub`, `my_app.web`, and a path containing spaces. |
| `src/agent_takkub/chatlog_scanner.py:340`, `src/agent_takkub/chatlog_scanner.py:365`, `src/agent_takkub/chatlog_scanner.py:380`, `src/agent_takkub/remote/notify.py:326`, `src/agent_takkub/remote/notify.py:347`, `src/agent_takkub/remote/notify.py:362` | breaks-both | Both desktop and mobile recent-session pickers scan all profile project dirs, decode each dir name, and require decoded path equality with `lead_cwd(project)`. Because the decoder is lossy, valid sessions under common project paths are silently skipped, producing "no sessions" even when JSONL files exist. This affects the newly added `list_recent_lead_sessions` path and its remote sibling. | Replace the decode/equality filter in both scanners with encoded-name equality against the resolved cwd. Better: factor a single `session_project_dir_for_cwd(config_dir, cwd)` helper and have both desktop and remote list from that exact directory instead of scanning+decoding every sibling. |
| `src/agent_takkub/spawn_engine.py:90`, `src/agent_takkub/spawn_engine.py:107`, `src/agent_takkub/spawn_engine.py:114`, `src/agent_takkub/spawn_engine.py:118`, `src/agent_takkub/remote/api.py:265`, `src/agent_takkub/user_actions.py:306` | breaks-both | Explicit resume (`spawn(resume_uuid=...)`) validates the selected UUID by globbing `*/<uuid>.jsonl`, decoding the parent dir, and comparing the decoded path to the target cwd. The same lossy decode makes a legitimate picker choice fail with `resume_uuid does not match cwd` for project paths containing hyphens/underscores/dots/spaces. Desktop and remote both close the current Lead before calling `spawn(..., resume_uuid=uuid)`, so this can leave the user with the original pane closed and resume rejected. | Validate by looking in `config_dir_for(project)/projects/<encode_path_for_claude(cwd)>/<uuid>.jsonl` directly. Only close the existing Lead after validation succeeds, or make `resume_lead`/the Qt action prevalidate before `orch.close(...)`. |
| `src/agent_takkub/spawn_engine.py:1642`, `src/agent_takkub/spawn_engine.py:1646`, `src/agent_takkub/spawn_engine.py:1650`, `src/agent_takkub/orchestrator.py:2033`, `src/agent_takkub/orchestrator.py:2034` | risky | The 5-minute auto-resume path compares `prior_uuid_cwd == spawn_cwd` as raw strings, while session reports can stamp `session_uuid_cwd` from hook-provided `cwd`. On Windows case-insensitive filesystems, `C:\Repo` vs `c:\repo`, slash differences, or short/long/symlinked forms can represent the same cwd but fail this raw comparison; on macOS, symlinked project roots have the same risk. This does not bleed sessions, but it disables intended `--resume` recovery and starts a fresh `--session-id` after crashes/stuck recovery. | Store a normalized identity alongside the display cwd: `Path(cwd).resolve()` plus `os.path.normcase()` on Windows. Use that normalized value for `can_resume`; keep the original string for UI/logging. Add tests for Windows case/slash variants and a symlinked cwd on POSIX where possible. |
| `src/agent_takkub/config.py:363`, `src/agent_takkub/config.py:392`, `src/agent_takkub/config.py:398` | risky | `lead_cwd()` returns raw configured strings. For multiple relative project paths, `os.path.commonpath()` can return a relative common parent such as `.`; if `Path(".").is_dir()` is true, Lead can spawn in the cockpit process cwd rather than the intended project directory. This is platform-neutral but becomes more fragile across launch methods on Windows/macOS because the process cwd differs between terminal, shortcut, and packaged app launches. | Normalize project paths at load/save time or inside `lead_cwd()` with `Path(p).expanduser().resolve()` before `commonpath()`. Reject or absolutize relative paths in project creation. |

## Notes

- `user_profile.config_dir_for()` itself is conservative: default profile resolves through `default_claude_config_dir()` and custom profiles return the registered path. I did not find a cross-platform break there in this pass.
- `worktree_root()` / `worktree_dest()` use `Path.resolve()` and sanitize role/project components before containment checks; I did not find a path-separator or drive-letter escape in the reviewed code.
