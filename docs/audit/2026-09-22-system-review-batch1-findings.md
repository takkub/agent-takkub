# System review — ชุด 1: บั๊กที่ทีมอ่านโค้ดรายงาน (ยังไม่ verify)

วันที่ 2026-09-22 · 26 ทีมอ่านทีละ subsystem · ข้อดิบ 345 → หลังตัดซ้ำ 335 · high 39 / med 127 / low 169

> สถานะ: **ยังไม่ผ่านการค้าน** (ชุด 2 = tracer + skeptic ต่อข้อ) — ตัวเลข confidence คือความมั่นใจของผู้รายงานเอง

## ตามพื้นที่

| กลุ่ม | จำนวน |
|---|---|
| knowledge | 20 |
| core-brain | 17 |
| lead-inbox | 16 |
| provider-helpers | 15 |
| quota | 15 |
| settings-window | 15 |
| settings-mgmt | 14 |
| editor-services | 14 |
| cli-server-guard | 14 |
| pipeline-tasks | 14 |
| orchestrator-B | 14 |
| boot-update | 13 |
| storage-config | 13 |
| main-window | 13 |
| spawn | 13 |
| core-infra | 12 |
| provider-config | 12 |
| doctor-gates | 12 |
| remote | 12 |
| pty | 12 |
| migration | 11 |
| orchestrator-C | 10 |
| git-resources | 10 |
| orchestrator-A | 10 |
| cli | 9 |
| leftovers | 5 |

## HIGH (39)

| # | conf | file:line | เรื่อง | scenario | fix ที่เสนอ |
|---|---|---|---|---|---|
| 1 | 0.85 | `boot_flow.py:901` | A 'skipped' run_boot_stage result is rendered as a successful migration (page D) — 'ลองใหม่' after a rollback and the disk-space gate both show fake success with the plan's counts | First-time migration fails validate → rollback → `_save_state({'rolled_back_for_version': app_version})`. User clicks 'ลองใหม่' (boot_flow_window.py:2108) → run_migration → run_boot_stage hits auto_migrate_boot.py:633 `if st.get('rolled_back_for_version') == app_version: return _done('skipped', 'pre | Treat `action == 'skipped'` with a non-None plan as `ok=False` (error=result.reason, data_intact=True, promoted/archived/junk_deleted empty) or add a distinct 'skipped' outcome the window renders as ' |
| 2 | 0.95 | `boot_flow_window.py:2221` | GUI boot never runs auto_migrate_boot.run_boot_stage() on a fully-migrated (v2) install → version-marker never re-pinned, new ladder steps and drift-repair never applied (prod proof: marker stuck at 2.1.0 while venv is 2.1.31) | Every normal boot of an installed cockpit: wizard → _proceed_to_migration_check → plan_migration() returns None because layout_state()=='v2' (boot_flow.py:345) → `_on_plan_ready(None)` just `accept()`s. `run_migration()` (the only wizard-side path into run_boot_stage → apply_pending) is reachable so | In `_proceed_to_migration_check`'s worker (or in `_on_plan_ready` when plan is None) call `auto_migrate_boot.run_boot_stage()` (via `flow.run_migration(None)`) before `accept()`, mirroring app._run_au |
| 3 | 0.90 | `boot_flow_window.py:2349` | Boot wizard's worker QThreads are parented to a QDialog that run_boot_flow_gate drops on return → 0xC0000409 abort whenever the gate exits with a worker still running (X/Esc during provider check, #640 deadline, Ctrl+C) | User launches the cockpit, sees 'กำลังตรวจสอบอัพเดต provider…' and clicks the X (or presses Esc): closeEvent/reject only guard PAGE_MIGRATING (lines 2236, 2249), so done() emits flowFinished(True), loop.exec() returns, MainWindow is built (2412) and the local `wizard` is garbage-collected while `_Ca | Keep the wizard alive past the gate (module-level ref or parent it to the QApplication) and `wait()`/`quit()` every entry in `_workers` before returning; give workers no parent (hold them in `_workers |
| 4 | 0.85 | `claude_auth_config.py:177` | Claude Auth override (base_url/api_key/extra_env) silently ignored: pane reads it from the curated CLAUDE_CONFIG_DIR, Settings saves it to the base profile dir | User opens Settings → Claude Auth, sets a base URL + API key (or extra env) for a profile, respawns panes of any real project (project != 'default', TAKKUB_SKILL_GATE unset/1 = default). spawn_engine.py:3342-3343 sets env['CLAUDE_CONFIG_DIR'] to the curated dir (pane_env.py:544-545 → user_profile.cu | Either add `_AUTH_FILENAME` to the mirror list in ensure_curated_claude_config_dir (user_profile.py:853), or make apply_claude_auth_overrides resolve the BASE profile dir (user_profile.config_dir_for( |
| 5 | 0.30 | `config.py:641` | load_projects() fail-open to an empty registry is persisted by the next writer (set_open_tabs/clear_active_project) → every project wiped from the V2 registry | Post-#504 the V1 projects.json is archived, so the V2 registry is the only copy. If one read of registry.json fails transiently (OSError/PermissionError from an AV/backup tool holding the file, or a UnicodeDecodeError), load_projects logs a warning and returns `empty` ({"active": None, "projects": { | Make load_projects distinguish 'file exists but unreadable' from 'no file' (raise or return a sentinel), and have save_projects_json/set_open_tabs/clear_active_project refuse to persist when the read  |
| 6 | 0.95 | `core/accounts/facade.py:74` | V2 account override clobbers the curated CLAUDE_CONFIG_DIR (#563 skill gate) on every claude spawn | User opens any real project with the claude provider (V2 router is default-ON, TAKKUB_SKILL_GATE unset). spawn_engine.py:3343 `inject_curated_claude_config_dir` sets CLAUDE_CONFIG_DIR to DATA_HOME/providers/claude/<profile>-<slug> (curated skills + filtered enabledPlugins), then spawn_engine.py:3349 | Either (a) in spawn_engine.py move `_apply_v2_account_env_override` BEFORE `inject_curated_claude_config_dir` and pass the resolved `account.config_dir` as `base_dir` to `ensure_curated_claude_config_ |
| 7 | 0.85 | `core/migration/promote_v1.py:247` | _SHARED_DIR_LEGACY_GLOBS 'agents/*.md' archives the live custom-role .md files spawn reads from | User creates a custom role in Role Manager after migration. custom_roles.create_role() writes CUSTOM_AGENTS_DIR/<name>.md (custom_roles.py:211-223) where CUSTOM_AGENTS_DIR = SETTINGS_HOME / "agents" (config.py:515) = DATA_HOME/agents/<name>.md on prod, and spawn reads that exact path (config.py:1011 | Remove 'agents/*.md' from _SHARED_DIR_LEGACY_GLOBS, or only archive an agents/<role>.md when the V1 custom-roles.json source still exists and the role is not present in the V2 registry; never archive  |
| 8 | 0.80 | `core/migration/promote_v1.py:1220` | A late-write-kept entry leaves the archive WAL forever: resume never rescans, re-created live files are re-deleted or clobber the archived V1 snapshot, validate() stays red every boot | After the 11/9 archive, `_prune_phase` kept a live-rewritten file (late_write_kept non-empty) so `ledger.clear()` at 1220-1221 never ran. On every later apply_pending pass: apply_copy_only() sees ledger.exists() and rebuilds entries ONLY from the WAL (2675-2678, T6 'never a fresh scan'), so bin/, pr | When every WAL entry is SOURCE_PRUNED at resume time, clear the WAL and take a fresh candidate scan; treat a source that reappears AFTER its entry was PRUNED as a new file (never refresh dest / never  |
| 9 | 0.95 | `core/migration/promote_v1.py:2449` | ArchiveV1LegacyStep sweeps LIVE top-level state (remote.json, theme, graft store, cloudflared bin) into the archive on every apply pass | Installed build: SETTINGS_HOME == DATA_HOME (config.py:228-230). Live writers keep state at DATA_HOME top level: remote/config.py:22 `_PATH = SETTINGS_HOME / "remote.json"` (remote token/secret/public_url/password hash), theme_settings.py:33, performance_settings.py:69, plan_tier.py:50, provider_sta | Default-deny: archive only names known to be V1 (LEGACY_MAPPING sources + _SHARED_DIR_LEGACY_GLOBS + an explicit allowlist), never 'everything not in a skip list'. At minimum add every live SETTINGS_H |
| 10 | 0.75 | `core/migration/registry_copy_step.py:39` | write_json_atomic never invalidates the read cache -> post-apply validate() reads pre-write data for 3 s | apply_pending() re-applies an already-applied domain step (readonly-registries/capability/state/role-agent/project) whose validate() went red: engine.py:598 `s.validate()` reads the target via legacy_reader.read_json -> cached_read (target mtime old => cached with recent=False); apply() then write_j | Call agent_takkub.cached_read.invalidate(path) at the end of write_json_atomic (same rule the memory note already mandates for every cached_read writer), or have each step's validate() read targets wi |
| 11 | 0.80 | `core/scheduling/facade.py:128` | RAM-derived max_panes_global is sampled once at cockpit boot and never refreshed; Settings/cache invalidation never reaches the live governor | `effective_slot_policy()` is called exactly once — `ResourceGovernor.__init__` (resource_governor.py:329) from `Orchestrator.__init__` (orchestrator.py:1806) — while the cockpit is still booting (WebEngine + boot sweep eating RAM). `_ram_derived_max_panes_global()` = floor((available - total*min_ava | Re-evaluate the RAM cap at request time: in `_denial_reason` (or a cheap throttled helper) recompute `max_panes_global` when the policy leaves it None instead of freezing it in `__init__`; keep the pa |
| 12 | 0.50 | `core/storage/v2_target.py:210` | read_data serves a 3s-TTL cached object to read-modify-write callers → cross-process lost update / duplicate issue numbers on state/issues/local.json | Cockpit process reads local.json at T0 (issues._load_local_issues → read_data → read_cached, entry cached with recent=False). A pane runs `takkub issue "..."` (CLI process, cli.py:6649 → issues.cmd_issue_new) which writes local.json at T0+1s. At T0+2s the cockpit's auto_issue_capture.py:353 / auto_i | Read-modify-write stores must bypass the TTL: have _load_local_issues (and any writer path) call cached_read.invalidate(target) before read_data, or add a `fresh=True` parameter to read_data/read_json |
| 13 | 0.95 | `custom_roles.py:160` | save_custom_roles never invalidates cached_read → every read within 3s is stale (registry lost-update, delete no-op, create not registered) | Registry read via legacy_reader.read_json → cached_read (3s stat-skip TTL); writer uses registry_copy_step.write_json_atomic which does NOT call cached_read.invalidate(). Reproduced with venv python + temp file: load → save {a,b} → load returns ['a'] only; delete_role('b') returns True while b stays | In save_custom_roles call `from .cached_read import invalidate; invalidate(path())` right after write_json_atomic (same contract v2_target.write_data already follows), or route the write through v2_ta |
| 14 | 0.85 | `editor_widget.py:153` | UTF-8 BOM file gains one extra BOM on every Ctrl+S (data corruption) | Open any BOM-prefixed file (PowerShell .ps1, .csproj, files saved by Notepad/VS on Windows) in the Monaco editor and press Ctrl+S. The file on disk now starts with EF BB BF EF BB BF; each further save appends another BOM and Monaco shows a growing run of invisible U+FEFF chars at line 1. | In read_file_for_editor use `encoding="utf-8-sig"` (or `text.lstrip("﻿")`), and in editor_service._encode_for_write strip a leading U+FEFF from `text` before prepending _UTF8_BOM. |
| 15 | 0.80 | `editor_widget.py:628` | Re-opening an already-open file silently discards unsaved edits | User edits a file in the editor (dirty), then double-clicks the same file in the Explorer or clicks its row in the CHANGES panel (main_window.py:542 -> open_file(show_diff=True)). EditorHost.open_file has no already-open short-circuit: it re-reads disk and calls editorOpenFile, and the JS side repla | In open_file, if the resolved path is already in _open_paths: skip the worker, run_js to activate the tab (and dispatch _on_diff_requested when show_diff) instead of editorOpenFile; alternatively make |
| 16 | 0.65 | `lead_inbox.py:1761` | Feedback-prompt auto-skip in _send_when_ready._check has no cooldown/attempt cap → '0\r' written every 150 ms poll; delivery stays prompt-blocked | A gemini/agy teammate shows the CLI survey ('How's the CLI experience so far? … [0] Skip') while a task is being delivered. `_prompt_block_reason` (line 223-227) calls `is_at_feedback_prompt()` with NO provider → generic regex over the FULL screen (pty_session.py:2805,2818). `_check` runs every 150  | Reuse PaneState.feedback_prompt_dismiss_ts/attempts here (skip if now-ts<3.0 or attempts>=3), and do not count 'feedback' as prompt_blocked once a skip has been sent within the last few seconds. Also  |
| 17 | 0.85 | `limit_autoresume.py:109` | Weekly-window quota hit is declared a false positive: deny verdict checks only five_hour / codex primary | Max user's claude pane hits the 7-day cap (seven_day=100%, five_hour=30%) and shows the quota banner. Watchdog -> _maybe_auto_resume_park -> confirm thread -> confirm_verdict_for_provider('claude') -> _usage_confirms_limit False (five_hour<95) -> _usage_denies_limit returns (True, 30.0) -> 'denied'  | Only return 'denied' when EVERY known window (five_hour, seven_day, seven_day_sonnet; codex primary AND secondary via p_usage.windows) is below threshold; treat any window >= threshold as 'confirmed'. |
| 18 | 0.80 | `main_window.py:975` | Preset roles spawn into whichever project is active 15 s after boot, and boot-restored tabs steal focus/active | projects.json has A active with presets=[frontend] and open_tabs=[A,B]. Boot: A's tab is built, then at 2.5 s `_open_project_tab('B')` sets B active + current. At 15 s `orch.spawn('frontend')` (no project) resolves the active project = B, so the preset pane opens in B's cwd. Also every boot with 2+  | Capture `initial_project` and pass `project=initial_project` to the preset spawn lambdas; give `_open_project_tab` a `make_current: bool` parameter and pass False from the boot restore loop (then re-s |
| 19 | 0.90 | `main_window.py:1062` | Lead never spawns while the cockpit is not the foreground app, and the gate poll floods events.log at 20 lines/s | User launches the cockpit (or `takkub restart` from the phone) and alt-tabs away / locks the screen before the window becomes active. `_spawn_lead_when_quiet` requires ApplicationActive, so the Lead pane stays empty until the user clicks the window; meanwhile a `boot_lead_gate_blocked` line is writt | Drop `app_active` from the gate (or accept ApplicationInactive after N turns / a timeout), and log `boot_lead_gate_blocked` once per reason change instead of every poll tick. |
| 20 | 0.65 | `main_window.py:1524` | Lead-spawn failure on the only/new tab destroys the shared usage corner; status ticks raise forever and later tab switches abort before set_active_project | All tabs closed (empty state), user opens a project via + whose Lead spawn returns not-ok (provider binary missing, role/preset gate, PTY error). The failed tab is removed and deleteLater'd while it still hosts `_usage_corner` (meter + performance chip). From then on `_update_status` raises RuntimeE | Factor the corner detach (`setCornerWidget(None)`, `_usage_corner.setParent(None)`, `_limit_label_host = None`) into a helper and call it in the `_open_project_tab` failure branch (and in `_on_tab_swi |
| 21 | 0.85 | `main_window.py:1588` | Closing a project tab leaves its teammate panes registered in the orchestrator; reopening the project can never spawn those roles again | User closes project A's tab while it has a qa pane, later reopens A (+ picker) and Lead runs `takkub assign --role qa`. spawn() finds the dead qa AgentPane in the registry, never emits paneRequested, launches the provider process, then attach_session raises RuntimeError on the deleted terminal -> 'f | In `_close_project_tab`, after `close_all_teammates`, call `self.orch.unregister_pane(role, project=tab.project_name)` for every pane in the saved `teammate_panes` list (or pop the whole `_panes_by_pr |
| 22 | 0.60 | `orchestrator.py:14628` | Stuck-recover respawn reads last_spawn_resumed before a deferred/queued spawn has run → full task re-pasted into a --resume'd conversation (Bug-5 duplicate work) | Machine is overloaded (exactly when panes wedge). `_check_stuck_panes` → `_auto_recover_stuck` → close() → 2 s later `_do_respawn` restores session_uuid + `_recent_exits` (14552-14581) then calls spawn(). spawn() hits the resource-governor gate or the FIFO arbiter and returns `(True, '… spawn deferr | Decide replay-vs-nudge after the session actually attaches: move the `snap_task`/`_STUCK_RESUME_NUDGE` choice into a callback keyed on the spawn completing (e.g. check `msg` for 'deferred'/'queued' an |
| 23 | 0.60 | `orchestrator.py:15042` | Quota banner still on screen after reset is re-detected and rolled +24h (or re-added duration) → phantom new quota episode, Lead notice, park/reroute | claude teammate hits its quota at 10:00 with banner '<quota-marker> … resets 3pm'. At 15:00 the QTimer fires `_emit_rate_limit_reset` (or the next tick clears via line 15033-15037), `rate_limited_until` becomes 0. The banner text is still in the visible screen (claude does not clear it; display_line | After a reset (both in `_emit_rate_limit_reset` and the `now >= existing` branch of `_rate_limit_suppressed`) set `ps.quota_false_positive_armed = True` (or a dedicated `quota_reset_seen_marker`) so t |
| 24 | 0.90 | `pane_guard.py:827` | strip_takkub_text_payload splits on newline BEFORE blanking quotes — multi-line assign/done text is classified as commands (#649 regression) | Lead runs `takkub assign --role backend "<spec>"` where the spec has a line starting with `npm run build` / `pytest` / `tsc` while any other pane is 'working' (machine-state.json fresh). classify() line 4015-4029 returns busy_machine:pm_build and Lead cannot hand out the task — the exact #649 sympto | Split with the quote-aware `_split_chain_segments` (line 2207) or apply `_QUOTED_RUN.sub('""', ...)` to the whole command when `_TAKKUB_TEXT_CMD` matches at a segment start; then strip once at the top |
| 25 | 0.85 | `pane_guard.py:4135` | full_suite / scope_tiny / browser / git rules run on the raw command — a `takkub done` note quoting a runner or git verb is denied | A backend pane reports `takkub done "ran:\npytest\nall green"` (or a note with a fenced block whose line starts with `jest`, `vitest run`, `git push ...`). The hook denies the done call with full_suite:pytest / git_lead_only:push; the pane retries (#399 loop) and Lead must recover the pane by hand. | Compute `code = strip_takkub_text_payload(cmd)` once after `_unwrap_shell_wrappers` (line 3980) and evaluate every rule against `code` (keep `raw_cmd` only for the GIT_* env scans). |
| 26 | 0.90 | `pane_tools_policy.py:234` | save_policy never invalidates cached_read: regen + CLI verify read stale policy, `takkub mcp allow/deny` reports 'this is a bug' and variant file keeps the old MCP set | User runs `takkub mcp deny --role qa playwright` (policy file last written >2s ago, i.e. always in practice). deny_item() calls load_policy() (populates cache with hit[3]=False), save_policy() writes via registry_copy_step.write_json_atomic (no invalidate), then _regen_role_variants_best_effort() -> | In save_policy call `from .cached_read import invalidate; invalidate(path())` immediately after write_json_atomic (L234) and after `path().unlink(missing_ok=True)` (L198), before _regen_role_variants_ |
| 27 | 0.75 | `qa_gate.py:1461` | Node auto-targeted tier: `vitest related` without --passWithNoTests fails the gate when a changed file has no related spec | Specialist edits a component/util in a vitest project that has no spec importing it, runs `takkub qa-gate --auto`. classify_diff → 'targeted' → step `test:vitest-related` runs `npx --no-install vitest related --run <files>`; vitest finds no test files and exits 1 ('No test files found, exiting with  | `pm_exec(pm, "vitest", "related", "--run", "--passWithNoTests", *files)`. |
| 28 | 0.85 | `release.py:401` | release rollback leaves the annotated tag behind → every retry refused with 'tag already exists' | `takkub release` commits (l.387) and tags (l.391), then `build_wheel` (l.398) fails (no `build` module, stale build/ dir, 300s timeout). The except block resets the commit and restores the 4 files but never runs `git tag -d`, so `vX.Y.Z` now points at an orphaned commit. Re-running `takkub release`  | Track `tag_created = True` after l.391 and in the except block run `_git(repo_root, "tag", "-d", tag)` (best-effort) before the reset; or build the wheel before tagging. |
| 29 | 0.95 | `remote/__init__.py:172` | Boot-time remote start builds the Qt bridge on a worker thread: every bridged /api/* route answers 504 after each cockpit boot | remote.json enabled=true (the user's prod config). Cockpit boots -> main_window._start_remote_off_thread runs RemoteControl.prepare() via cli.run_off_thread (a plain daemon threading.Thread, cli_server.py:309). prepare() -> _start_blocking() -> http_server.start_server() -> RemoteHttpServer.__init__ | Create `_Bridge` on the Qt thread: give RemoteHttpServer a `bridge=None` slot, construct/attach it in RemoteControl._start_qt() (or call `self._server.bridge.moveToThread(QCoreApplication.instance().t |
| 30 | 0.90 | `remote/tunnel.py:461` | Quick-tunnel / ngrok-random URL is only ever harvested by the Settings-dialog 6-second poll; after a cockpit restart the pairing URL is the previous run's dead hostname (or blank) | tunnel.type = quick (or ngrok url_mode=random). (a) Restart the cockpit: _start_blocking() builds `Tunnel(self.config.tunnel, self.config.public_url, port)` with the *.trycloudflare.com hostname saved last session; Tunnel.__init__ sets `captured_url = public_url or None`, so _scan_for_url (line 638  | For quick/ngrok-random, always start with captured_url=None (ignore the stale public_url) and have RemoteControl expose a 'tunnel_url_ready' callback/timer used by both boot (main_window) and Enable p |
| 31 | 0.85 | `role_messages.py:108` | _write_all never invalidates cached_read → lost `takkub send` records and mark_delivered silently no-ops (→ spurious replays) | `_reap_role_messages` (orchestrator.py:5628) reads messages/<ns>.jsonl every 5s tick, warming the cache with recent=False. Lead runs two `takkub send` within 3s (or one send followed by its own `_confirm_role_message` ~1-2s later, orchestrator.py:5606): the second `read()` hits `read_cached` line 82 | In `_write_all`, after write_text call `from .cached_read import invalidate; invalidate(path)` (same pattern as backlog.py:130 / provider_config.py:128). Optionally write via tmp+os.replace. |
| 32 | 0.65 | `service_spawner.py:242` | service-stop / service-list trust a bare PID: PID reuse kills an unrelated process tree | A registered service (e.g. cloudflared) dies; Windows recycles its PID for a pane's node.exe/claude process. `takkub service-list` keeps showing the service alive (never pruned), and `takkub service-stop <name>` runs psutil.Process(pid).kill() plus all children on that unrelated process — e.g. a liv | Record `psutil.Process(proc.pid).create_time()` in ServiceRecord at spawn; in _pid_alive/stop treat the row as dead unless `abs(Process(pid).create_time() - rec.create_time) < 1.0`. |
| 33 | 0.90 | `settings_knowledge_design.py:295` | Second click on Knowledge Refresh / Design Tools Refresh / Test raises RuntimeError (deleted _CallableThread) — button does nothing and an auto-issue is filed | Knowledge tab → Refresh → wait for result → Refresh again: `_cleanup` (148) already ran `deleteLater()` on the thread, so `self._kd_knowledge_thread.isRunning()` raises 'wrapped C/C++ object of type _CallableThread has been deleted' inside the slot → nothing happens on screen; app.py's excepthook ro | Wrap the three `isRunning()` checks in try/except RuntimeError that sets the attribute to None (copy settings_usage.py:591-597), or have `_cleanup` also clear the owner's reference. |
| 34 | 0.80 | `settings_window.py:1641` | Save & Apply under solo-lead/pair persists rolesEnabled=False for every position; later switching to full/auto shows all positions OFF, flips the project to 'custom' or leaves assign rejected | Project on 'ทำเอง' (solo-lead) or 'คู่' (pair) → open Roles page → change anything (e.g. a provider) → Save & Apply. Every position toggle was rendered OFF (2549: `enabled=_row_enabled_now(...)` → `_can_spawn_from_cfg` returns False for positions under those presets, team_preset.py:99-112 roles all  | Only persist rolesEnabled for toggles the user actually changed (diff against the values rendered at build time), or render the toggle from `is_role_enabled` alone and show preset exclusion as a separ |
| 35 | 0.90 | `settings_window.py:4376` | Autoskills scan/install QThreads are parented to the WA_DeleteOnClose SettingsWindow (#688 pattern) — closing Settings mid-scan kills the whole cockpit | Skills page → click 'ดึง skill ตาม stack' (autoskills CLI, up to ~60s) → press Cancel/Esc/X on Settings while it runs → SettingsWindow deleteLater destroys its child QThread while running → Qt qFatal 'QThread: Destroyed while thread is still running' → fail-fast 0xc0000409, no Python traceback. Same | Construct both autoskills threads unparented, keep them alive in a module-level set (mirror `_CATALOG_THREADS` at 841/1071-1073), connect `finished` → discard + `deleteLater`; PyQt auto-drops the `res |
| 36 | 0.80 | `settings_window.py:4420` | autoskills QThreads parented to WA_DeleteOnClose SettingsWindow — close mid-run = Qt fatal abort (#688 class) | Settings → SKILL → click 'ดึง skill ตาม stack' (preview via `npx --yes autoskills@latest`, up to 60s) or confirm an install (up to 120s), then press Esc/Cancel/X on Settings while it runs. SettingsWindow is WA_DeleteOnClose (line 956) so the QThread parented to it is destroyed while still running →  | Parent both threads to None (or the main window), keep the reference on self, and in SettingsWindow.closeEvent either wait() on a running thread or disconnect resultReady and let it finish detached; s |
| 37 | 0.75 | `shared_dev_tools.py:1325` | ensure_user_mcps() deletes MCPs the user added via `takkub mcp add` / Tools dialog on next boot (prunes everything not in ~/.claude.json) | User adds an MCP with `takkub mcp add context7 npx ...` (cli.py:5267 → add_mcp_server, writes ONLY runtime/shared-mcp.json). At the next cockpit boot orchestrator.py:1647 calls ensure_user_mcps(): if ~/.claude.json has at least one credential-free mcpServers entry (to_merge non-empty) the prune loop | Track provenance: only prune entries that were themselves merged from ~/.claude.json (e.g. tag merged entries with `"_source": "claude.json"` or keep a runtime/user-mcps-merged.json list) and leave ad |
| 38 | 0.70 | `spawn_engine.py:4410` | Crash auto-respawn replays the task even when spawn() only deferred/queued, so a later --resume gets the task twice | A teammate crashes (or the user types /exit) while a modal is open (Settings dialog -> _spawn_gate_pred True) or while another pane's ConPTY spawn is in progress (fan-out). 2.5 s later _auto_respawn calls spawn(), which returns (True, 'spawn deferred (gate blocked)' / 'spawn queued (arbiter busy)')  | Only replay when the spawn actually attached a session in this call (e.g. return a structured result / check pane.session.is_alive and that last_spawn_resumed was stamped by THIS spawn), or defer the  |
| 39 | 0.90 | `task_ledger.py:155` | Ledger state saved without invalidating cached_read → second assign within ~3s overwrites the first (row + open pointer lost) | Lead fires two assigns in one burst (e.g. `takkub assign --role frontend … && takkub assign --role backend …`, a `--shards N` / `--plan` fan-out whose shards are staggered 400 ms apart by `_fire_qa_plan_fanout`, or `_drain_fanout_queue`). Assign #1: `_load_state` stats the file (mtime old → cached w | In `_save_state` after `_atomic_write(...)` add `from .cached_read import invalidate; invalidate(_state_path(project))` (mirror backlog.py:128-130). Optionally also invalidate in `_atomic_write` calle |

## MED (127)

| # | conf | file:line | เรื่อง | scenario | fix ที่เสนอ |
|---|---|---|---|---|---|
| 1 | 0.65 | `agent_pane.py:664` | attach_session steals keyboard focus to every newly spawned pane, including input-locked teammates | User is typing into Lead. Lead runs `takkub assign` (or auto-respawn / limit_autoresume fires) -> spawn_engine._launch_session -> pane.attach_session() on the main thread -> `self._terminal.setFocus()` -> QWebEngineView of the new teammate pane takes focus. Teammate panes are input-locked by default | Only call `self._terminal.setFocus()` when `not self._lockable` (user-driven Lead/Shell), or drop it entirely and rely on user_actions' explicit shell focus. |
| 2 | 0.90 | `app.py:1176` | Restart-successor boot path never starts auto_issue_signals.start_watch() | After any `takkub restart` / status-bar restart / auto-update relaunch (TAKKUB_RESTART_SUCCESSOR=1), the cockpit boots through the early-return branch, so the #297 events.log pattern watcher (freeze / lost-delivery / watchdog auto-issues) is silently off until the next cold start. | Extract the post-window setup (signal handlers, deadman, start_watch, show, exec) into one helper used by both branches. |
| 3 | 0.80 | `auto_issue_signals.py:158` | Signal rule listens for event `no_content_pane_recover` that nothing emits → rule can never fire | The watchdog's no-content recovery repeats 3+ times in 6 h (the #569 class of defect this rule was written for) but the emitted event names are `no_content_pane_warned` (lead_inbox.py:3246) / `no_content_pane_respawned` (maintenance.py:50 label), so `scan_for_signals` counts 0 and no auto-issue is e | Point the rule's `event` at the real emitted name (e.g. `no_content_pane_respawned`, or a rule per emitted event) and add a test that every `RULES[*].event` string appears in a `_log_event(` call in s |
| 4 | 0.75 | `auto_migrate_boot.py:448` | [caller of subsystem] Steady-state apply_pending() path walks the entire runtime/ tree every boot and silently skips when free disk < 4x runtime size | _run_apply_pending calls `_disk_has_room(config.DATA_HOME)` every boot on 'v2'/'mixed'; `_estimate_copy_bytes` (170-239) does `_dir_size(data_home / "runtime")` (full os.walk of runtime/, 100k+ files on prod: browser-profiles 1.76 GB, exports 2.07 GB, core 1.16 GB) then doubles it, and the gate requ | Size the steady-state gate from what apply_pending would actually copy (promote candidates + archive candidates + pre-migrate entries), not runtime/ wholesale; log/doctor-surface a disk-space skip. |
| 5 | 0.70 | `autoskills_installer.py:576` | _build_staging_mirror hardlinks the ENTIRE project tree (node_modules/.venv/dist included) into the repo root; a crash strands it there | Settings → 'ดึง skill ตาม stack' → confirm install on a Node/Python project. The walk excludes only `.git` and `.claude/skills` (lines 584-587), not node_modules/.venv/dist/.next, so tens of thousands of hardlinks are created under `<project_root>/.autoskills-staging-<tok>/` (inside the repo, untrac | Prune the same ignore set the Explorer uses (IGNORED_NAMES: node_modules, .venv, venv, dist, build, .next, coverage, __pycache__) and git-ignored dirs; put the staging dir under RUNTIME_DIR/tmp (use c |
| 6 | 0.85 | `backlog.py:376` | `backlog import` raises ValueError on any status cell not in STATUSES → partial import, duplicates on retry | Owner runs `takkub backlog import` on an audit-report markdown table that has a สถานะ/status column ("ยังไม่แก้", "open", "✅ done"). `parse_markdown_table` copies the raw cell into `fields['status']` (line 361); `add_item(status=...)` raises `ValueError('invalid status: ...')` (line 166-167). `backl | Normalise status like severity/impact: add a `_STATUS_WORDS` map (Thai labels + open/todo/done/blocked…) with fallback `"todo"` in `parse_markdown_table`; and/or validate all parsed rows before the fi |
| 7 | 0.75 | `boot_flow.py:615` | Phase-3 'ตรวจสอบ' events are re-labelled phase 4 on a first-time migration (max-phase clamp vs engine.validate() firing after archive) → verify row never active, archive row count overwritten, failed heading names the wrong phase | First-time v1 apply: run_boot_stage runs `engine.apply()` (backup → promote → domain steps → archive copy → deferred prune, all firing on_entry for phases 1/2/4) and only THEN `engine.validate()` (auto_migrate_boot.py:652), which is where `on_validate_step` fires (engine.py:903). By then state['max_ | Exempt phase 3 from the clamp (track validate events with their own phase/clock; only clamp late phase-1/2 prune notifies to the current phase), and for a validate-report failure set `failed_phase=3`. |
| 8 | 0.85 | `boot_flow_window.py:2207` | Provider-update results are discarded and worker exceptions are swallowed unlogged → a failed update looks like 'nothing happened' | User ticks claude on page A and clicks 'อัพเดตที่เลือก (1)'. `run_provider_updates` runs `provider_update.update_provider` which can return STATUS_FAILED (npm lock held by another instance, placeholder-binary check, npm error — provider_update.py:362-373). The window connects `resultReady` to `lambd | After run_provider_updates, re-populate page A rows with the returned statuses and pause on any FAILED row (chip 'อัพเดตไม่สำเร็จ' + message) before proceeding; in every `_WorkerError` branch call `_L |
| 9 | 0.85 | `browser_chrome.py:183` | NativeChromeManager.ensure_started busy-waits up to 8 s (+5 s taskkill on failure) on the Qt main thread inside spawn() | Windows, first spawn of qa/critic/designer after boot, or any spawn after the #406 idle-release killed the cockpit Chrome: spawn_engine.py:2445 calls manager.ensure_started() synchronously on the GUI thread; it Popen()s chrome.exe then loops `_cdp_ready()` (urlopen timeout 0.2 s) + `time.sleep(0.05) | Launch Chrome from spawn() but poll CDP readiness from a QTimer (or daemon thread + signal) and only log/notify when ready; never call close()/taskkill synchronously from the spawn path. |
| 10 | 0.85 | `cli.py:3087` | takkub wait heartbeat/progress lines are block-buffered under a pipe — background wait looks hung until it exits | Lead runs `takkub wait --role backend` as a background shell (the documented use, comments at lines 172-176 and 94-98). stdout is a pipe/file, Python block-buffers it (no PYTHONUNBUFFERED anywhere in the repo, no flush=True), so '[wait] watching…', '[wait] still waiting on…', '[wait] resolved: …' an | In `_ensure_utf8_stdio` call `stream.reconfigure(encoding="utf-8", line_buffering=True)` (or `write_through=True`), or add `flush=True` to every `[wait]` print in cmd_wait/_request_with_retry. |
| 11 | 0.90 | `cli.py:3652` | _utf8_print writes to sys.stdout.buffer behind the TextIOWrapper — output order inverts vs print() when stdout is a pipe (tail/task show/worktree clean) | Lead runs `takkub tail --role codex` from a pane (Bash tool → pipe). cmd_tail does `print(f"[{args.role}] {path}")` (2921, buffered in TextIOWrapper) then `_utf8_print(line)` per line (2926, written straight to .buffer and flushed) → the transcript lines print FIRST and the `[codex] <path>` header L | Make `_utf8_print` call `sys.stdout.flush()` before the raw write, or simply `print(text)` — `_ensure_utf8_stdio()` already reconfigured stdout to UTF-8 in main(), so the raw-buffer path is unnecessar |
| 12 | 0.90 | `cli.py:5474` | assign --effort rejects xhigh/max at argparse although the flag's own help and provider_spec advertise them for claude | Lead reads `takkub assign --help` ('claude alone also takes xhigh/max'), runs `takkub assign --role backend --effort xhigh "…"` → argparse: invalid choice: 'xhigh' (choose from 'low','medium','high'). The provider-aware validator that was meant to do this check (`assign_effort_override_error`, provi | Drop `choices=` on `--effort` (keep the help text) and let `assign_effort_override_error` validate per effective provider; or extend choices to include xhigh/max. |
| 13 | 0.75 | `cli_server.py:929` | assign dedup fingerprint is stamped before the synchronous subagent assign() result and refreshed on every hit — a corrected retry within 8 s is falsely acked as 'deduped' | `takkub assign --role backend --mode subagent --model opus "task"` → orchestrator.assign returns `(False, "model override is not supported in subagent mode")` (orchestrator.py:2976). Lead drops `--model` and reruns the same task within 8 s → same (project, role, task_hash, 'subagent') key → reply `o | Stamp the fingerprint only after a successful dispatch (`ok` True on the sync path, after `_fire_staggered` on the async path) and do not refresh it on a dedup hit; include model/provider/effort/isola |
| 14 | 0.75 | `cli_shim.py:140` | ensure_cli_shims is never idempotent for takkub.cmd on Windows (CRLF read as LF) — rewrites + os.replace on every spawn, racing panes executing the shim | Installed (prod) cockpit on Windows: `_pane_cli_bin_dir` (spawn_engine.py:235) calls `ensure_cli_shims` on every pane spawn. `_cmd_shim` content is joined with `\r\n` (line 47) but `target.read_text(encoding="utf-8")` (140) applies universal newlines and returns `\n`, so `current != content` is alwa | Compare bytes: `current = target.read_bytes().decode("utf-8")` (or `target.open(encoding="utf-8", newline="")`) so CRLF survives the comparison; optionally use a per-process tmp name to avoid two writ |
| 15 | 0.75 | `config.py:624` | load_projects() reads the V2 project registry/projects.json uncached on every 2s status tick from the Qt main thread | status_header._status_timer fires every 2s (status_header.py:650-653 → _update_status) and calls _refresh_overage_chip (line 994), _refresh_plan_badge (line 1014) and _update_provider_chip (line 667), each doing config.active_project() → load_projects() → `registry_path.exists()` + `read_text()` + j | Route both reads in load_projects through cached_read.read_cached(path, json.loads, missing=None) (it already has the invalidate hook in _write_json_atomic), keeping the same fallback/warning behaviou |
| 16 | 0.55 | `core/brain/store.py:77` | BrainStore appends via whole-file read-modify-replace with no lock -> concurrent done() threads silently drop memory records | BrainStore builds JsonlStore(path) with the default true_append=False, so every append_event reads the whole project .jsonl, writes a tmp and os.replace()s it (jsonl_store.py:56-62). orchestrator.done()/subagent_done() start a NEW daemon thread per done for brain.facade.on_pane_done (orchestrator.py | Construct BrainStore's JsonlStore with true_append=True (the brain log is append-only by design, store.py:5-12) or guard append_event/load with a module-level threading.Lock per path. |
| 17 | 0.75 | `core/conversation/_json_io.py:17` | Conversation JSON writers never invalidate the stat-TTL read cache -> stale summary/cursor reads for 3 s | summary.load_summary, store.get_conversation and cursor_store.get_cursor/set_cursor all read through core.storage.legacy_reader.read_json -> cached_read.read_cached, which (cached_read.py:82) serves a cached entry for 3 s WITHOUT a stat when the file was not recently written. write_json_atomic (used | Call `cached_read.invalidate(path)` right after os.replace in write_json_atomic (and in JsonlStore.append for true_append paths read via read_json, if any). |
| 18 | 0.90 | `core/conversation/ingest/claude_adapter.py:25` | Claude transcript ingest looks in the wrong project dir and falls back to an unrelated session | Any claude teammate pane runs `takkub done`. orchestrator.done() (orchestrator.py:7936-7946) spawns conversation.facade.on_pane_done -> ingest_provider_transcript -> claude_adapter.resolve_source(cwd, session_id). It calls find_session_by_uuid(cwd, session_id, config_dir=None) with no project_ns/bas | Thread the pane's `_claude_config_dir` and `_claude_project_dir_name` (already on PtySession, agent_pane.py:888-889) through facade.on_pane_done -> ingest_provider_transcript -> adapter.resolve_source |
| 19 | 0.55 | `core/conversation/ingest/codex_adapter.py:123` | Non-claude ingest binds the newest session in cwd with no spawn-time bound -> sibling pane's transcript attached to this role | codex/gemini/cursor/kimi panes never get a cockpit-minted session id (agent_pane.py:869-872, token_meter.py:470-476), so done() passes session_id=None and codex_adapter.resolve_source -> _scan_root_for_cwd picks the newest rollout whose cwd matches (102-120); gemini_adapter.resolve_source (74-78) an | Plumb the pane's spawn timestamp (`pane._spawn_ts`) through facade.on_pane_done -> ingest_provider_transcript -> resolve_source(cwd, session_id, not_before=...) and skip candidates older than it, mirr |
| 20 | 0.85 | `core/conversation/summary.py:126` | After one `done --fail`, stale `next: fix:` and `pending:` lines are injected into every later assign for that role | qa runs `takkub done --fail "tests red"` -> apply_done_note sets next_action="fix: [qa] tests red" and prepends the entry to pending/warnings (lines 117-125). qa later fixes it and runs `takkub done "tests green"` (different headline). The success branch (126-134) only removes a pending entry equal  | In the success branch set next_action="" and drop pending/warnings entries tagged `[{role}]` (or all entries older than this success); alternatively clear pending on any clean done for the same role. |
| 21 | 0.70 | `core/migration/engine.py:532` | Every apply_pending()/validate() sha256-hashes the entire archive (191k files / 10.5 GB on prod) before MainWindow can open | apply_pending() line 532 calls ArchiveV1LegacyStep.validate() up front; that walks every backups/v1-archive-* generation and _archive_entry_problems() re-hashes every recorded file (promote_v1.py:2986, 1455-1461). validate() (898) and validate_ok_steps() (958) do it again; a WAL resume in apply_copy | Validate archives by manifest presence/size (or a sampled subset) on the boot path and reserve full sha256 verification for `takkub migrate validate`/doctor; cache the disk estimate or skip _estimate_ |
| 22 | 0.70 | `core/migration/promote_v1.py:2587` | One legacy-v2 marker rmdir failure leaves a generation PENDING forever: every later pass is blocked by _stale_generation_problems and restore-v1 hides that generation | Windows: `_rmdir_tree(legacy_root)` (2840) fails once (Explorer/indexer handle on DATA_HOME/v2). Branch 2849-2867 writes the manifest with state PENDING and says 'retry apply to finish', but `_prune_phase` already cleared the WAL (1221), so prune() for that generation can never run again. Next pass: | In the marker-failure branch keep the WAL (or write a marker-only WAL) so prune() can finish the SAME generation; in _stale_generation_problems treat a PENDING generation whose archived entries all ve |
| 23 | 0.60 | `core/migration/promote_v1.py:2675` | Leftover archive WAL pins apply_copy_only to the old entry list; new candidates never archived, archive copy refreshed from live files | _prune_phase leaves the ledger on disk when late_write_kept is non-empty (1220-1221) and the step never clears it afterwards. On the next apply, apply_copy_only() sees ledger.exists() and rebuilds new_entries from the WAL instead of rescanning (2675-2678), so the docstring promise at 816-818 ('kept  | After a successful prune, clear the WAL even when late writes were kept (record kept names in the manifest instead), and have apply_copy_only merge a fresh candidate scan into resumed WAL entries rath |
| 24 | 0.60 | `core/migration/steps_v1.py:1059` | core-internal-store re-apply clobbers live core_home() JSONL/brain stores with the frozen runtime/core copy | After the flip, core_store_path() and brain/store.py write straight into system/ (accounts.jsonl, account_pools.jsonl, account_switches.jsonl, models.jsonl, model_profiles.jsonl, obsidian_dedup.jsonl, brain/<project>.jsonl, conversations/) while runtime/core/ keeps the frozen pre-migration copy. _RE | Exclude every core_home()-resolved store from re-apply (or skip re-apply entirely once target exists and only check existence, as the dir branch already does); never copytree-merge brain/ and conversa |
| 25 | 0.90 | `core/migration/steps_v1.py:1119` | CoreInternalStoreStep.apply() copies the whole system/ store into a new BackupManager slot on every apply and nothing ever rotates slots (1.18 GB on prod) | Every apply() unconditionally runs `self.backups.backup(self.step_id, target)` where target = system/ (conversations, brain, model catalog). BackupManager.backup (backup.py:31-45) always creates a new timestamped slot with a full copytree and has no retention. Pre-#486/#488 the step re-applied on ~e | Skip the backup when apply() is a no-op merge (target already validates), and add per-step slot retention in BackupManager (keep N newest, or one per app version). |
| 26 | 0.40 | `core/storage/jsonl_store.py:69` | read_all decodes strictly (no errors='replace') so a torn/partial UTF-8 line raises UnicodeDecodeError instead of counting as corrupt — silently kills the V2 conversation done-hook thread | V2 conversation is ON by default (conversation/flag.py:1). On every done(), orchestrator.py:2574-2579 runs facade.on_pane_done on a bare daemon thread (no try/except); it appends messages via JsonlStore(true_append=True) (conversation/store.py:148) and reads back bindings/messages via read_all (stor | In read_all use `self._path.read_bytes().decode("utf-8", errors="replace")` (same as read_from) and have JsonlStore.append write `line + b"\n"` in one write() call; wrap facade.on_pane_done's thread b |
| 27 | 0.60 | `core/storage/v2_target.py:199` | write_data → registry_copy_step.write_json_atomic: unconditional fsync (file + dir) and no PermissionError retry on os.replace — every V2 settings save stalls the main thread and can fail unhandled on Windows | User changes a role's model/provider in Settings → role_models.py:161/205 write_data (twice) + provider_config.py:124 _write_routing → each call does tmp.write_text + os.fsync(tmp) + os.replace + dir fsync on the Qt main thread. #658 measured FlushFileBuffers under an AV filter at ~1s and made fsync | Give write_json_atomic a `durable: bool` flag (default False for v2_target.write_data, provider_config._write_routing, pane_tools_policy, skill_policy, custom_roles, auto_issue_capture; True only for  |
| 28 | 0.85 | `custom_roles.py:233` | create_role silently overwrites an existing custom role's registry entry and instructions .md | User opens Settings → New Role and types the id of a custom role that already exists (typo / forgot it exists). validate_role_name only rejects built-in names; create_role does `previous = roles.get(name)` and proceeds, then `tmp_path.replace(role_file)` replaces the role's hand-written .md with the | In create_role return (False, 'มี custom role นี้อยู่แล้ว — แก้ไขแทน') when name in load_custom_roles() or role_file.exists(); make the UI route an existing name to the edit path instead. |
| 29 | 0.70 | `db_preflight.py:91` | DB preflight false-negatives (mongodb+srv, prod DATABASE_URL) make qa-gate fail-fast and skip the entire test tier | Project .env has `DATABASE_URL=mongodb+srv://cluster0.x.mongodb.net/app` (Atlas): urlsplit gives port None → `_DEFAULT_PORTS['mongodb+srv']` = 27017 → socket.create_connection((srv-hostname, 27017)) fails (an SRV name has no A record / no listener) → ok=False → qa_gate.py:1843-1848 marks every remai | Treat `mongodb+srv` (and any scheme without a resolvable literal port) as skipped; only fail-fast when the URL var is TEST_DATABASE_URL or the host is loopback/docker-local, otherwise downgrade an unr |
| 30 | 0.60 | `digest_facts.py:153` | detect_investigate_task falls back to the DONE NOTE headline → a note saying 'ready for review' mutes the #278 zero-files alarm on an implementation task | backend is assigned 'implement X'. It reports `takkub done` with note 'Implemented X — ready for review' but touched 0 files (the #278 incident shape). orchestrator.py:8078 passes `raw_note`; the first line matches `\b(review)\b` → investigate_task=True → `_files_bit` renders 'ไฟล์ที่แตะ: 0 (งานตรวจ | Only accept the weak headline words from the ASSIGNMENT text; for the note fallback require a strong marker (`_INVESTIGATE_STRONG_RE`) or drop the note fallback entirely. |
| 31 | 0.90 | `doctor.py:367` | Default `takkub doctor` reads every transcript line of the last 14 days (measured 301 files / 410 MB / 10.6 s here) for the tools-drift check | `check_claude_tools_drift` is in `run_all_checks()` (l.234 via check_claude) so every `takkub doctor`, `doctor --fix` (which runs it twice, cli.py:3661+3665) and the Doctor button walk `~/.claude/projects/**/*.jsonl` (+ DATA_HOME/claude-config/projects on prod) and json-parse each tool_use line. Mea | Cap to the N most-recent files / a total byte budget, or cache the extracted tool set keyed by (path, mtime, size) under runtime/, and make the full scan opt-in (`--tools-drift`). |
| 32 | 0.80 | `editor_widget.py:738` | Editor diff-vs-HEAD uses roots[0] as the git repo root -> wrong/empty diff for multi-root or sub-directory roots | Project has two configured paths (web, api) or a single path that is a subdirectory of the repo. Diff on a file under the 2nd root: `relative_to(roots[0])` raises -> read_head_blob returns None -> whole file rendered as newly added. For a sub-dir root, `git show HEAD:<rel-to-subdir>` resolves from t | Resolve the repo root with git_changes_service.resolve_repo_root(resolved.parent) (or the configured root that contains the file) and compute `rel` against it in build_diff_result/read_head_blob. |
| 33 | 0.45 | `gemini_helper.py:370` | agy workspace miss-cache keyed on the main .db (mtime,size) never invalidates under WAL — a session probed too early stays 'unknown workspace' for the cockpit's lifetime | gemini pane spawns; the notifier's dir-signature check (conversations dir mtime bumps when the new .db/-wal/-shm appear) triggers an immediate rescan; _antigravity_workspace() opens the brand-new db before agy has committed the trajectory_metadata_blob row (or gets SQLITE_BUSY during -shm setup, tim | Fold the `-wal` sibling's (mtime_ns,size) into `sig` (or stat both and take the max), and give miss entries a short TTL (e.g. 10 s) so a transient lock/early probe cannot pin a live session. |
| 34 | 0.55 | `gemini_helper.py:426` | First cold find_antigravity_sessions walk still runs synchronously on the caller — the Remote notifier QTimer (main thread) can be that caller (#701 residual) | Boot with a live gemini Lead and Remote on: notify.py:2201 `_resolve_jsonl` → :924 resolve_antigravity_transcript → find_antigravity_sessions(cwd, limit=1) runs on the 200 ms QTimer; the single-flight only makes a SECOND concurrent caller return []. The first caller does the full 500-db sqlite walk  | Warm the cache from a daemon thread at boot / on gemini pane spawn, and have the main-thread resolver ALWAYS use the non-blocking path (return [] until warm) — e.g. pass `block=False` from notify's re |
| 35 | 0.45 | `git_changes_service.py:310` | _read_current_text can raise OSError out of _DiffWorker.run → diffReady never fires, editor diff pane shows nothing | User clicks a file in the Explorer CHANGES panel that another process holds open without share-read (Windows, e.g. a running app's db/log) or that a pane deletes/renames between the `exists()` check and `stat()`/`open()`. `resolved.stat()` / `resolved.open("rb")` raise PermissionError/FileNotFoundEr | Wrap the body of diff_sync (or _DiffWorker.run) in `except OSError as exc: return DiffResult(path=..., unified=None, error=f"{type(exc).__name__}: {exc}")` so the UI gets an error row. |
| 36 | 0.35 | `git_status.py:62` | git subprocesses spawned on worker threads bypass project_file_index.SUBPROCESS_LOCK — the exact concurrent-CreateProcess pattern #375 documented as an interpreter crash | Git tab visible (task_dock._GitStatusWorker on QThreadPool runs git_status.collect every 30 s) while the Explorer's _StatusWorker/_DiffWorker (lock-holding) and cli_server._run_off_thread's daemon thread (WorktreeManager()._default_runner for collect_done_git_facts on every `takkub done`) all call s | Have git_status._run_git and worktree_manager._default_runner acquire `project_file_index.SUBPROCESS_LOCK` around subprocess.run (lazy import to keep the leaf contract), or route them through git_chan |
| 37 | 0.50 | `git_status.py:186` | Background `git status` polls take index.lock inside live panes' worktrees — pane `git commit`/`add` fails with 'index.lock: File exists' | Git tab open in the Task dock: every 30 s _collect_worktrees runs `git status --porcelain` with cwd = each wt/* worktree (including ones a pane is actively working in); git_changes_service runs `git status --porcelain=v2 -z` on the shared root 1.5 s after every file change (a pane writing files trig | Set `env={**os.environ, "GIT_OPTIONAL_LOCKS": "0"}` (or prepend `--no-optional-locks`) in git_status._run_git, git_changes_service._run_git and worktree_manager._default_runner for read-only probes (s |
| 38 | 0.80 | `headless.py:85` | Headless boot never runs the storage-migration ladder (run_boot_stage) before spawning panes | `python -m agent_takkub.headless` (Docker/headless install) upgrades from 2.0.x or across releases: HeadlessWindow.boot() binds the CLI, starts remote control and spawns every Lead immediately. auto_migrate_boot's contract ('Callers MUST finish this stage before constructing MainWindow — a pane spaw | In headless.main(), before `HeadlessWindow()`, call `auto_migrate_boot.run_boot_stage(progress_cb=_log.info)` (same shape as app._run_auto_migrate_headless) and log the result; abort boot (return 1) o |
| 39 | 0.75 | `lead_context.py:913` | Non-claude Lead's AGENTS.md is written into the user's repo without the git-exclude that teammate AGENTS.md gets (and is overwritten by teammate cheatsheets in a shared cwd) | Switch Lead to codex/gemini-agy/opencode (Settings or #698 menu). `render_lead_agents_md` writes ~20KB of cockpit policy to `<lead_cwd>/AGENTS.md` and returns; unlike `codex_agents_md.ensure_agents_md` (line 454 `_ensure_git_excluded(spawn_path, "AGENTS.md")`) it never adds the file to `.git/info/ex | Call `codex_agents_md._ensure_git_excluded(spawn_path, "AGENTS.md")` after the write in render_lead_agents_md; for the shared-cwd case tag the marker with the role and refuse/merge instead of silently |
| 40 | 0.55 | `lead_inbox.py:2072` | No-content watchdog measures from raw elapsed[0] (includes pre-session spawn-gate wait) → premature kill/respawn, second time degrades to claude | A modal/dialog (Settings, QMenu) blocks ConPTY construction so spawn() defers (spawn_engine.py:2392-2401, retry every 50 ms with no ceiling; the 2026-07-11 dogfooding case was ~70 s). `_check` keeps polling with session None and `elapsed[0] += 150` (line 1718). When the pane finally spawns, `elapsed | Use `elapsed[0] - (elapsed_at_session_alive[0] or 0) >= no_content_ceiling_ms`. |
| 41 | 0.60 | `lead_inbox.py:2612` | _warn_lead_* helpers drop task-loss notices when the Lead pane is momentarily absent instead of letting _notify_lead queue them durably | User restarts Lead (or switches Lead provider) while teammates are being assigned. During the few seconds Lead is down a spawn fails / a delivery hits the single-flight block (#339) / the 30 s prompt-block ceiling gives up without pasting (#484) / auth-failure or account-pending recovery fires. Ever | Remove the Lead-alive guard from the 'task was NOT delivered' helpers (blocked, blocked_ceiling, spawn_failed, boot-timeout, auth/account) and rely on `_notify_lead`'s durable fallback; keep it only w |
| 42 | 0.60 | `lead_inbox.py:3802` | Durable notice stores (_save_pending_done_notices / _save_pending_cc) are written non-atomically → a crash mid-write silently loses every queued notice | The store exists precisely for crash/restart durability (#13). `write_text` truncates then writes; a cockpit crash/kill during the write leaves a partial JSON, `_load_pending_done_notices` (3815-3828) swallows the parse error and drops the whole project's queue — done reports spilled while Lead was  | Write to `path.with_suffix('.tmp')` then `os.replace`, mirroring NoticeDeduper._persist; log the exception instead of `pass`. |
| 43 | 0.85 | `lead_inbox.py:4904` | _force_deliver_done_notices never calls _mark_done_notices_delivered → `takkub inbox` later reports a delivered report as 'missing (#678)' | Done notices spill to the durable queue (Lead on a prompt >30 s, draft-hold expired, Lead died mid-queue, or writer rejected) and the reaper force-delivers them after 60 s not-ready (`done_notice_force_flush`). The text lands in Lead's pane but `_done_unread[(ns, role)]` (set in done(), orchestrator | After the successful write at 4899-4905 call `self._mark_done_notices_delivered(project_ns, body)` (body already contains every rendered item). |
| 44 | 0.70 | `lead_wait.py:730` | poll_wait uses the registration's started_ts, not the attaching client's → attached wait can report expired/‘never spawned’ on its first poll | Lead is blocked in `takkub wait --timeout 1800` and the Lead pane is restarted (provider switch #698 / Restart Lead) — the CLI process dies without `end_wait`, its client entry lingers until `timeout+120s` after its last poll (line 229-233). 20 min later new Lead runs `takkub wait --role qa --timeou | `client_started_ts = client_info.get('started_ts', active.get('started_ts', now))` and pass it to `_resolve_role_wait_status`/`_pending_user_input_interrupt`; for the same-role attach path (254-273) a |
| 45 | 0.60 | `limit_autoresume.py:129` | Deny verdict built from up-to-5-min-old cached telemetry can override a live quota banner | LimitStore polled 4 min ago at 93%; six panes burn 8% and one shows the banner. confirm_verdict_for_provider -> fetch_usage_shared(max_age_s=300) returns the cached 93% -> _usage_denies_limit -> 'denied' -> episode cleared as false positive, Lead told 'ไม่ได้ชนโควตา', no reroute. Pane stays frozen u | A 'denied' verdict must come from a fetch newer than the detection timestamp (pass max_age_s=0 / force refresh unless backed off) and/or require pct < threshold - margin; cached data may only produce  |
| 46 | 0.70 | `limit_autoresume.py:572` | Quota reroute snapshot drops ps.worktree and last_assigned_task_file: rerouted --worktree pane never gets its branch merge-proposed | Lead assigns backend with --worktree; backend hits quota on codex; _reroute_pane_to_provider calls close(keep_queue=True, suppress_pipeline=True, suppress_auto_chain=True) -> recovery_close, so close() pops _pane_state (orchestrator 6307) and skips _close_worktree_git (6332). _do_reroute_respawn reb | Snapshot and restore ps.worktree, last_assigned_task_file, last_assigned_scope (and task_delivered=False) alongside the existing fields; add a regression test with a worktree-assigned pane rerouted th |
| 47 | 0.75 | `limit_autoresume.py:925` | Parked pane is force-respawned by the stuck watchdog at reset time, 3 min before the wake timer fires | No reroute candidate -> _park_pane_for_limit; wake armed at reset_at + WAKE_BUFFER_S (180 s). At reset_at, orchestrator._rate_limit_suppressed (15031-15037) zeroes rate_limited_until; _emit_rate_limit_reset returns early for limit_parked (15155) WITHOUT the #53 last_content_change_ts stamp; _check_s | Either set ps.rate_limited_until = reset_at + WAKE_BUFFER_S when parking, or make _check_stuck_panes skip ps.limit_parked, or stamp last_content_change_ts before the early return at orchestrator.py:15 |
| 48 | 0.50 | `limit_status.py:206` | Claude's live .credentials.json is rewritten non-atomically, unlocked, by four independent threads/processes | Token nears expiry; within the same seconds LimitStore._do_fetch, ProviderUsageStore (fetch_claude_usage -> fetch_usage_shared), the auto-resume confirm thread and the quota reprobe thread (plus a second cockpit instance on the same profile) each call fetch_usage -> _refresh_and_persist. Each POSTs  | Write via tmp + os.replace, serialize refresh behind a module-level lock (and the shared-state file for cross-process), and skip refreshing when the token is not yet expired for this fetch (or defer t |
| 49 | 0.85 | `mcp_bridge.py:498` | describe_mcp_handshake('codex') counts `enabled=false` deny tokens as granted servers → doctor --pane reports denied MCPs as OK | codex-cli 0.155.1 (installed here) is outside _CODEX_RESOLVE_SAFE_MAX_VERSION, so _codex_mcp_argv emits `-c mcp_servers.<name>.enabled=false` for every inherited server not in the role's allowlist (:383-386). describe_mcp_handshake then regex-matches every `mcp_servers.<name>.` token, so server_name | Skip tokens ending in `.enabled=false` (or collect only `.command=` tokens) and derive has_mcp_config from the granted set. |
| 50 | 0.60 | `opencode_helper.py:411` | opencode resume picker / newest-session resolve scan only the 50 most recent sessions across ALL projects, then filter by cwd — busy projects vanish from the picker | opencode.db is one machine-wide db (here 258 MB). list_recent_opencode_sessions runs `ORDER BY time_updated DESC LIMIT 50` with no directory filter and drops rows whose directory != cwd; once the user has >50 opencode sessions spread over projects, a project whose newest session is older than the gl | Filter in SQL: `WHERE directory = ?` (try both raw and normalized forms, or `LOWER(REPLACE(directory,'\\','/')) = ?`) and only then LIMIT; same for resolve_opencode_session. |
| 51 | 0.80 | `orchestrator.py:2405` | _register_subagent / subagent_done run git worktree add and finalize inline on the Qt main thread | `takkub assign --role docs --mode subagent --isolation worktree "..."` — cli_server handles mode==subagent synchronously on the QTcpServer slot (cli_server.py:940-961), assign() → `_register_subagent` → `WorktreeManager().create(...)` + `load_worktree_config` run inline (the #408 off-thread staging  | Reuse the #408 staging for subagent mode: run `worktree_assign_inputs` + `WorktreeManager.create` via `_run_off_thread` and pass `worktree_prepared` into `_register_subagent`; run subagent_done's `_fi |
| 52 | 0.95 | `orchestrator.py:2491` | Subagent-assign path assigns create_assignment's (warning, path) tuple to `warning` → always truthy → _notify_lead raises → ledgerChanged never emitted | Any `takkub assign --mode subagent`, and every auto-picked subagent assign (cli_server.py:848 `resolve_auto_assign_mode` chooses subagent for a <400-char task on claude with no model/provider/effort/shards/worktree, role not reviewer/critic). `create_assignment` returns `('', WindowsPath(...))`; `if | Change line 2491 to `warning, _detail_path = create_assignment(...)` (match line 4051). |
| 53 | 0.85 | `orchestrator.py:3099` | Governor re-admission closure drops subagent_fanout/scope/distinct_from/base_ref/team — --shards N silently becomes sequential | Lead runs `takkub assign --role backend --shards 3 "..."` (or any assign with --scope deep / --distinct-from) while the ResourceGovernor denies the slot (machine overload latch, browser/heavy class cap — #543 shape). First pass sets `_fo_ps.subagent_fanout = 3` (line 2925). When the governor admits  | Capture and forward every assign() kwarg in the on_admitted closure (subagent_fanout, scope=resolved_scope, distinct_from, base_ref, team) — or build the closure from a single `assign_kwargs` dict; sa |
| 54 | 0.55 | `orchestrator.py:5184` | send() before task delivery: #586 merge appends to a task file a preloaded claude pane never reads, and the direct write has no ready-wait | Lead assigns backend (claude, task preloaded via system prompt: `spawn_initial_task_state="delivered"`) and within the cold-boot window sends a follow-up `takkub send --to backend "อย่าลืม X"`. `task_delivered` is still False (only set when `_send_when_ready` pastes, lead_inbox.py:1445 — for preload | When the target's task is undelivered, do not write the body now: append it to the PENDING delivery (update `spawn_initial_task`/the DeliveryManager payload, or re-run `_send_when_ready` with the merg |
| 55 | 0.70 | `orchestrator.py:5235` | send() to Lead writes straight into Lead's PTY, bypassing the Lead-inbox pump (draft guard, #133 single-chain lock, durability) | A teammate follows IDLE_REMINDER_TEXT and runs `takkub send --to lead "blocked: ..."`. cli_server routes it to `orch.send("lead", ...)` (cli_server.py:1092), which has no Lead special-case: it calls `_safe_session_write` immediately with WritePriority.TASK and arms `_delayed_enter_verified`. The PTY | In send(), when `to_role == LEAD.name`, route the body through `self._notify_lead(project_ns, body, from_role=from_role, kind="peer-to-lead")` (which already handles draft hold, busy backoff, verify-c |
| 56 | 0.65 | `orchestrator.py:6442` | toggle_provider / set_plan_tier / set_team_preset / clear_team_preset_override / notify_roles_changed write `[system] …` + Enter straight into Lead's PTY, bypassing the draft guard and ready check | User is halfway through typing a long prompt in the Lead pane and flips a provider or team preset in Settings. These five methods call `session.write(notice)` then `_delayed_enter(..., 150)` directly (6442-6445, 6485-6486, 6529-6530, 6566-6567, 6590-6591) instead of `_notify_lead`. `_lead_can_accept | Route these through `self._notify_lead(project_ns, notice, kind="system")` (queued, draft-aware, ready-aware); if they must be immediate, gate on `_lead_can_accept_injection` and `is_at_ready_prompt() |
| 57 | 0.80 | `orchestrator.py:7896` | done() still shells out to git synchronously on the Qt thread (count_test_files_in_diff, UI-evidence gate porcelain, 2×N sibling reads) despite #408 moving 'the' git facts off-thread | A clean `takkub done` from a shared-tree pane on a large repo with 2 other panes: `_finish` (cli_server.py:1164-1169) runs done() on the Qt thread, which then runs `git diff --numstat` + `git status --porcelain` (worktree_manager.py:2761/2769 via `count_test_files_in_diff`), `shared_tree_status_porc | Fold the numstat/status/sibling reads into `done_git_inputs` → `collect_done_git_facts` so they ride the existing worker thread, and skip `count_test_files_in_diff` when `git_facts` already carries a  |
| 58 | 0.85 | `orchestrator.py:8236` | `done --blocked` / delivery-failed reports set `_done_unread` but no regex ever clears it → permanent false '[role done] … missing' row in `takkub inbox` | qa runs `takkub done --blocked "no test creds"`. done() sets `_done_unread[(proj,'qa')]` (8236) and sends a notice built by `_build_blocked_handoff` = `[qa BLOCKED] …` (7070). The pump clears markers via `done_roles_in_notice` (lead_inbox.py:365) whose regex `_DONE_NOTICE_TAG_RE` = `\[([\w][\w-]*)\s | Extend `_DONE_NOTICE_TAG_RE` (and `_FAILED_NOTICE_RE`/`_FAILED_TAG_RE`) to `(?:done\|FAILED\|BLOCKED\|delivery-failed)`, or only set `_done_unread` when the notice body matches the tag the clearer kno |
| 59 | 0.90 | `orchestrator.py:8371` | done(): `_note_path` re-declared as None after being set → deferred-close info never appended, grace-expiry kill is fully silent | devops calls `takkub done` while `docker compose build` still runs. `_close_if_same_session` defers (line 8474). The only signal to Lead (#635 replaced the `_notify_lead` with an append to the session .md) reads `_note_path`, which line 8371 reset to None after line 7919 set it, so nothing is append | Delete line 8371 (keep the 7919 assignment) — and since the note file was already rendered into the Lead notice before the deferral happens, consider re-instating a one-shot `_notify_lead` (or passing |
| 60 | 0.70 | `orchestrator.py:8408` | Deferred-close poll runs a synchronous psutil process-tree walk on the Qt main thread every 5 s for up to 15 min (plus one more in close()) → UI freezes while a pane has live children after done | Any pane whose done() leaves a child alive (leftover wrapper shell, long build, #537/#554 shapes). `_close_if_same_session` calls `_live_non_scaffolding_children(..., sync=True)` each `DONE_CLOSE_LIVE_CHILD_POLL_MS`=5 s tick (8408), which #658's own comment measures at 0.9–3 s per walk on prod; over | Run the poll's scan on `bg_pool` and re-enter the timer from the result callback (same pattern as `_refresh` at 5793), or use `sync=False` for poll ticks after the first and only force a fresh scan ri |
| 61 | 0.80 | `orchestrator.py:8920` | `_save_decision_note` drops any later report whose FIRST LINE repeats an earlier one (process-lifetime `_DEDUP_HASHES`) → session .md silently not written, Lead pointer/hot.md link dangle | qa reports `done "PASS — all green\n…20 lines…"` at 10:00 for task A; at 14:00 for task B the note starts with the same first line. `_is_dedup_note` (vault_mirror.py:227-238, key = (project, role, md5(first line)), set never cleared) returns True → returns None before writing: no runtime/sessions fi | Key the dedup on (project, role, hash of whole scrubbed note) with a short TTL (e.g. 60 s — its stated purpose is 'rapid-fire duplicate done events'), or scope it to the vault mirror only and always w |
| 62 | 0.80 | `orchestrator.py:9106` | BLOCKED / delivery-failed reports are invisible to `_has_pending_lead_notice`, `_notice_role_tag` and `_is_blocking_lead_notice` → `takkub wait` never wakes for them, `takkub inbox --role X` files them under 'system' | Lead runs `takkub wait --role qa` while devops reports `done --blocked`. `_pending_notice_outside` (9163-9174) requires `_is_blocking_lead_notice` (lead_inbox.py:460, matches only `\bFAILED\b` inside brackets or the 4 system markers) → BLOCKED is not a wake trigger, Lead sits blind for the full --ti | Treat `[role BLOCKED]` and `[role delivery-failed]` as first-class failure tags in all four regexes (one shared alternation constant), then add a test that `done(blocked=True)` wakes an unrelated `wai |
| 63 | 0.60 | `orchestrator.py:10829` | pane_status_report walks every day directory under runtime/sessions per pane on the Qt main thread — UI stall grows with history | `takkub status` (cli_server `_dispatch`, a QTcpSocket slot on the main thread) calls this. For each pane the `done_events` loop iterates ALL day dirs (119 on this machine) and every file in them, and evidence roles run a second full walk for cited screenshots (10785-10814); `_find_latest_transcript_ | Stop iterating once `day_dir.name` (YYYY-MM-DD) is older than the day containing `since_ts`/`assign_ts`; skip the sorted() copies; or cache per-day listings with an mtime check. Longer term, run the r |
| 64 | 0.70 | `orchestrator.py:10945` | harvest_info defaults since_ts to pane._spawn_ts; with 2.1.17 pane reuse the window covers the PREVIOUS task's artifacts | Lead assigns task A to `backend`, it reports done, the pane stays alive (pane reuse, DONE_PANE_TTL_S=1800). Lead assigns task B to the same role: a shared-isolation re-assign pastes into the running pane (orchestrator.py:3664-3665) — no respawn, so `pane._spawn_ts` (set only in `attach_session`, age | Default `since_ts` to `max(ps.assign_ts, spawn_ts_raw)` from `self._pane_state.get(_exit_key(project_ns, role))`, falling back to `_spawn_ts` only when no assign was recorded. |
| 65 | 0.70 | `orchestrator.py:11407` | _build_post_compact_brief reads the whole raw PTY transcript on the Qt main thread and injects unstripped ANSI/repaint fragments into Lead's context file | User picks 'Restart Lead' (or switches Lead provider) while teammates are alive and `last-session.json` is <5 min old. `spawn()` (spawn_engine.py:2625 / 3007, main thread) calls this: `pathlib.Path(transcript_path).read_bytes()` loads the entire `.transcript.log` (raw PTY byte stream — lead transcri | Use `_read_tail_bytes(path, _TRANSCRIPT_TAIL_BYTES)` + `_extract_transcript_lines(raw, max_lines=5)` (drop the partial first line like 10735-10736), or prefer `pane.session.display_lines()` when the p |
| 66 | 0.90 | `orchestrator.py:13679` | QTimer.singleShot(0, _finish) called from a plain threading.Thread never fires → shell-open-dialog detector latches dialog_scan_in_flight=True forever and never notifies Lead | Any working teammate pane that has been quiet for _SHELL_DIALOG_IDLE_GATE_S: the first scan sets `ps.dialog_scan_in_flight = True` (13648) and spawns `_scan_worker`, whose only way back is `QTimer.singleShot(0, _finish)` executed on the worker thread. Verified on this venv (PyQt 6.8.1 / Qt 6.8.2): a | Marshal back to the Qt thread with a signal (declare e.g. `shellDialogScanned = pyqtSignal(str, str, bool)` on Orchestrator, emit from the worker, connect to a slot that runs `_finish`), or `QMetaObje |
| 67 | 0.75 | `orchestrator.py:15398` | close-all / tab close resurrect any pane whose task had not reached it yet (close() undelivered-task auto-forward) | Lead assigns codex/gemini pane (90 s boot) or a claude pane parked on the trust modal, then runs `takkub close-all` (or the user closes the project tab). `close_all_teammates` calls plain `close(n, project=...)`; in close(), `_task_undelivered_close` is True (task accepted, `task_delivered` still Fa | Give close() an explicit `forward_undelivered: bool` (default True only for the single CLI `close` if that #484 behaviour is really wanted) and have close_all_teammates/_close_project_tab pass False ( |
| 68 | 0.50 | `orchestrator_text.py:393` | UI evidence gate rejects real screenshots whose path contains spaces or the $TAKKUB_ARTIFACTS_DIR form it itself recommends | A frontend pane writes `$TAKKUB_ARTIFACTS_DIR/screenshots/login-390.png` (the exact form the rejection text at :365/:381 tells it to use) or `C:\Users\Mon Chai\...\shot.png` in its done note. _SCREENSHOT_PATH_RE stops at whitespace and `exists` never expands env vars, so every path is 'missing' and  | Apply os.path.expandvars + expanduser before the isfile check; add a quoted-path alternative to the regex ("..."/'...') so paths with spaces are captured whole. |
| 69 | 0.85 | `pane_env.py:605` | `claude --version` subprocess runs synchronously on the Qt main thread on EVERY claude spawn, never cached | Every claude spawn (Lead, each teammate, each auto-respawn, each fan-out pane) calls inject_claude_project_dir_name_env which blocks the GUI thread on subprocess.run([claude,'--version'], timeout=3). On Windows the npm shim is claude.cmd -> cmd.exe -> node, typically 0.5–1.5 s; with Defender scannin | Cache the parsed version per (executable path, st_mtime) at module level (same shape as mcp_bridge's codex cache), or probe once at boot (boot_flow already runs `--version`) and read the cached value  |
| 70 | 0.90 | `pane_guard.py:388` | browser_driver:inline-import fires on text arguments — `grep "from playwright"` is denied for non-browser roles | A backend/devops pane runs `grep -rn "from playwright" src/`, `rg "import selenium"`, `git log --grep "import puppeteer"` or echoes such text into a note: the hook denies with browser_driver:inline-import although the module docstring (line 32-35) promises reading about these tools is always allowed | Anchor the rule to an executing sink (`node -e`, `python -c`, heredoc whose introducer matches `_PYTHON_HEREDOC_SINK`/node), or skip matches that sit inside a quoted argument of grep/rg/echo/git log. |
| 71 | 0.85 | `pane_guard.py:951` | machine-state.json (the primary busy source) carries bare role names, so the #646 project tag never shows and a same-named pane in another project is excluded from the busy check | Lead in project `easy-ui` gets 'มี pane อื่นกำลังทำงาน (backend)' for a pane that belongs to another project (the #646 lie, still), because the project suffix is only synthesised from last-session.json's `projects` map (used ≤17% of the time). Conversely `backend` in project A running `npm run build | Write `f"{role}@{project}"` in `_maybe_write_machine_state` and, in the reader, exclude only the caller's own (role, project) pair instead of every same-named role. |
| 72 | 0.85 | `pane_guard.py:1320` | Lead direct-edit sensitive-content regexes deny any Python diff containing `return True` or the words token/session/login — the tiny-fix feature is effectively off for ordinary Python | Lead makes a 1-line fix `return False` → `return True` in `src/foo/util.py` (scope tiny, 1 file): denied lead_direct_edit:deep_category. Likewise any diff mentioning `max_tokens`, `session_timeout`, `login_url` or a comment with the word 'token' — in this codebase nearly every module. | Restrict the `return true` / bare-word content patterns to the sensitive-path leg (or require both a sensitive identifier AND a bypass shape in the same hunk); at minimum do not lowercase before match |
| 73 | 0.85 | `pane_guard.py:1957` | _worktree_role_owns matches `<role>-` on ANY path segment, so a sibling role's worktree under a project named `<role>-*` is treated as owned | Project `backend-app` (or `mobile-app`, `qa-tools`): worktrees live at `.../worktrees/backend-app/frontend-<ts>/`. Role `backend` runs `git -C <frontend's worktree> reset --hard` / `checkout .` / `clean -fdx` (or has cwd there after a `cd`): `_in_worktree` returns True → #545 carve-out → frontend's  | Locate the `worktrees` segment, skip one segment (project), and test only that next segment against `base`/`prefix`. |
| 74 | 0.90 | `pane_guard.py:3653` | git default-deny allow-list omits common read-only plumbing (ls-remote, for-each-ref, show-ref, symbolic-ref, format-patch, diff-tree, range-diff, cherry, archive) | Any guarded role on the shared tree runs `git symbolic-ref refs/remotes/origin/HEAD`, `git ls-remote --heads origin`, `git for-each-ref refs/heads`, `git show-ref`, `git format-patch -1 HEAD` to inspect state → denied git_shared_default_deny:<sub> and the pane routes around it or stalls. | Add the read-only verbs (`ls-remote`, `for-each-ref`, `show-ref`, `symbolic-ref` (no 2nd positional), `diff-tree`, `diff-index`, `format-patch`, `range-diff`, `cherry`, `archive`, `fsck`, `verify-comm |
| 75 | 0.80 | `pane_guard.py:3980` | Heredoc bodies are only stripped for the poll-loop and instance rules — git/browser/full-suite rules read a quoted issue body or commit message as executed shell | A qa pane files a bug: `gh issue create --body "$(cat <<'EOF'\nrepro:\ngit push origin main\nEOF\n)"` → denied git_lead_only:push; a worktree pane's `git commit -F - <<'EOF'` whose body has a line starting `pytest` → full_suite. Same class #287 documented for the poll loop (line 610-615) but never a | After unwrapping, set `code = _strip_heredoc_bodies(cmd)` (keeping shell-sink heredocs, as the helper already does) and match all `_CMD_START`-anchored rules against `code`. |
| 76 | 0.85 | `pane_provider_label.py:100` | Every gemini pane with a pinned model shows a permanent false '⚠ model mismatch' (live footer label is free text, spawn model is an id) | Role pinned to gemini-3.7-flash-high via Settings picker (spawn_engine.py:2838 sets pane.model.spawn_model to that id). agent_pane.py:1007 passes live_model = session.current_model_label('gemini') which pty_session._GEMINI_MODEL_RE (pty_session.py:1394) returns as e.g. 'Gemini 3.7 Flash (High)' or ' | Normalize the gemini footer label to id form before comparing (lowercase, strip '(' ')' '·' ':', collapse whitespace to '-': 'Gemini 3.7 Flash (High)' -> 'gemini-3.7-flash-high') in shorten_model_name |
| 77 | 0.75 | `pane_tools_dialog.py:33` | MCP/Plugins matrix hides the codex row although codex panes DO enforce this policy, while showing opencode/kimi/cursor rows whose toggles do nothing | _MATRIX_EXCLUDED_ROLES drops 'codex' with the comment 'codex/gemini = non-claude binaries never load --mcp-config'. But mcp_bridge.py:372 `role_mcp_allowlist(base_role)` drives codex's session_override (`-c mcp_servers.*`) from this same policy, shared_dev_tools._ROLE_MCP_POLICY['codex'] = frozenset | Derive exclusions from PROVIDER_REGISTRY: exclude a role iff its forced provider's spec.mcp_adapter_variant == 'none' or the role is 'shell' (so codex is shown, opencode/kimi/cursor are hidden or rend |
| 78 | 0.85 | `pipeline_executor.py:471` | Pipeline completion notice only reflects the LAST hop's failures — earlier-hop crashes reported as 'all hops complete ✓' | Feature template (frontend+backend → devops → qa): frontend pane crashes/is closed without done in hop 1 → close() adds it to `hop_failed` (orchestrator.py:6350) and the hop advances; `_fire_pipeline_hop` for hop 2 resets `run.hop_failed = set()`; hops 2-3 finish cleanly → `_advance_pipeline` render | Keep a run-level accumulator (e.g. `run.failed_by_hop: dict[int, set]`) appended in close()/spawn-fail paths and render it in `_advance_pipeline`; leave `hop_failed` per-hop for the advance logic. |
| 79 | 0.50 | `plugin_installer.py:166` | Plugins button checks/installs/uninstalls against default_claude_config_dir() while panes load plugins from the project's profile dir | A project with a named user profile (or a cockpit launched from a shell where CLAUDE_CONFIG_DIR is already set, which `_claude_env`'s setdefault honours but `installed_on_disk` ignores). `installed_on_disk()` and `_purge_plugin_cache()` look only in `default_claude_config_dir()/plugins/cache`; `lead | Thread the same `config_dir_for(project)` through installed_on_disk/_purge_plugin_cache/_claude_env (pass project from user_actions), and never uninstall on a disk-check miss — report the mismatch ins |
| 80 | 0.70 | `preview_widget.py:330` | Preview reloads its page on every tab switch back to the project (SPA state/scroll lost) | Preview shows http://localhost:3000; user navigates inside the app to /dashboard, switches to another project tab and back. MainWindow._sync_preview_to_active_tab (main_window.py:1658 -> :603-605) calls show_state with the same state; show_state unconditionally calls load_url(state.target), re-navig | Early-return the load when `self._view is not None and self._project == project and self._state == state` (PreviewState is a frozen dataclass, equality works); still refresh device/header. |
| 81 | 0.80 | `project_explorer.py:233` | Explorer tree never re-lists a directory — files a pane creates/deletes never appear until the tab is recreated | Expand `src/` in the Project Explorer, then a teammate pane creates `src/new_module.py` (or deletes one). `_LOADED_ROLE` was set True on the first expand and is never reset anywhere; collapse+expand returns at line 233; `refresh_changes()` (the only refresh entry, also what MainWindow calls on edito | Reset `_LOADED_ROLE` to False on collapse (itemCollapsed) or add a re-list of expanded items in `refresh_changes()` (re-request listing for every expanded directory item, diffing children). |
| 82 | 0.80 | `project_explorer.py:422` | Git M/A/D badges never match when the first configured root is a subdirectory of the repo (porcelain paths are repo-root-relative) | Project with paths {api: repo/api, web: repo/web} (the sibling layout graft_autobuild's docstring calls frequent). GitStatusService runs `git status --porcelain=v1` with cwd=repo/api; porcelain v1 output paths are ALWAYS relative to the repository top-level (`api/src/x.py`), but `_apply_badge` compu | In `_on_repos_discovered`, re-create `GitStatusService(primary_toplevel)` (or compute `rel` against the discovered toplevel) so badge keys match porcelain paths. |
| 83 | 0.85 | `project_wizard.py:84` | Import folder silently overwrites an existing project with the same slug (different folder) | User already has project 'web' at C:/a/web (tab open, Lead running). They Import D:/other/web. The slug collides, projects.json paths for 'web' are replaced with the new folder, and because the tab is open the wizard just switches to it ('Updated project: web'). Lead keeps working in the old cwd whi | Reuse the duplicate-name check from `_new_project_with_rules` in `_import_existing_project` (offer rename / cancel), and when the project tab is open restart its Lead or refuse the path change. |
| 84 | 0.90 | `provider_model_refresh.py:411` | Boot-time pinned-model bump (refresh_provider_model) is never called from any production path — the module's whole purpose is unwired | Module docstring: 'Runs in the SAME boot phase as the binary update (provider_update.py)'; settings_window.py:449 comment relies on it ('provider_model_refresh bumps a pinned model'). Grep of every *.py under src/ finds no caller of refresh_provider_model/_refresh_pins (only tests/test_provider_mode | Either wire refresh_provider_model(name, binary) into boot_flow's per-provider update loop (after update_provider, for eligible providers) and surface STATUS_BUMPED in the boot window, or delete _refr |
| 85 | 0.70 | `provider_spec.py:534` | CLAUDE_DEFAULT_BUILTIN_TOOLS is stale — doctor WARNs 'new built-in tools detected' on every run today (ToolSearch, Monitor, TaskOutput, TaskStop, ListAgents, ScheduleWakeup…) | Running `_scan_claude_transcript_tools()` on this machine returns tools absent from the list: ListAgents, Monitor, ScheduleWakeup, TaskOutput, TaskStop, ToolSearch (+ harness-only Artifact/StructuredOutput/Workflow). `takkub doctor` therefore shows `⚠ tools_drift new built-in tools detected: …` ever | Add the confirmed Claude Code built-ins (ToolSearch, Monitor, TaskOutput, TaskStop, ListAgents, ScheduleWakeup) to CLAUDE_DEFAULT_BUILTIN_TOOLS; have the drift check ignore harness-only names (Workflo |
| 86 | 0.50 | `pty_session.py:1769` | _ReaderThread._flush runs concurrently on reader thread and threading.Timer threads -> batches fed to pyte/transcript/xterm out of order | Timer T1 fires `_flush`, pops batch A under `_lock`, releases, and is inside `_on_data(A)` (pyte parse of a large chunk under GIL contention with N panes takes >10 ms). Reader thread reads B+C, hits `len(_pending) >= _batch_bytes`, calls `_flush` itself, pops B+C and runs `_on_data(B+C)` concurrentl | Serialize delivery: have the timer only wake the reader (e.g. set an Event / put a sentinel) and let the reader thread be the sole caller of `_on_data`+emit; or wrap `_on_data`+emit in a dedicated ord |
| 87 | 0.55 | `pty_session.py:2002` | Spawn-time console sweep SW_HIDEs the user's own Windows Terminal opened within 3.5 s of any pane spawn | Windows 11, default terminal = Windows Terminal. Cockpit boots or auto-respawns a pane (spawns happen many times per session). User launches a new Windows Terminal window (class CASCADIA_HOSTING_WINDOW_CLASS) within the 150-3500 ms sweep window. `_sweep` computes `snapshot - pre_hwnds` and calls `hi | In `_sweep`, skip HWNDs whose class is in `_UNOWNABLE_WINDOW_CLASSES` unless `_is_descendant_of(pid, root_pid)` proves ownership; keep unconditional hiding only for ConsoleWindowClass/PseudoConsoleWin |
| 88 | 0.60 | `pty_session.py:2362` | macOS: descendants of a CLI that exits on its own are never reaped (Windows job object has no POSIX equivalent here) | macOS pane (any provider) starts `npm run dev` / `next dev` in the background, then the CLI exits naturally (crash, OOM, user `/exit`, quota-triggered exit). `_on_exit` -> `_teardown_resources(kill_process=False)`: `_tree_kill` is only in the `if kill_process:` branch, and the `elif _job_object is n | Capture `pgid = os.getpgid(pid)` right after spawn on POSIX and, in the natural-exit branch, `os.killpg(pgid, SIGKILL)` (best-effort) to mirror the job-object semantics. |
| 89 | 0.65 | `qa_gate.py:571` | Python gate hardcodes cockpit-only plugin flags/paths and only recognises `.venv` — any other Python project fails on tooling, not code | qa pane runs `takkub qa-gate`/`--auto` inside a non-cockpit Python project (the #401 unittest fallback and `--exec` exist precisely for this). (1) `_pytest_cmd` always adds `--timeout=…`, `--max-worker-restart=0` (and `-n 4 --dist loadscope` on full) → pytest without pytest-timeout/pytest-xdist: 'un | Probe plugins (`pytest --version` / importlib.metadata in the target venv) before adding flags; derive ruff paths from pyproject/`ruff check .`; skip lint-imports when no importlinter config; also acc |
| 90 | 0.70 | `qa_gate.py:1472` | Python auto-targeted tier passes deleted/renamed test paths and non-test helper modules to pytest → exit 4/5 FAIL | (a) Rename `tests/test_old.py`→`tests/test_new.py` uncommitted: `git diff --name-only HEAD` (l.1369) lists the deleted old path, `_TEST_FILE_RE` matches it, pytest gets a nonexistent path → 'file or directory not found' exit 4 → FAIL. (b) Edit only `tests/_subprocess_call_ast.py` / `tests/_qt_timer_ | In `_map_python_tests` keep only `(root / f).is_file()` and basename matching `test_*.py`; treat deleted paths as 'no test' (widen to full or drop). |
| 91 | 0.85 | `qa_gate.py:2042` | `qa-gate --auto` never writes the report/step logs even on the full tier, contradicting qa.md and disabling the #607 retry evidence | The qa role is told to run `takkub qa-gate --auto` once per batch and that the report is 'auto-saved to <DATA_HOME>/runtime/qa-reports/' (.claude/agents/qa.md:35, docs/qa-gate-policy.md:44). With `auto=True`, `tier` is always a DiffTier so `write_report` is False → `log_dir=None` (l.2078-2080) and ` | `write_report = targeted is None and (tier is None or tier.tier == "full")`, and set `log_dir` for auto-full as well. |
| 92 | 0.85 | `release.py:427` | release failure message drops git/hook stderr — pre-commit hook failures surface as a bare 'exit status 1' | `git commit` at l.387 runs the repo's pre-commit chain (ruff, docs-verify, import-linter, depgraph). When a hook rejects the commit, CalledProcessError is re-raised as `RuntimeError(f"release failed; working tree reverted: {exc}")`; `str(CalledProcessError)` is only "Command [...] returned non-zero  | Append `getattr(exc, 'stderr', '') or getattr(exc, 'stdout', '')` (tail ~40 lines) to the RuntimeError message. |
| 93 | 0.85 | `remote/api.py:628` | Image upload message is prefixed '[remote → lead] ' twice, so the phone mirrors a second 'me' bubble containing the raw workstation path | Phone attaches an image (control mode). lead_upload_image builds `message = f'[remote → lead] แนบรูปจาก mobile ...: "{image_path}"'` and calls lead_say -> cli_server send -> Orchestrator.send(from_role='remote'), which prepends its own header (orchestrator.py:5205-5206), producing '[remote → lead] [ | Drop the literal '[remote → lead] ' from api.py:628 (send() adds it), and make the PWA suppress/relabel the mirrored upload line (e.g. server tags it `kind:'sys'` or the phone matches on the 'แนบรูปจา |
| 94 | 0.90 | `remote/http_server.py:390` | SSE 'report' event has no listener in the shipped PWA: `takkub report publish --send` says 'pushed' but the phone never shows the attachment card | Lead runs `takkub report publish --send` (#390). cli._push_report_to_mobile -> report-send -> Orchestrator.push_report returns (True, 'pushed <name> to project ...') -> reportShared -> LeadNotifier._on_report_shared -> SSEBroadcaster.push('report', payload). app.js registers listeners only for worki | Add `es.addEventListener("report", ...)` in app.js rendering {name,url,label,size_bytes,attachment} as a card (and bump sw.js CACHE_NAME), or remove the push path and make cmd_report print the plain l |
| 95 | 0.55 | `remote/notify.py:857` | Lead history read parses up to 8 MB of JSONL on the Qt main thread for every phone history request | Long-running Claude Lead session (tens of MB JSONL). Every /api/lead/history (project switch, reconnect, resume, and app.js:3182 `refreshOpenProjectHistories()` for EVERY open project each time the phone returns from >30 s in background) is an inline bridge action; _read_recent_claude_messages reads | Move lead_history (and lead_sessions) to _OFF_THREAD_ACTIONS after resolving the transcript path + provider on the Qt thread, or cache parsed history per (path, size) so repeated refreshes only parse  |
| 96 | 0.80 | `remote/notify.py:1518` | Codex resume picker scans the entire ~/.codex/sessions tree (rglob + first-line read of every rollout) on the Qt main thread | Lead provider = codex, user taps Resume on the phone. /api/lead/sessions is an inline bridge action (http_server.py:268-272) that runs on the Qt main thread; _list_recent_codex_sessions does `root.rglob("rollout-*.jsonl")` and `_codex_session_meta(path)` (open + readline) for every file in the store | Reuse _codex_rollout_candidates(root) (newest day first, lazy) with an early stop once `capped` matches are found, or move lead_sessions into _OFF_THREAD_ACTIONS (it only reads files + config, no Qt/o |
| 97 | 0.90 | `remote/tunnel.py:179` | Windows tunnel launch via `cmd /d /c` + list2cmdline cannot start any executable/script whose path contains a space | cloudflared resolved from PATH at C:\Program Files (x86)\cloudflared\cloudflared.exe (winget install), or a Windows user name with a space (DATA_HOME\bin\cloudflared.exe from cloudflared_install), or a .bat script path with spaces. _spawn builds `list2cmdline(argv)` -> '"C:\Program Files\...\cloudfl | Spawn the binary directly (`subprocess.Popen(argv, ...)`) for cloudflared/ngrok and only wrap .bat/.cmd scripts with `cmd /d /c`, passing the script as separate argv elements (['cmd','/d','/c', script |
| 98 | 0.55 | `report_builder.py:163` | images.txt relative paths are resolved against the process cwd, not the content dir | images.txt says `login\|screenshots/login.png` (relative to report-content/, the natural reading of docs/lead/report-publish.md:38 `name\|path`). Lead runs `takkub report build --content report-content` from the project root -> os.path.exists('screenshots/login.png') is False -> 'WARNING: image not  | Resolve `p = Path(path) if os.path.isabs(path) else self.content_dir / path` (fall back to cwd-relative for compatibility) before exists()/read. |
| 99 | 0.85 | `report_builder.py:311` | Content-dir template.html override is dead: shipped template always wins (contradicts docs + docstring) | User drops a custom template.html into the report content dir (documented in docs/lead/report-publish.md:33 as 'ชนะ template ที่ ship มา'). `takkub report build` silently ignores it and uses assets/report/template.html on every dev/installed build where the kit exists. | Check `self.content_dir / "template.html"` first, then the shipped asset, then the fallback. |
| 100 | 0.75 | `service_spawner.py:199` | spawn-service cannot start .cmd/.bat shims on Windows (npm/pnpm/npx) — works on macOS only | `takkub spawn-service --name web -- npm run dev` (or pnpm/npx/yarn) on Windows: Popen without shutil.which resolution -> CreateProcess only appends .exe, so npm.cmd is not found -> both creationflags attempts raise FileNotFoundError -> ServiceSpawnError "could not start 'npm': [WinError 2]". Same co | `exe = shutil.which(cmd[0], path=child_env.get("PATH"))`; if found, replace cmd[0] with it (which honours PATHEXT so npm.cmd resolves); otherwise raise ServiceSpawnError with a clear 'not on PATH' mes |
| 101 | 0.70 | `settings_accounts.py:637` | 'แชร์ session' runs convert_profile_to_shared on the Qt main thread — copies/renames the whole projects/todos/plugins/skills tree, freezing the UI | Accounts → claude named account → 'แชร์ session' → OK. `user_profile.convert_profile_to_shared` walks `projects/` etc. with rglob and copies every file into ~/.claude (`_merge_tree`, user_profile.py:240-252), then renames and links — for a used Claude profile that is hundreds of MB of session JSONL, | Run the conversion in a worker (QThreadPool/QRunnable like `_AccountsRefreshWorker`), disable the card's buttons meanwhile, show the results dict on the finished signal. |
| 102 | 0.85 | `settings_management/repositories/plugins.py:72` | Opening the new Settings window runs `claude plugin list --json` synchronously on the Qt main thread (30s timeout), and again on every plugin get/create/delete_plan | TAKKUB_SETTINGS_UI=new: SettingsManagementWindow.__init__ calls plugins_page.refresh() (window.py:109) → plugins_repo.list() → _installed_entries() → plugin_installer.list_installed() → subprocess `claude plugin list --json` with timeout=30 on the GUI thread. The whole cockpit (all panes) freezes fo | Run list_installed in a QRunnable/worker and paint from a cache (same shape as status_header's _PlanProbeWorker); cache the result per window and reuse it across get/delete_plan instead of re-spawning |
| 103 | 0.80 | `settings_management/repositories/skills.py:36` | Skills 'Project' root and Role Access skill list use Path.cwd() (cockpit process cwd), not the active project | TAKKUB_SETTINGS_UI=new: an npm-installed cockpit launched from e.g. C:\Users\monch scans C:\Users\monch\.claude\skills as 'Project' skills (empty), and '+ New Skill' → create() → skill_scan.create_skill(roots[0], …) with project_ns=None writes <cwd>/.claude/skills/<name>/SKILL.md into the user's hom | Resolve the root from config.active_project() (lead_cwd) and pass project_ns to create_skill as the legacy window does; have roles_page use skills_repo.assignable_names(). |
| 104 | 0.60 | `settings_usage.py:414` | Usage page runs two full ledger rollups per account plus quota-file scans on the Qt main thread on every open/range switch | User opens Settings > Usage (lazy builder -> _build_usage_view -> _render_usage) or flips the range combo. query_usage(refresh=True) calls rollup_daily per account (takes _UsageLock: 50 ms poll loop, up to 10 s if the Refresh import thread is mid-write; reads the current month jsonl + _seen_ids.json | Call daily_series(refresh=False) after query_usage, and move the refresh=True query into a _CallableThread (same as Refresh) rendering from the result; render immediately with refresh=False. |
| 105 | 0.85 | `settings_window.py:981` | pending_team_preset is never consumed by the caller — orchestrator.set_team_preset (Lead broadcast + team_preset_set event) never fires from Settings | Roles page → pick a team-size card → Save & Apply. The preset file is written (1637 `set_current`) but the comment at 974-980 promises the caller routes it through `orchestrator.set_team_preset` for the live-Lead broadcast; user_actions.py:402-408 reads only `pending_provider_disabled` and `pending_ | In `_open_legacy_settings_window`, after Accepted: `if dlg.pending_team_preset: self.orch.set_team_preset(dlg.pending_team_preset, _proj)` (the file write is idempotent) — or emit the broadcast from t |
| 106 | 0.85 | `settings_window.py:2006` | โหมดเครื่อง / discard-renderer change is saved but never applied live: pending_performance_reload is only read when the dialog is Accepted, and General has Save & Apply disabled | ทั่วไป page → change โหมดเครื่อง (or the discard toggle) → file is written and `pending_performance_reload=True` → footer Save & Apply is disabled on this page (284 `_NO_FOOTER_SAVE_VIEWS`, 1488) so the user closes with Cancel/X → `reject` → user_actions.py:387-388 returns before 407-408 → `orch.rel | Apply the reload immediately in `_set_machine_mode`/`_on_pane_discard_toggled` via a callback/signal, or have the caller read `pending_performance_reload` regardless of the exec() result. |
| 107 | 0.85 | `settings_window.py:2323` | Clicking a team-size card rebuilds the roster from disk and silently discards unsaved provider/model/effort edits on every role row | Roles page → set backend's provider to codex and a model → then click 'ทีมเต็ม' card → `_rebuild_roster_panel()` deletes all rows and rebuilds them from `provider_config.role_provider_map` (2450) and `role_models.model_for` (2817) → backend is back to the on-disk value; the footer still shows 'มีการ | Before rebuilding, snapshot each row's current provider/model/effort combo values and re-apply them to the rebuilt rows (or only update the enable toggles in place instead of a full rebuild). |
| 108 | 0.70 | `settings_window.py:3706` | Claude API-override tab lists every provider's profiles (codex included) and resolves by first name match — override can be written into a codex home | Accounts → 'การเชื่อมต่อ API (ขั้นสูง)' → profile combo shows codex accounts too (`user_profile.list_profiles()` returns all providers, user_profile.py:132-138, no filter). Saving for a codex account writes `takkub-claude-auth.json` into the codex home where nothing reads it (silently 'saved'). If a | Populate the combo from `user_profile.profiles_for_provider("claude")` and key the lookup by (provider, name) / store config_dir as item data. |
| 109 | 0.85 | `settings_window.py:5114` | Selecting another template (or Duplicate/Delete) drops the staged hop edits of the template being edited while the dirty flag stays set | Pipeline page → edit hops of template A (add/remove roles; staged in `_pb_hops`) → click template B in the list (or press Duplicate/Delete, which reloads the list and reselects row 0 at 5092) → `_load_pb_hops(B)` replaces `_pb_hops` → A's edits are gone; Save & Apply then writes B's (unchanged) hops | Keep a per-template staged-hops dict (`{template_id: hops}`) and flush every staged entry on Save; after Duplicate/Delete reselect the previously selected id instead of row 0. |
| 110 | 0.85 | `skill_scan.py:445` | create_skill writes description into YAML frontmatter unescaped — ': ' / '#' / leading '[{*&!%@' / newline breaks the skill | Settings → New Skill with description 'Use when: reviewing PRs' or 'ใช้เมื่อ: …' (colon-space), or a description containing ' #', or starting with '[', '*', '&', '%', '@', '`', or a quote. The written SKILL.md has invalid YAML: `_parse_frontmatter` returns {} so the Skill Matrix/catalog shows no des | Build the frontmatter with `yaml.safe_dump({'name': name, 'description': description}, allow_unicode=True, sort_keys=False)` exactly like update_skill. |
| 111 | 0.80 | `spawn_engine.py:2445` | Native Chrome launch busy-waits up to 8 s with time.sleep on the Qt main thread for every qa/critic/designer spawn on Windows | First assign to qa/critic/designer on Windows (CDP 9222 not up): spawn() calls NativeChromeManager.ensure_started() inline. browser_chrome.py:183-189 loops `while time.monotonic() < deadline: if self._cdp_ready()...; time.sleep(0.05)` with _START_TIMEOUT_SEC = 8.0 — the whole cockpit (all panes' ter | Popen Chrome and return immediately; poll _cdp_ready from a QTimer (or a worker thread) and log the result asynchronously — the pane does not need CDP before the ConPTY spawn. |
| 112 | 0.80 | `spawn_engine.py:2600` | Non-claude teammate shard panes resolve cwd with the shard name (qa#2) instead of base_role, landing in the wrong project path | Project paths {'api': ..., 'web': ...}; qa is routed to gemini/codex; `takkub assign --role qa --shards 2` (browser roles still fan out as real panes). The generic branch calls default_cwd_for_role('qa#1') — _ROLE_PATH_PREFS and the project's role_paths override are keyed by bare role, so lookup mis | Use default_cwd_for_role(base_role, project=project_ns) in the generic-provider branch, matching the claude branch. |
| 113 | 0.70 | `spawn_engine.py:2762` | First codex/opencode/kimi spawn on an installed build seeds the isolated home synchronously on the main thread: unbounded rglob + up to 40 x 64 MB copies | Prod install (DATA_HOME != REPO_ROOT), first ever codex pane: ensure_provider_home -> _seed_codex -> _clone_codex_sessions does `sessions.rglob('rollout-*.jsonl')` over the user's entire ~/.codex/sessions tree, stats every file, sorts, opens the head of each candidate, then copy2's up to RECENT_SESS | Run the seed on a worker thread (or at boot in boot_update/boot_flow before any pane exists, as the module docstring already suggests for updates), and/or cap total bytes (e.g. 256 MB) and per-file si |
| 114 | 0.80 | `spawn_engine.py:4321` | User typing `exit` in the Open-Shell pane is treated as a crash: Lead gets a 'process died mid-task' notice and the shell is auto-respawned twice | User clicks Open Shell, runs a few git commands, types `exit` (code 0). AgentPane._on_process_exited -> decide_exit_state returns 'exited' (expected_exit only set by close()/done()). _on_session_exit sees state=='exited', calls _warn_lead_pane_exited('shell') -> Lead is told 'pane shell exited (code | Early-return in _on_session_exit (before the notice and before scheduling _auto_respawn) when role_name == 'shell' (or when the pane's provider_name is 'shell'); also skip _warn_lead_pane_exited for s |
| 115 | 0.75 | `spawn_engine.py:4400` | A failed auto-respawn is silent: Lead was told to wait for auto-respawn, but a spawn() failure is only written to events.log | Pane crashes; _warn_lead_pane_exited tells Lead 'ถ้า auto-respawn อยู่รอรอบถัดไปได้'. 2.5 s later _auto_respawn -> spawn() returns (False, msg) — e.g. provider binary mid-update / missing (`spec.install_instructions`), role toggled off in Settings, McpResolutionError, cwd no longer valid (worktree f | On `not ok`, call self._warn_lead_spawn_failed(role_name, project, msg) and run the same capped-pane bookkeeping as the AUTO_RESPAWN_MAX branch (auto-chain release, pipeline hop_failed, close()). |
| 116 | 0.65 | `status_header.py:687` | _update_status (2s timer + every statusChanged) does ~17 uncached file reads on the Qt main thread per tick | Every 2s and on each statusChanged emit: _update_provider_chip calls user_profile.profile_for up to 12× (6 providers in the text loop at 685-689 and again in the tooltip loop at 700-703); each call does an uncached `path.read_text` of projects/<slug>/user-profile.json (user_profile.py:401-404). _ref | Read user-profile.json and team_preset.json through cached_read.read_cached (with invalidate() in their writers), and compute profile_for once per provider per tick (build the text and tooltip from on |
| 117 | 0.75 | `task_delivery.py:79` | UNCERTAIN is in _IN_FLIGHT_STATES → every [delivery-uncertain] is followed ≤120 s later by a second [delivery-stale-reap] notice for the same delivery | A gemini pane returns to its ready prompt after the paste (the #359/#404 shape). `_on_settled` marks UNCERTAIN and sends `[delivery-uncertain]` (lead_inbox.py:2678). The delivery keeps its 120 s TTL; `expire_stale()` (task_delivery.py:514) reaps it and `_reap_stale_deliveries` (lead_inbox.py:2528) s | Either exclude UNCERTAIN from `_IN_FLIGHT_STATES` (it already released its slot and was announced) or record the uncertain notice in `_stale_reap_notified` so the reaper's cooldown suppresses the seco |
| 118 | 0.60 | `terminal_widget.py:548` | _pending_writes grows unbounded on a discarded pane and is replayed as one giant termWrite on reattach | Teammate pane in a background project tab goes idle >10s -> #364 discard fires (_page_ready=False, renderer discarded). Pane then receives a new task and works for 30-60 min: every PTY chunk (spinner repaints, tool output) goes to `_pending_writes` (raw ANSI, MBs/hour). User switches to that tab ->  | Cap `_pending_writes` (keep only the tail, e.g. last 256 KB / N lines, since xterm scrollback is 500 lines anyway) and, when truncated, prepend a `\x1b[2J` or rely on the Ctrl-L redraw; or stop queuei |
| 119 | 0.75 | `update_panel.py:922` | 'Enable updates' converts to a git checkout synchronously on the Qt main thread (git init/remote/fetch/reset, up to ~2 min) → whole cockpit frozen | A copied/ZIP install (no .git, not site-packages) shows '🔧 Enable updates'; user clicks OK on the confirm → `init_git_repo()` runs `git init` (15 s cap), `remote add` (15 s), `fetch origin main` (60 s — a full history fetch), `reset --hard` (30 s) inline; `_status.showMessage('Converting…')` never r | Run init_git_repo (and the dirty re-check local_status) on a QRunnable like PullUpdateWorker/_safe_emit and finish in a slot; disable the chip while it runs. |
| 120 | 0.85 | `update_worker.py:352` | Silent self-update re-execs `python <path>/__main__.py` → ImportError on relative imports; the cockpit dies right after pulling on a git checkout | Dev/git-checkout install launched the documented way (agent-takkub.bat:110 `start "" ".venv\Scripts\pythonw.exe" -m agent_takkub`). origin/main gains commits (e.g. dependabot merge), tree is clean → try_silent_self_update pulls, prints 'restarting...' and calls `os.execv(sys.executable, [sys.executa | `os.execv(sys.executable, [sys.executable, "-m", "agent_takkub", *sys.argv[1:]])` (or reuse build_restart_successor_env + the same argv `_restart_cockpit` uses). |
| 121 | 0.85 | `usage_ledger.py:668` | Claude importer never reads subagent transcripts (projects/<slug>/<session>/subagents/*.jsonl) | Every `takkub assign --shards N` (1 pane + N native subagents, #641) and every Lead-side Agent call writes its assistant turns to projects/<slug>/<session-id>/subagents/agent-*.jsonl. import_claude only globs one level, so those tokens never reach the ledger: Settings > Usage and `takkub usage` unde | Use `projects_dir.rglob("*.jsonl")` (or add `*/*/subagents/*.jsonl`); request-id dedup in record_turn already prevents double counting. |
| 122 | 0.85 | `usage_ledger.py:1319` | 'วันนี้' / daily buckets use the UTC calendar day, not the user's local day | Bangkok user (UTC+7) opens Settings > Usage at 09:00 and picks 'วันนี้' (settings_usage.py:80-81 -> days=1): start/end are UTC dates, and _aggregate_rows buckets by ts[:10] (UTC). The row set covers 07:00 today .. now only; turns from 00:00-07:00 local are attributed to yesterday and the sparkline's | Convert each row ts to local date when aggregating/filtering (datetime.fromisoformat(ts).astimezone().date()) and compute start/end with datetime.now().date(); or label the range as UTC. |
| 123 | 0.80 | `user_actions.py:200` | 'Lead ใช้ provider' menu hardcodes claude/codex/gemini; opencode, kimi and cursor Leads cannot be selected | A user whose Lead runs on OpenCode/Kimi/Cursor (supported per lead_context.py and seen in the field) opens 🤖 Accounts -> 'Lead ใช้ provider': their current provider is not listed (no item checked) and they cannot switch to it from the UI. | Iterate `PROVIDER_REGISTRY` (filtered by installed/enabled via provider_state) for the Lead submenu instead of the literal triple. |
| 124 | 0.85 | `user_actions.py:854` | Settings -> Remote Enable runs the entire blocking remote start (bind, loopback probe, cloudflared spawn+sleep, 5 s public probe) on the Qt main thread | User clicks Enable in the Remote dialog with a named tunnel or a slow/unreachable edge: the whole cockpit (panes, IPC, Lead typing) freezes for ~1-8 s; the watchdog logs a main_thread_stall. #640 only moved the BOOT path off-thread. | Mirror the boot path: run `RemoteControl.prepare` and `probe_public` via `self.cli.run_off_thread`, then `finish_start()` + dialog `_render_state` in the main-thread callback; make `on_apply` async (d |
| 125 | 0.75 | `user_profile.py:402` | profile_for() does an uncached read_text per call; status header calls it 11x every 2s on the Qt main thread (the #658 class, only half-fixed) | status_header._update_status runs on a 2s QTimer (status_header.py:651) and calls _update_provider_chip (L760), which calls user_profile.profile_for once for Lead plus twice per other provider in PROVIDER_REGISTRY (L679, L685-688, L700-703) = 11 calls, each doing `path.read_text` + json.loads of pro | Read the per-project file through cached_read.read_cached(path, json.loads, missing={}) in profile_for/default_provider (the writer _atomic_write already invalidates at L85); and have _update_provider |
| 126 | 0.70 | `worktree_manager.py:2204` | clean_isolated's 5-minute dir-mtime guard blocks explicit --branch/--force/--discard cleans and mislabels the reason | Lead closes a pane and runs `takkub worktree clean --branch wt/backend-<ts> --discard` within 5 min of the pane's last top-level write (any build output or `pnpm install` bumps the worktree dir's own mtime, not just creation). Output: 'KEEP wt/backend-… — worktree สร้างใหม่ (120s ที่แล้ว) — pane อาจ | Skip the guard when `branch` is given or `force`/`discard` is set, and derive age from the registration (`.git/worktrees/<id>` creation time or the `.git` pointer file's mtime) rather than the checkou |
| 127 | 0.75 | `worktree_manager.py:2761` | done() still shells out to git on the Qt thread — #408 offload bypassed by later additions | Any `takkub done` from a shared-tree pane in a project with 2-3 other shared-tree panes on a large Windows repo: cli_server._run_off_thread precomputes collect_done_git_facts off-thread, then runs Orchestrator.done() on the Qt thread via _mainThreadCall (cli_server.py:289-309, 1159-1171). done() the | Move count_test_files_in_diff and _sibling_shared_tree_touched_paths into the off-thread `_collect` payload (extend done_git_inputs/collect_done_git_facts to return `test_files` and `sibling_touched`) |

## LOW (169)

| # | conf | file:line | เรื่อง | scenario | fix ที่เสนอ |
|---|---|---|---|---|---|
| 1 | 0.45 | `_win_console.py:190` | Periodic sweeper's already_seen set is never pruned; recycled HWND values are never re-evaluated | Long-running cockpit session: a console HWND value ruled 'not ours' (or already hidden) is destroyed and Windows recycles the same HWND value for a new codex `pwsh` console minutes later. `fresh = snapshot - already_seen` excludes it, so the parent walk never runs and the window stays visible (the f | Each sweep: `already_seen.intersection_update(current)` before computing `fresh`. |
| 2 | 0.70 | `agent_pane.py:131` | SESSION_CAP_SETTING is read from QSettings at pane construction but nothing ever writes it (phantom knob, and would be stale if it were) | The session-cap threshold is resolved once per pane from QSettings key `session_cap/prompt_tokens`; no Settings page or CLI path sets that key anywhere in the repo, so every user runs the hard default. If a writer were added later, existing panes would keep the old `_session_cap_spec` until re-creat | Either expose it in Settings > Performance and re-resolve in `apply_performance_settings`, or drop the QSettings read and make the default explicit. |
| 3 | 0.70 | `agent_pane.py:1171` | Constructing any pane overwrites the global "last zoom" fallback with that role's stored size | User zoomed Lead to 14 pt (writes `pane/_default/font_pt`=14). Cockpit boots or a teammate pane is created: `_restore_font_size` finds `pane/<role>/font_pt`=10 -> `set_font_point_size(10)` -> emits `fontSizeChanged` -> `_save_font_size(10)` -> `pane/_default/font_pt`=10. The next brand-new role pane | Have `_restore_font_size` apply the size without emitting (e.g. a `persist=False` flag on `set_font_point_size`), or only write `_FONT_SIZE_DEFAULT_KEY` from `_on_font_zoomed`/button paths. |
| 4 | 0.35 | `agent_pane.py:1182` | Auto clear-view writes Ctrl-L (\x0c) into every provider's PTY; redraw semantics only confirmed for claude | A codex/opencode/kimi/cursor teammate pane sits hidden and quiet for 10 min (`_check_idle_auto_clear`) or enters `done` (`_on_done_clear_timeout`) -> `session.write("\x0c")`. If that TUI does not bind Ctrl-L to redraw, the byte is either ignored (pane stays blank until next output) or inserted into  | Add a `ProviderSpec.clear_screen_seq` (None = skip the write, just wipe xterm) and gate the nudge on it; confirm per provider by capture before enabling. |
| 5 | 0.85 | `agent_pane.py:1230` | Export button writes only the visible pyte screen (36 rows), not the pane buffer/scrollback it advertises | User clicks ⤓ ("Export pane buffer to .txt") on a pane after a long run expecting the transcript; the file contains just the last screenful (rows x cols of the pyte grid), everything scrolled off is missing. | Use `self._terminal.request_buffer_text(lambda text: write(text))` with a fallback to `display_lines()` while discarded. |
| 6 | 0.85 | `auto_issue_capture.py:82` | Catch-all redaction regex blanks every ≥20-char identifier, so auto-filed tracebacks lose function names and paths | Any auto-captured cockpit crash: `_redact` runs `\b[A-Za-z0-9+/_-]{20,}\b` over the traceback, which matches ordinary Python identifiers (`_maybe_fire_auto_chain_handoff`, `_check_idle_teammates`, `_inject_shard_fanout_handoff`) and posix path fragments (`v2/state/issues/dedup`), so the GitHub issue | Require token shape (must contain both letters and digits, or no `_`), skip tokens that are valid Python identifiers / follow `in ` on a `File "..."` frame line, or run the catch-all only on the excep |
| 7 | 0.90 | `auto_issue_signals.py:394` | `stop_watch` is never called | Only `start_watch()` is wired (app.py:1348); the scan thread is daemon so nothing breaks, but the stop API and `_stop` Event are unreachable in production. | Wire into app shutdown or remove `stop_watch` + `__all__` entry (keep `_stop` for the loop). |
| 8 | 0.70 | `backlog_dialog.py:210` | Order-mode click / quick action rebuilds the list, which clears selection and blanks the detail pane | Owner toggles 'โหมดเลือกลำดับงาน' and clicks a card: `_on_click` → `_rebuild_list()` → `QListWidget.clear()` emits `currentItemChanged(None, prev)` → `_on_select(None)` sets the detail pane to '' and no item is current afterwards; every pick wipes the detail the owner was reading. Same after `พักไว้ | In `_rebuild_list` capture `self._current_id()` first, block signals during `clear()`/`addItem`, then re-select the row whose UserRole matches; in `_confirm` show a status hint when nothing is selecte |
| 9 | 0.55 | `boot_flow_window.py:1021` | Window's has_update (`latest != current`) ignores backend status/selected → phantom 'มีอัพเดต' rows whose 'อัพเดตที่เลือก' silently does nothing | A provider whose `--version` probe fails/times out (current None) or whose installed build is newer than the registry's latest tag: backend sets status FAILED/UP_TO_DATE with selected=False (boot_flow.py:159-183, compare_versions-based) but the window computes `has_update = bool(latest) and latest ! | Drive rows from `item.status == PROVIDER_STATUS_UPDATE_AVAILABLE` (show FAILED rows as 'ตรวจสอบไม่สำเร็จ') and pass `replace(item, selected=True)` for user-checked rows. |
| 10 | 0.70 | `boot_flow_window.py:2162` | Remembered 'skip' still runs the full provider probe every boot (5 `--version` subprocesses + npm view calls) before proceeding | User ticks 'จำตัวเลือกนี้ไว้' and 'ข้าม — ใช้เวอร์ชันเดิม'. Next boots: start() sees mode 'skip' but `_apply_remembered_choice` starts `_CallWorker(check_provider_updates(30.0))` first and only checks `mode == 'skip'` inside `_on_items`. Measured 1.8 s online here; offline/flaky network each `npm vi | In start()/_apply_remembered_choice, `if mode == 'skip': self._proceed_to_migration_check(); return` before creating the worker. |
| 11 | 0.85 | `chatlog_scanner.py:325` | count_user_corrections / count_tool_retries / count_hook_fires have no callers (superseded by scan_hot_md_metrics) | No call site anywhere in src/ (orchestrator uses scan_hot_md_metrics at 11897); the three functions each re-walk every jsonl and duplicate the logic inside scan_hot_md_metrics, so any future fix has to be made twice. | Delete the three functions (and any tests that only exercise them) or make scan_hot_md_metrics the single implementation they delegate to. |
| 12 | 0.80 | `cli.py:404` | _instance_banner's installed-build branch probes REPO_ROOT/runtime/port where REPO_ROOT is <venv>/Lib — dead: prod CLI can never warn that a dev cockpit is also running | User has both prod (installed) and dev cockpits up and runs `takkub list` from a prod pane: the code looks for the dev instance's port file at `Path(config.REPO_ROOT) / "runtime" / "port"`, but for an installed package `REPO_ROOT = Path(__file__).resolve().parents[2]` = `<venv>/Lib` (site-packages'  | Either drop the installed-build branch (accept one-directional warning) or resolve the dev checkout via a recorded path (e.g. a `dev-checkout` breadcrumb the dev cockpit writes into ~/.agent-takkub/ru |
| 13 | 0.95 | `cli.py:4458` | cmd_provider install: `if … : pass` no-op block | After a failed `takkub provider install <name>` for a manual-only provider the code evaluates `installable_providers()` and `PROVIDER_REGISTRY` only to execute `pass`; nothing observable happens on either branch. | Delete the three lines (and the now-unused `installable_providers` import at 4368). |
| 14 | 0.80 | `cli.py:6419` | docs-verify / audit-skills write their report into `runtime/…` relative to the pane's CWD — litters a `runtime/` dir into the user's project repo | A pane (any role — neither command is lead-gated) runs `takkub docs-verify` or `takkub audit-skills` while cwd is the user's project (the normal pane cwd): `Path(args.report)` = `runtime/docs_drift.md` relative to cwd, `output_path.parent.mkdir(parents=True, exist_ok=True)` (3300, 3376) creates `<us | Default the report path under `config.RUNTIME_DIR` (e.g. `RUNTIME_DIR / "docs_drift.md"`) and anchor a relative `--report` to REPO_ROOT, not cwd. |
| 15 | 0.90 | `cli.py:6691` | `mcp add --command` uses dest 'command' and overwrites the top-level subcommand name (same class as #483) | `takkub mcp add foo --command npx …` → after parsing, `args.command == 'npx'` (not 'mcp'), so main()'s `_enforce_role_gate(args.command)`, the `_WRITE_COMMANDS_REQUIRE_CONFIRMATION` check and the `{list,status,inbox}` banner check all key on the MCP executable name; `--command list` prints the other | `sm_add.add_argument("--command", dest="mcp_exe", …)` and use `args.mcp_exe` in cmd_mcp (5266). |
| 16 | 0.70 | `cli_server.py:676` | `_done_auth_warned` latch is never cleared — only the first stale-token `done` per (project, role) ever reaches Lead | backend's pane is respawned; its old shell (or a retried CLI) calls `takkub done` with the retired token → Lead gets `[done-auth-rejected] backend` once. Days later the same thing happens for a new backend pane → silently rejected, Lead sees 'nothing happened'. | Key the latch by token instead of (project, role), or clear the pair when a new token is minted for that role. |
| 17 | 0.80 | `cli_server.py:1296` | "version" cmd alias is never sent by any client | `elif cmd in ("instance-identity", "version")` — cli.py only ever sends `instance-identity` (cli.py:305, 3693); nothing in src/ sends `version`. | Remove the alias (or document it as an external-client contract). |
| 18 | 0.80 | `cli_server.py:1656` | `team set` escalation gate only checks `from == lead`; the command is neither lead-only nor token-gated, so a teammate pane can flip the project to solo-lead/pair | A backend/codex pane runs `takkub team set solo-lead` (from: backend). cli.py LEAD_ONLY_COMMANDS (cli.py:37-70) has no `team`, the server's `_LEAD_ONLY_CMDS`/`_token_gated_cmds` don't include it, and line 1656 skips the B-M1 check for non-lead callers → `set_team_preset('solo-lead')` succeeds and `l | Require the Lead token OR an empty `from` (non-pane terminal) for `team set`; reject any non-empty non-lead `from`. |
| 19 | 0.80 | `cli_shim.py:69` | Generated takkub.cmd spawns a PowerShell child on every invocation (~220-260 ms) just to read 2 header bytes | Any `takkub …` call routed through cmd.exe/PowerShell on Windows (codex/opencode panes use PowerShell; a user's PowerShell terminal; the dev `bin/takkub.cmd` has the identical `for /f … powershell -NoProfile` line 32) pays a full `powershell.exe` start before Python even launches — on top of Python  | Replace the PowerShell probe with a cmd-native check (e.g. `findstr /B /M /C:MZ` on the first bytes via `more +0` is unreliable; simplest: `certutil -encodehex … 1` or skip the magic check in the .cmd |
| 20 | 0.55 | `cockpit_theme.py:972` | `QLineEdit::placeholder` is not a Qt QSS sub-control — rule is ignored, placeholder stays unreadable | Settings/New Role inputs on dark ground: the comment says this rule fixes the near-invisible placeholder, but Qt's style-sheet parser has no `::placeholder` pseudo-element, so the block is dropped and the palette default is still used. | Set `QPalette.ColorRole.PlaceholderText` on the application/dialog palette (retheme() hook) instead of a QSS pseudo-element. |
| 21 | 0.70 | `codex_agents_md.py:319` | _ensure_git_excluded writes an unanchored `AGENTS.md` pattern to .git/info/exclude and never removes it — hides any future user AGENTS.md at any depth from git status | Spawn any codex/agy teammate into a repo once → `.git/info/exclude` gains the line `AGENTS.md` (matches at every depth). remove_managed_context_files later deletes the planted file but the exclude line stays. When the user later writes their own AGENTS.md (root or e.g. apps/api/AGENTS.md) it never s | Anchor the pattern (`/AGENTS.md`) and remove that exact line in remove_managed_context_files when the planted file is deleted. |
| 22 | 0.60 | `codex_agents_md.py:447` | ensure_agents_md treats a user-owned EMPTY AGENTS.md as cockpit-managed, overwrites it, and later deletes it | User keeps an empty AGENTS.md placeholder in their repo; a codex/agy pane spawns → `existing.strip()` is falsy so the user-owned check is skipped, the file is overwritten with the marker cheatsheet, and when the last pane closes remove_managed_context_files (orchestrator.py:2062) unlinks it — the us | Treat any pre-existing file without the marker as user-owned regardless of content (`if TAKKUB_MARKER not in first:`). |
| 23 | 0.50 | `codex_helper.py:129` | resolve_codex_jsonl_for_cwd opens every rollout in the whole store (no day bound, no cache) — 800 files here — and is called on the Qt main thread at resume | Resume a codex Lead/teammate: spawn_engine.py:527-529 `_resume_uuid_matches_provider_cwd` → resolve_codex_jsonl_for_cwd(cwd, uuid) rglobs sessions/ + archived_sessions/ and read_codex_session_meta() opens + readline()s each rollout until the id matches; ~/.codex/sessions holds 800 rollout-*.jsonl on | Walk day directories newest-first (reuse the sharded walk) and stop at a not_before cutoff; memoize id→path per process (a rollout's id/cwd never change). |
| 24 | 0.85 | `config.py:112` | _write_json_atomic's `durable=True` branch is never taken | No caller in src passes durable=True (grep finds the flag only in the docstring), so the opt-in fsync path at config.py:127-128 is dead-by-construction. | Drop the parameter, or pass durable=True from the one writer that needs crash durability (e.g. auto_migrate_boot.py:154). |
| 25 | 0.90 | `core/accounts/registry.py:93` | Account pools are never written, so every selector strategy, switch_log and the pool branch of resolve_account_for are unreachable; ProviderAccount.limits is dropped on read | No code in src calls `AccountPoolRegistry().upsert` (the Accounts & Pools page was removed — accounts_adapter.py:297-299 only shows leftover rows), so `_pool_for` (facade.py:27-34) always returns None and `Sticky/QuotaAware/CooldownFailover` selectors, `selector_for`, `log_switch`, `read_switches` a | Delete AccountPool/selector/switch_log code paths or add a real writer; if keeping the registry, round-trip `limits` in `_record_to_account`. |
| 26 | 0.90 | `core/brain/adapter.py:32` | NativeBrainAdapter, facade.recall and the brain_read circuit breaker are never used | NativeBrainAdapter is only exported from core/brain/__init__.py; nothing constructs it. facade.recall (facade.py:60-86) with its dedicated get_breaker('brain_read', 10 failures/20 s) has no caller: context_builder deliberately uses RetrievalEngine directly (context_builder.py:6-13). The breaker poli | Delete adapter.py, facade.recall/submit's public surface if unused, the brain_read breaker registration, and tests/test_core_brain_adapter.py + tests/test_core_brain_facade_recall_breaker.py (or wire  |
| 27 | 0.85 | `core/brain/context_builder.py:124` | Global brain bucket (_global.jsonl) is queried on every assign but nothing ever writes to it | _recall_records runs a second RetrievalEngine(BrainStore(None)).recall(...) per assign and doctor.py:3984 counts BrainStore(None). Every writer (facade.on_pane_done -> submit(candidate, project=project), lines 305-315) appends to the PROJECT file regardless of candidate.scope; no code routes Scope.G | Route GLOBAL/USER-scoped candidates to BrainStore(None) inside facade.submit (store by scope, not caller project), or remove the global query. |
| 28 | 0.80 | `core/brain/context_gate.py:194` | gate_budget's retry ceiling multiplier can never change the budget | facade.build_context_for_assign applies escalate_for_retry first (facade.py:176-178), so any retry_count>0 arrives with size already >= medium (ceiling 8000/12000), while base_budget from budget_tokens_for is clamped to <= 6000 (context_builder.py:38 _BUDGET_CEILING). min(base_budget, ceiling*multip | Either raise _BUDGET_CEILING / apply the multiplier to base_budget, or delete the retry_count kwarg and its tests. |
| 29 | 0.90 | `core/brain/facade.py:233` | Context trace always saved with sources=() / latency 0 / dedup 0 -> doctor [context] and Settings Knowledge show hollow numbers; the whole context_sources layer is unused | _save_gate_trace builds ContextTrace(mode=..., sources=(), total_tokens, budget_tokens, dedup_count=0, latency_ms=0.0) on every gated assign, so trace_store/last_context_trace.json never carries per-source counts, real latency or dedup. doctor_section.build_findings (doctor_section.py:54-58) then pr | Either wire build_context through the ContextSource layer (measure latency, fill SourceTrace, run collapse_near_duplicates/apply_scope_and_trust) or delete core/context_sources/{base,brain_source,conv |
| 30 | 0.90 | `core/brain/sources/decision_note_source.py:41` | Three brain ingest adapters (decision notes, knowledge base, role memory) are never called | from_decision_note_text, knowledge_source.from_knowledge_base_text (knowledge_source.py:30) and role_memory_source.from_role_memory_text/parse_bullets (role_memory_source.py:24,36) have no caller in src; only reflection_source and digest_facts_source are wired via facade.on_pane_done (facade.py:299- | Wire them into on_pane_done/boot backfill, or delete the three modules plus tests/test_core_brain_sources.py:35-125. |
| 31 | 0.80 | `core/brain/task_complexity.py:118` | Thai file-count regex never matches 'N ไฟล์' followed by space/end (\b after ์ fails) | A Thai Lead assigns 'แก้ 3 ไฟล์ ใน src' or 'แก้ 3 ไฟล์'. `\b(\d+)\s*(?:files?\|ไฟล์)\b` requires a word boundary after ์ (U+0E4C), but ์ is not \w (isalnum() False) and neither is the following space/end, so there is no boundary and the count is ignored -> estimated_files=0 -> smaller size/budget th | Use `(?:files?\b\|ไฟล์)` so the trailing \b applies only to the ASCII alternative. |
| 32 | 0.50 | `core/capabilities/audit.py:75` | Lockless cross-process append to events.log from the guard hook subprocess | `log_capability_event` runs inside the `takkub _guard` hook subprocess (cli.py:4997 → PermissionEngine.evaluate_shell_command) on every DENIED Bash call, appending to the same `runtime/events.log` the cockpit process appends to, with a plain `open(..., 'a')` and no lock. On Windows two processes app | Route hook-side audit lines through the IPC server (cockpit is the single writer) or use a per-process side file merged by the cockpit; at minimum skip rotation from the hook process. |
| 33 | 0.90 | `core/capabilities/registry.py:46` | CapabilityRegistry, PluginManager, design_integrations.build_client and the Figma/21st clients have no production callers | `CapabilityRegistry` (registry.py:46) and `PluginManager` (plugin_manager.py:39) are never instantiated outside core; `build_client` (design_integrations.py:255) is only reached through the dead `CapabilityRegistry.design_integration_client`, so `TwentyFirstClient`, `FigmaClient`, `IntegrationDenied | Delete registry.py, plugin_manager.py, `build_client` + TwentyFirst/Figma clients (keep `detect_storybook`, `integration_config_status`, `optional_design_mcp_status`, `register_twentyfirst_mcp`, `Perm |
| 34 | 0.90 | `core/conversation/facade.py:131` | Every done() writes a 6-file checkpoint and re-parses the whole message history, but no product code ever reads checkpoints | _on_pane_done_impl always calls CheckpointManager(cs).create, which (checkpoint.py:87-103) writes summary/working-context/tasks/git-state/provider-binding/runtime-state under checkpoints/checkpoint-NNNNN/ and calls store.read_messages (store.py:154-169: loads and json-parses EVERY rotated part) to k | Drop the per-done checkpoint (or gate it behind an actually wired resume path) and delete resume.py; if kept, read only the last part's tail. Tests pinned to it: tests/test_core_conversation.py:232-34 |
| 35 | 0.95 | `core/conversation/facade.py:152` | on_account_switch is a permanent no-op: the only caller never passes conversation_id | core/accounts/selector.py:190-192 calls on_account_switch(pool.id, pool.provider_id, prior_id, picked_id, reason=...) without conversation_id, so line 152 `if not v2_conversation_enabled() or conversation_id is None: return` always returns. The 'checkpoint before account switch' feature never fires  | Either derive conversation ids from the pool's live panes (conversation_id_for(project, role)) at the call site or delete on_account_switch + tests/test_core_conversation.py:432-520. |
| 36 | 0.50 | `core/conversation/ingest/claude_adapter.py:68` | Byte-offset ingest advances the cursor past a torn trailing line, dropping that record forever | The ingest thread starts the instant `takkub done` returns (orchestrator.py:7936), while the pane's CLI is still flushing the current turn's JSONL line (tool_result / assistant text). read_new reads to EOF, the incomplete last line fails json.loads and is skipped, but next_cursor is set to the full  | Only consume up to the last b"\n" in `raw`; return next_cursor = offset + index_of_last_newline + 1 so a partial line is re-read next time. |
| 37 | 0.95 | `core/conversation/resume.py:41` | resume.py (build_resume_context / render_resume_prompt / ResumeContext) has no caller | Grep across src finds no import of build_resume_context or render_resume_prompt; only tests/test_core_conversation.py:297-340 exercise them. RollingSummary.objective and important_files (summary.py:36,42) are read only here and never written anywhere. | Delete resume.py and its three tests, or wire it into spawn_engine's resume-failure path; drop objective/important_files from RollingSummary if not wired. |
| 38 | 0.90 | `core/conversation/summary.py:40` | RollingSummary.in_progress / objective / important_files have no writer; 'in progress' context lines can never appear | apply_done_note only filters in_progress (line 130) and never adds to it; nothing sets objective or important_files. context_builder._recent_summary_lines (context_builder.py:204) therefore never emits '- in progress:' lines, and the fields only ride along into the (dead) resume prompt. | Populate in_progress from assign (the assign hook has the task headline) or remove the fields and the context line. |
| 39 | 0.90 | `core/migration/engine.py:941` | MigrationEngine.validate_ok_steps() has no production caller (removed in #574 round14b) | apply_pending() now fills last_validate_reports itself; auto_migrate_boot.py:473 and boot_flow.py:811 only mention validate_ok_steps in comments. Only tests/test_core_migration.py:645-728 still call it. | Delete the method and its tests, or keep it as the documented CLI helper if cli.py is meant to use it. |
| 40 | 0.95 | `core/migration/pre_migrate_backup.py:654` | PreMigrateBackupStep.restore_into() has no callers | cli.py restore-v1 uses restore_from_backup_dir (4284) and restore_backed_up_items (4232) directly; nothing calls restore_into. | Delete restore_into(). |
| 41 | 0.95 | `core/migration/promote_v1.py:2300` | ArchiveV1LegacyStep._run_id ClassVar is unused by its own admission | Declared and never read or written anywhere. | Delete the attribute. |
| 42 | 0.80 | `core/migration/steps_v1.py:688` | credential-reference records are written every migration but never read by anything; kimi (the only provider with a #504 providers/<p>/default home) is omitted | _refs() only enumerates claude/codex/opencode; config._PROVIDER_HOME_SUBDIRS (config.py:287-309) also isolates kimi under providers/kimi/default, so a kimi account gets no provider.json/account.json. But no module outside core/migration reads "provider.json"/"account.json"/config_dir_exists (grep),  | Either wire a reader (accounts registry) and add kimi, or drop the step from the ladder. |
| 43 | 0.95 | `core/migration/wal.py:37` | wal.write_json_durable() is dead: every ledger/manifest call site passes write_json_atomic and nothing in src calls it | Every TransferLedger construction in promote_v1.py/pre_migrate_backup.py passes write_fn=write_json_atomic; the only importer is tests/test_core_migration_round4_wal.py:39. | Remove it (and the test import) or make TransferLedger default to it and drop the per-call write_fn. |
| 44 | 0.90 | `core/model_catalog/registry.py:63` | core/model_catalog package (ModelRegistry, ModelProfileRegistry, 4 legacy readers) is unused by product code | No module in src imports core.model_catalog; the only reference is a migration test fixture (tests/test_core_migration.py:1114-1142). legacy.py:120-125 claims `core.routing.Router.effective_model_for` needs read_legacy_role_model_pin, but grep finds no such caller. Model/role pins are served by prov | Delete core/model_catalog/ and tests/test_core_model_catalog.py (34 tests); replace the fixture usage in tests/test_core_migration.py:1114-1142 with a plain JsonlStore, or wire the registry into routi |
| 45 | 0.95 | `core/providers/registry.py:11` | ProviderAdapter layer (adapter_for, ClaudeCliAdapter, CliProviderAdapter, build_plan, SpawnPlan, ProviderAdapterNotWired) is dead-by-construction | spawn_engine imports only `assemble_generic_argv` (spawn_engine.py:2947), `assemble_claude_argv` (:3824) and `account_env_overrides` (:263). `adapter_for`, both adapter classes, their `build_plan`, `build_generic_spawn_plan`, `models/spawn_plan.SpawnPlan` and `errors.ProviderAdapterNotWired` are nev | Remove registry.py, claude_adapter.py, cli_adapter.py, errors.py, `build_generic_spawn_plan`, models/spawn_plan.py and contracts/provider_adapter.py, keeping only plan.py's `account_env_overrides`/`as |
| 46 | 0.85 | `core/resilience/circuit_breaker.py:153` | Persisted breaker state stores time.monotonic() values that takkub doctor renders as absolute times and as a live OPEN state | `open_until`/`last_healthy` are `self._clock()` = `time.monotonic()` (default at line 101), snapshotted to DATA_HOME/resilience/state.json (lines 239-242). `doctor_section.build_findings` (doctor_section.py:76-77) prints `open_until=<monotonic seconds since that machine booted>` and flags WARN for s | Persist wall-clock equivalents (e.g. `time.time() + (open_until - monotonic_now)`) or a remaining-seconds field; render OPEN from a snapshot as 'last seen open' info rather than WARN; use `Callable`. |
| 47 | 0.90 | `core/scheduling/backpressure.py:63` | Backpressure ladder is non-monotonic: PAUSE_BACKGROUND admits LOW priority that the milder THROTTLE_NEW denies | `_MIN_PRIORITY_ADMITTED` maps THROTTLE_NEW->NORMAL (denies LOW, BACKGROUND) but the harsher PAUSE_BACKGROUND->LOW (denies only BACKGROUND). A LOW-priority request is refused while the machine is merely warm and accepted once it is overloaded. No production effect today because every caller uses the  | Make the table monotonic (PAUSE_BACKGROUND -> Priority.NORMAL or THROTTLE_NEW -> Priority.LOW) and fix the two asserts; or drop the Priority axis entirely since nothing sets it. |
| 48 | 0.90 | `core/scheduling/policy.py:25` | provider/account/project slot dimensions and the settings-file policy are unreachable in production (Scheduler page removed, request_slot never passes provider_id/account_id) | `save_scheduler_policy` (core_v2_settings.py:241) has no caller in src — the Settings Scheduler view was dropped (#515, settings_window.py:222 redirects VIEW_CORE_V2_SCHEDULER to VIEW_GENERAL), so core-v2-settings.json never gets a scheduler_policy and `effective_slot_policy()`'s settings path alway | Either wire `provider_id=effective_provider` into `request_slot` and restore a UI/CLI writer for the policy, or delete the provider/account/project dimensions (+ `SchedulerPolicyConfig` fields, `_poli |
| 49 | 0.95 | `core/scheduling/runtime_control.py:35` | AgentRuntimeControl / RunState.LIMIT_WAIT and ProcessRegistry have no production callers | `AgentRuntimeControl` (pause/resume/park_for_limit/checkpoint) and `ProcessRegistry` (process_registry.py:38, detect_stale) are constructed nowhere outside their own modules; limit_autoresume.py and the watchdog cluster still use `PaneState.rate_limited_until`, so the '#322 first-class scheduler sta | Delete both modules (and their tests) or wire `park_for_limit`/`resume_from_limit` into limit_autoresume; do not leave the #322 claim in the docstring. |
| 50 | 0.80 | `core/secrets/backends/file_backend.py:39` | Design-tool tokens are written world-readable on macOS (default write_text permissions) | Settings → Knowledge/Design → save credential (settings_knowledge_design.py:464) or `takkub design integrations enable --token` (cli.py:2011) stores the Figma/Penpot/21st token via `FileSecretBackend.set`, which writes `~/.takkub/secrets/<id>.json` (plus a `.tmp`) with `write_text` — 0644 under the  | Create the file with `os.open(tmp, O_WRONLY\|O_CREAT\|O_TRUNC, 0o600)` (and `os.chmod` the parent dir 0o700) before writing; on Windows optionally tighten the ACL. |
| 51 | 0.95 | `core/secrets/backends/wincred_backend.py:71` | WinCredBackend is never registered; secret://claude and secret://codex refs are minted but never resolved | `default_backends()` (manager.py:64-80) never includes `WinCredBackend`, and nothing else constructs it, so its ctypes CredReadW path only runs in tests. `legacy_reader.read_legacy_accounts` (legacy_reader.py:49) mints `secret://<provider>/<name>` refs that no caller ever passes to `get_secret`/`sta | Remove wincred_backend.py (and the claude/codex backends + `secret_ref` on legacy accounts) until a caller exists. |
| 52 | 0.90 | `core/storage/legacy_reader.py:40` | RoleProviderRoutingPolicy / read_role_provider_routing_policy are test-only | provider_config.py has its own _read_routing over config/routing.json; nothing in src imports read_role_provider_routing_policy or RoleProviderRoutingPolicy (only tests/test_core_jsonl_store.py:133-141). The 'Phase 1 proof of pattern' reader never became a real reader. | Delete the dataclass + function + tests; keep read_json. |
| 53 | 0.90 | `core/storage/paths.py:68` | conversation_dir() (with its project-root legacy-fallback branch) has no caller in src | ConversationStore builds paths from conversations_root() plus its own conversation_dir method (core/conversation/store.py:55-63); the only reference to paths.conversation_dir is a docstring (store.py:3). The `project_layout_root.is_dir()` flip at paths.py:82-83 therefore never runs, so the 'conversa | Delete paths.conversation_dir (and its test if any) or make ConversationStore use it — but not both definitions. |
| 54 | 0.85 | `core/versioning/probe.py:141` | Schema-drift detection is unreachable: detect_drift/probe_all/detect_all/CompatibilityMatrix.register+supports_feature have no src caller and no baseline is ever recorded | doctor.py:3514 calls probe_store only to print the fingerprint size; nothing ever calls record_component with a `store_schema:<provider>` component (grep: the string appears only in probe.py:145's docstring), so detect_drift (probe.py:141) can never have a previous_fingerprint and is only invoked by | Either wire doctor to record `store_schema:<provider>` via record_component and call detect_drift on the next run, or delete detect_drift/probe_all/detect_all/register/supports_feature and their tests |
| 55 | 0.60 | `core_v2_settings.py:188` | Corrupt core-v2-settings.json silently reads as defaults; the next Settings save writes defaults back, erasing the TypeSafe API key / scheduler policy | File becomes unparsable (partial disk, hand edit). load() swallows the error, caches _default_payload() under the corrupt file's stat key, and every save_* helper does `payload = load(); payload[key] = …; save(payload)` — so a Settings → Usage save of decide_mode rewrites the file with typesafe_api_ | Log a warning with the path on parse failure and archive the corrupt file via config.archive_settings_file before any save overwrites it. |
| 56 | 0.95 | `cursor_helper.py:365` | cursor_first_user_preview: `if session_uuid == path.parent.name: session_uuid = path.parent.name` is a no-op branch | The assignment sets the variable to the value it was just compared equal to; the intended case (file named differently from its session directory) is never handled, so a flat `<uuid>.jsonl` vs `<dir>/<other>.jsonl` layout is not normalized. | Probably meant `if path.parent.name looks like a uuid: session_uuid = path.parent.name` — implement that or drop the branch. |
| 57 | 0.90 | `cursor_helper.py:476` | cursor_exec / find_cursor_executable / _default_cursor_paths and opencode_exec have no production caller (tests only) | Binary discovery for cursor is done by provider_spec._discover_cursor (provider_spec.py:514, own shutil.which loop) and there is no `takkub cursor` / `takkub opencode` one-shot command (cli.py defines only cmd_codex :3579 and cmd_gemini :3606). Only tests/test_cursor_helper.py and tests/test_opencod | Delete cursor_exec, find_cursor_executable, _default_cursor_paths, opencode_exec and the corresponding test classes, or wire real `takkub cursor` / `takkub opencode` subcommands if the one-shot path i |
| 58 | 0.45 | `disk_usage.py:1283` | `takkub prune --level review --yes` with no --category implicitly deletes chat-history (user's own ~/.claude/projects on a dev checkout) and orphan-worktrees-review (source code) | With categories omitted, prune() expands to every category whose rank <= review, which includes `chat-history` (default_claude_config_dir()/projects/**/*.jsonl older than 30 days — on a dev checkout that is the user's personal ~/.claude history for ALL their sessions, not only cockpit panes; breaks  | Require an explicit --category whenever level==review (refuse the implicit expansion), and print every chat-history/exports target path before deletion like orphan-worktrees-review already does. |
| 59 | 0.60 | `doctor.py:213` | prod-profile auth check is file-only, contradicting the Keychain/Credential-Manager handling 100 lines above → false 'not logged in' WARN on installed builds | Installed cockpit (DATA_HOME != REPO_ROOT). The default-profile branch (l.113-145) reads the macOS Keychain and on Windows treats a missing file as SKIP 'not directly checkable'. The prod-profile branch checks only `_DEFAULT_CONFIG_DIR / ".credentials.json"` → on macOS (token in Keychain) and on Win | Reuse the same platform ladder for the prod profile (Keychain lookup on darwin; SKIP not WARN on win32 when the file is absent). |
| 60 | 0.85 | `doctor.py:462` | npx baseline grading is dead on Windows: probes bare `npx` instead of the resolved `npx.cmd` path it just found | `shutil.which("npx")` returns `…\npx.cmd`, but `_run(["npx", "--version"])` passes the bare name; CreateProcess cannot launch a .cmd without its extension, so rc≠0 and `_core_finding` always reports 'present · not probed' (l.432-433). The min/recommended npx version check therefore never runs on Win | `_run([npx, "--version"])` (and `_run([node, "--version"])` for symmetry). |
| 61 | 0.80 | `exec_mode.py:96` | machine_fanout_cap and MAX_FANOUT have no production callers (docstring-only mention in orchestrator) | orchestrator uses machine_total_pane_cap() only; the only 'machine_fanout_cap' text in src outside this module is a docstring at orchestrator.py:14727. The module docstring itself says it is 'exercised directly by tests for that reason'. | Remove machine_fanout_cap + MAX_FANOUT and the tests that pin them; keep machine_total_pane_cap. |
| 62 | 0.40 | `file_watch_service.py:143` | Watch is lost after delete-then-recreate (git checkout/stash, safe-write editors) -> later external edits unseen | File open in the editor; `git checkout <branch>` unlinks and recreates it. Qt drops a deleted path from the watcher; _on_file_changed only re-adds when p.exists() at signal time, so if the recreate lands after the signal the file gets a diskRemoved banner and is never watched again until the tab is  | In _flush, when `p.exists()` and `path not in self._watcher.files()`, call addPath(path) before dispatching the snapshot worker. |
| 63 | 0.55 | `gemini_helper.py:571` | resolve_agy_project_id sleeps up to 4 s on the Qt main thread when a second gemini spawn hits an unregistered cwd within 60 s | Spawn a gemini pane into a cwd agy has not yet registered (first spawn claims _MINT_INFLIGHT), then within 60 s restart that Lead or spawn another gemini role into the same cwd before agy has written ~/.gemini/config/projects/<uuid>.json (agy boots 5-10 s): provider_spec.py:491 `_resolve_gemini_proj | Return None immediately with a `minting` hint and let spawn re-check after boot, or move the poll into the async spawn path; never sleep in spawn(). |
| 64 | 0.40 | `git_changes_service.py:526` | Overlapping _StatusWorker runs have no generation guard — a slow `git status` can deliver a stale CHANGES list after a newer one | Big repo where `git status --porcelain=v2` takes longer than the 1.5 s debounce while a pane writes files continuously: request_refresh fires _run every 1.5 s, each starting a new _StatusWorker on the global pool; workers serialize on SUBPROCESS_LOCK in no guaranteed order, and every one emits chang | Stamp each worker with an incrementing `_status_gen` and drop results whose gen < the latest dispatched; skip starting a new worker while one is in flight (set a pending flag and re-run on completion) |
| 65 | 0.60 | `graft_autobuild.py:333` | `git ls-files --cached` keeps worktree-deleted tracked files in the staging mirror (hardlink keeps the old bytes alive) | A pane deletes a tracked file with plain `rm`/Edit-delete (no `git rm`). The file is still in the index so `--cached` lists it; `_sync_staging` keeps it in `desired` and never unlinks the mirror copy, and because the mirror entry is a hardlink the deleted content stays on disk. graft keeps answering | Drop rels whose source `stat` fails (or add `--deleted` handling: subtract `git ls-files --deleted` from the set) before computing `desired`. |
| 66 | 0.55 | `graft_autobuild.py:614` | done()-triggered rebuild is dropped (not queued) while a build for the same dir is already past its staging sync | Two roles share a cwd. Role A `takkub done` → 20s debounce → `graft build` starts (minutes on a real repo). Role B edits files and calls done 30s later → its debounce fires → `_build_one` returns at line 614 because the key is still in `_building`; A's build already ran `_sync_staging` (line 566) be | Record a 'dirty' flag when single-flight rejects a call and re-run `_build_one` once in the `finally` of the in-flight build when the flag is set. |
| 67 | 0.85 | `graft_store.py:326` | iter_store_dirs has no callers (disk_usage uses read_store_manifest with its own directory walk) | No call site in src/. | Delete, or make disk_usage.py use it. |
| 68 | 0.70 | `headless_window.py:145` | Headless boot leaves the last saved tab as the active project instead of the persisted active one → `takkub assign/send` without --project and the PWA target the wrong project | Headless cockpit restarts with tabs [A(active), B, C] saved: boot() opens A then B then C and `_open_project_tab` calls `set_active_project(project_name)` each time, so active ends as C. cli.py:3425/3540 default `args.project or active_project()[0]` and remote/api.py:120 read the same key, so a `tak | After the loop in boot(): `if active and active in self._tabs: set_active_project(active)`. |
| 69 | 0.90 | `hook_wiring.py:93` | role_wants_concise / role_wants_remote_control are matched on the shard name (qa#1), so shards never get Concise and explicit role lists miss them | spawn_engine.py:3448 passes role_name ('qa#1', 'qa#2') not base_role. Default roster {'qa'} matches 'qa' only, so a fan-out of the pilot role gets full output style on every shard (the token-diet measurement in #318 is skewed); likewise TAKKUB_CONCISE_ROLES=qa or TAKKUB_REMOTE_CONTROL_ROLES=qa never | Strip the `#N` suffix before matching (or pass base_role from spawn_engine.py:3448-3449). |
| 70 | 0.95 | `issues.py:34` | `_ROLES` tuple is defined but never referenced | Constant is not used for validation or labels anywhere; role labels are built from the caller-supplied `role` string. | Delete the constant. |
| 71 | 0.95 | `issues.py:167` | `"Could not resolve" in msg.lower()` can never match (mixed-case literal vs lowercased message) | `gh repo view` fails with 'could not resolve to a Repository' → the friendly 'no GitHub remote found … gh repo create' hint is never produced; the user gets the generic 'cannot detect GitHub repo: …' text instead. No functional impact beyond the message (outer callers catch RuntimeError either way). | Use `"could not resolve" in msg.lower()`. |
| 72 | 0.95 | `lead_bash_audit.py:76` | audit_lead_bash / detect_write_intent are dead — no runtime caller, only tests | Nothing wires this as a hook (hook_wiring.py only installs `takkub _guard`, `_activity`, session-report); the docstring's settings.json snippet was never applied. `tests/test_lead_bash_audit.py` and the conftest monkeypatch at tests/conftest.py:528-530 exercise code that never runs in production. Th | Delete the module, its test file, the conftest entry and the pyproject contract line (or actually wire it if Phase 2 is still wanted). |
| 73 | 0.35 | `lead_context.py:1006` | Plugin version dirs sorted lexically — '4.10.0' loses to '4.9.0' when a plugin update leaves the old dir | `claude plugin update` writes `<plugin>/4.10.0/` while `4.9.0/` remains; `sorted(reverse=True)` on strings picks '4.9.0' → panes are spawned with `--plugin-dir` of the stale version. Latent on this machine (one version dir per plugin today). | Sort with a numeric version key (e.g. `packaging.version` or split-on-dot ints, non-numeric names last). |
| 74 | 0.70 | `lead_inbox.py:4747` | _flush_pending_done_notices re-queues durable items without from_role → adaptive digest window can never shrink for them | Any done notice that took the durable path (Lead was down/busy) is re-fed through `_notify_lead` with the default `from_role='system'`; `_other_roles_still_active(project_ns, 'system')` then counts the reporting role's own (still 'done'/'working') pane as another active role, so the 3 s adaptive set | Pass `from_role=item.get('role', 'system')` (and `note=item.get('note')`). |
| 75 | 0.90 | `lead_wait.py:263` | ref_count is written in five places and never read | #598 docstring says pops happen 'only when ref_count reaches 0', but every decision uses `if not clients:` — `ref_count` is bookkeeping nobody consumes. | Delete the field and the docstring claim. |
| 76 | 0.90 | `lead_wait.py:803` | _wait_echo_by_id grows without bound (one entry per resolved wait_id, never pruned) | Every resolved/expired/interrupted `takkub wait` stores its full result dict under its wait_id; nothing removes entries after `_WAIT_RESOLVED_ECHO_GRACE_S`. A cockpit left running for days with an autonomous Lead polling `takkub wait` in every turn accumulates thousands of dicts. | Prune entries older than `_WAIT_RESOLVED_ECHO_GRACE_S` when inserting, or use a bounded OrderedDict. |
| 77 | 0.60 | `limit_autoresume.py:764` | After a cockpit restart an already-elapsed quota stall is never cleared or announced (docstring claims re-arm) | codex quota-hit recorded at 14:00 with reset 19:00; cockpit restarted at 20:00. No reset QTimer exists in the new process, _maybe_reprobe_quota_stalls skips entries with reset_at <= now, and nothing else calls _clear_provider_quota_stall -> provider-quota.json keeps the stale entry forever (until co | In _maybe_reprobe_quota_stalls, for reset_at <= now call _clear_provider_quota_stall(project, provider, reason='window_elapsed') (once), or re-arm _schedule_provider_quota_reset_notice for every loade |
| 78 | 0.50 | `limit_status.py:505` | save_shared_state uses one fixed tmp filename: concurrent writers collide and the 429 backoff is silently not persisted | LimitStore thread and ProviderUsageStore thread (or prod + dev cockpit sharing one profile dir) both save at once: both write takkub-usage-state.json.tmp; on Windows the second tmp.replace() hits PermissionError while the first still has it open -> swallowed at L508-509 -> that writer's backoff_unti | Unique tmp name (pid + thread id / uuid) and a module-level lock around load+merge+write. |
| 79 | 0.90 | `limit_status.py:740` | class Poller is dead code (no caller in src or tests); main_window.py:159 comment still references it | Poller (L740-833, threading.Timer-based single-profile poller) was superseded by LimitStore; the only mentions are its own docstring (L631) and a stale comment in main_window.py:159. Never instantiated anywhere in the repo. | Delete the class and fix the main_window comment to say LimitStore. |
| 80 | 1.00 | `logs_panel.py:60` | `_EVENT_COLOR` is never referenced (dead) | n/a — module-level dict built from cockpit_theme tokens at import but no reader anywhere in the tree. | Delete it. |
| 81 | 0.80 | `logs_panel.py:147` | LogsPanel polls events.log every second on the main thread even while its dock is hidden (the default) | events.log grows hundreds of times a minute, so nearly every 1 s tick re-reads 256 KiB, splits lines, json-parses 120 rows and calls setPlainText on a widget nobody sees, for the whole session. | Start the timer in showEvent / stop in hideEvent (or gate `_poll` on `self.isVisible()`), and do the initial fill on first show. |
| 82 | 0.90 | `main_window.py:1331` | `_on_project_changed` / hidden `_project_combo` path and `_refresh_rtk_button` stub are dead-by-construction | n/a — the combo is hidden, `_refresh_project_list` blocks its signals while mutating it, nothing else pokes it, so the slot never runs; `_refresh_rtk_button` is `pass` yet called 4x; `DEFAULT_TEAMMATES` is imported only to be assigned to `_`. | Remove `_on_project_changed`, the `_project_combo` wiring, the `_refresh_rtk_button` stub and its call sites, and the `DEFAULT_TEAMMATES` import. |
| 83 | 0.70 | `main_window.py:1450` | Tab open runs `git ls-files` synchronously on the Qt main thread (15 s timeout) via skill-link repair | Opening a project tab (manual + or each boot-restored tab) blocks the UI for the duration of a git subprocess; on a slow disk/AV-scanned repo this is a visible stall per tab. | Run `_ensure_project_skill_links` through `self.cli.run_off_thread` (it only touches the filesystem) and report results to the status bar in the callback. |
| 84 | 0.80 | `maintenance.py:513` | _other_cockpit_events_log's installed-build branch points at <venv>/Lib/runtime/events.log — never exists | On an installed build config.REPO_ROOT is Path(__file__).resolve().parents[2] = <venv>/Lib (config.py:184), so `Path(_REPO_ROOT) / "runtime" / "events.log"` is `<venv>/Lib/runtime/events.log`, which is filtered out by `p.is_file()` (maintenance.py:532, 626). `takkub ma` run from an installed cockpit | Drop the prod→dev branch (return None when not is_dev) or resolve the dev checkout's DATA_HOME the way cli._instance_banner does. |
| 85 | 0.50 | `mcp_fallback.py:84` | mb fallback grant is a lockless read-check-write on a non-atomic file — two shards can both be told 'granted', then one is denied by the hook | Two browser shards whose Playwright MCP failed run `takkub mcp-fallback request` at nearly the same moment (separate processes, cli.py:5037): both _read_lock() see no holder (or a torn half-written JSON → None at :66-68), both _write_lock() and return granted=True; the last writer owns the file, so  | Write via tempfile + os.replace and re-read after write to confirm holder == role before returning granted; or take an OS-level lock (msvcrt.locking / fcntl.flock) around request(). |
| 86 | 0.85 | `obsidian_dedup.py:28` | Dedup index is write-only: lookup/all_records/NoteMetadata.touched are never called, so nothing is ever deduplicated | `vault_mirror.distill_session_facts` only calls `record_write` (line 809); `lookup` (line 28/90), module `all_records` (102) and `obsidian_metadata.NoteMetadata.touched` (89) have no callers in src/ (doctor uses only `count`). The JSONL under core_store_path('obsidian_dedup') grows one record per di | Either consult `lookup(meta.knowledge_id)` before appending in distill_session_facts (and compute knowledge_id from the fact text without the timestamp) or remove the index + touched() as dead. |
| 87 | 0.70 | `orchestrator.py:1027` | Fan-out queue (TAKKUB_QUEUE_FANOUT) is default-off dead code; when enabled it drops worktree_prepared/subagent_fanout/base_ref | `_fanout_queue_enabled()` is false unless the env var is set, so `_should_queue_assign`, `_enqueue_assign`, `_drain_fanout_queue`, `_fanout_queue_path`, `_save_fanout_queue`, `_load_fanout_queue` and the close() drain hook (6380-6381) never execute in production (memory: opt-in features never get us | Either delete the six methods + `_fanout_queue` state + close() hook + `docs/reviews/2026-06-30-queue-gap.md` reference, or make it real: default-on, carry the full assign kwargs (incl. worktree_prepa |
| 88 | 0.55 | `orchestrator.py:2886` | assign() creates PaneState via _ps() before validation; rejected/subagent assigns leave ghost state that steers resolve_pane_role | `takkub assign --role qa --provider bogus` (rejected at 3003) or `--role reviewer --mode subagent` (no pane ever) still executes `self._ps(_exit_key(project, role))`, which get-or-creates `_pane_state["proj::qa"]`. Later `takkub send --to reviewer` / `takkub close --role reviewer` with no live pane  | Read with `self._pane_state.get(key)` for the `subagent_fanout` comparison and only call `_ps()` inside the `subagent_fanout > 1` / clear branches after mode/provider/model validation has passed (move |
| 89 | 0.80 | `orchestrator.py:2946` | decide.scope() runs 2-3 times per assign (cli, orchestrator, routing_planner.classify) → duplicate paid Jev probes and log rows in shadow/on mode | Operator enables `TAKKUB_DECIDE_MODE=shadow` (or on). One `takkub assign`: cli.py:776 asks Jev in the CLI process, orchestrator.py:2946 calls `decide.scope(task)` unconditionally even when a concrete `--scope` was already shipped, and (preset=auto) orchestrator.py:2840 `suggest_team_size` → `routing | In `assign`, call `task_scope.classify` for the audit metric when `requested_scope != "auto"` and only `decide.scope` when it is auto; let `routing_planner.classify` accept a precomputed ScopeDecision |
| 90 | 0.60 | `orchestrator.py:3490` | _dispatch_next_assignment tells Lead "forwarded when pane ready" when the item was actually re-queued | done()/idle-drain pops the queued item and calls `_assign_dispatch(**item)`; if the pane reads busy at that instant, `_assign_dispatch` re-inserts the item at position 0 and returns `(True, "... queued after current task")` (3607-3617). The caller only checks `ok`, so Lead receives "[queued-assignme | Have `_assign_dispatch` return a distinguishable status (e.g. third tuple element or message prefix) for the re-queue branch and word the notice accordingly, or suppress the notice when the popped ite |
| 91 | 0.70 | `orchestrator.py:3752` | Idle provider/cwd-switch respawn releases the governor slot and the replacement pane spawns uncapped | qa (browser class) is idle; Lead re-assigns it with `--provider codex`. `_assign_dispatch` calls `close(..., keep_queue=True)`, which pops+releases the (project, role) ResourceToken (close() 6168-6171), then 2 s later calls `_assign_dispatch` directly — only `assign()` ever calls `governor.request_s | Either keep the token across a recovery close (skip the pop when `recovery_close`) or re-enter through `assign(..., _resource_token=None)` so the replacement requests a slot. |
| 92 | 0.60 | `orchestrator.py:4507` | Worktree pre-trust resolves the provider from the bare qa/critic row instead of settings_role_for (reviewer row) | `takkub assign --role reviewer --mode e2e --isolation worktree` (→ qa). `_assign_dispatch` spawns on `effective_provider_for(settings_role_for("qa"))` = the reviewer row (claude). `_assign_with_worktree` decides pre-trust with `effective_provider_for(role_name)` = the stale qa row in role-models.jso | Resolve exactly as `_assign_dispatch` does: `ps.provider_override or effective_provider_for(team_preset.settings_role_for(base_role, project_ns), project=project_ns)`; better, move the pre-trust into  |
| 93 | 0.80 | `orchestrator.py:5669` | `_reap_role_messages` tells Lead 'ส่งซ้ำให้แล้ว N ข้อความ' using len(pending) even when items were abandoned (empty body / replay cap) and nothing was replayed | A role's pending record hits `MAX_REPLAYS` (5652) or has an empty body (5649) → marked abandoned, no `_send_when_ready`. The notice after the loop still reports `len(pending)` replayed, so Lead believes the message reached the respawned pane and waits for a reply that never comes. | Count actual replays in a local counter and skip the notice when it is 0; mention abandoned count separately. |
| 94 | 0.50 | `orchestrator.py:5723` | `kill_pane_children` sort key calls `c.parents()` after `is_running()` without try/except → psutil.NoSuchProcess escapes if a child exits between the two calls, killing nothing | Lead runs `takkub kill --role devops` on a build tree whose short-lived helper (e.g. a `cmd.exe` shim) exits mid-sort. `sorted(...)` raises NoSuchProcess from `Process.parent()`/`ppid()` on Windows; the exception propagates out of the method (only the per-child loop at 5728 is guarded), cli_server's | Wrap the key in a helper that returns 0 on `psutil.Error`, or sort by a pre-collected `(pid, depth)` computed inside try/except. |
| 95 | 0.50 | `orchestrator.py:5783` | `_child_scan_cache` is keyed by PID, never evicted on close, and Windows reuses PIDs → a new pane can be served the previous process's stale child list; dict grows for the cockpit's lifetime | Pane A (pid 4212) closes with an entry in `_child_scan_cache`. Hours later a new pane gets pid 4212 (Windows PID reuse is aggressive). First non-sync probe hits `hit is not None`, age > TTL → returns `list(hit[1])` (dead psutil.Process objects whose `.name()` is cached on Windows) while a bg refresh | Pop `cache[pid]`/`inflight` in close() and `_on_session_exit`, and key the cache on `(pid, session_generation)` or the session object id. |
| 96 | 0.60 | `orchestrator.py:6774` | `_find_evidence_files` materialises the whole `rglob('*')` tree before applying the node_modules/vendor exclusion → done() walks tens of thousands of entries on the Qt thread | #500/#567 already documents a pane that npm-installed playwright inside the exports scratch dir. `list(directory.rglob("*"))` (6774) lists every file under node_modules first; the `_EVIDENCE_EXCLUDED_DIR_NAMES` check (6780) only skips them afterwards. done() calls this up to 4× (6846, 6857, 6955, 69 | Walk with `os.walk` and prune excluded dir names in place (`dirs[:] = [d for d in dirs if d not in _EVIDENCE_EXCLUDED_DIR_NAMES]`), or cap the walk. |
| 97 | 0.75 | `orchestrator.py:6844` | Evidence scan/dedup use done-time date while the pane writes to the spawn-time `TAKKUB_ARTIFACTS_DIR` → screenshots taken after midnight are invisible to done() | qa pane spawned 23:50 gets `TAKKUB_ARTIFACTS_DIR=…/exports/2026-09-22/<proj>` (pane_env.py:359-365, computed once at spawn). It finishes at 00:10 and done() scans `RUNTIME_DIR/exports/<today=2026-09-23>/<proj>` (6843-6844, 6943-6944) → no evidence found → note gets `⚠ no evidence cited`, `ui_evidenc | Store the artifacts dir actually handed to the pane on PaneState at spawn (or derive from `assign_ts`'s date) and scan that, not `datetime.now()` at done time. |
| 98 | 0.70 | `orchestrator.py:7615` | `_language_nudge_suffix` never nudges when task_id is None (`seen.get(key) == None` is True on first call) | A hand-driven pane (`takkub spawn` then typed instructions) or any PaneState without `task_id` calls `takkub done`/`progress` in English while the team language is Thai. `seen.get(key)` returns None for an unseen key and `task_id` is None → equal → returns "" — the #621 nudge is silently disabled fo | Use a sentinel: `if key in seen and seen[key] == task_id`. |
| 99 | 0.70 | `orchestrator.py:9230` | `_pending_notice_roles` reads only the in-memory `_recent_done` (never snapshotted) → after a cockpit restart, roles whose done report is in the durable `_pending_done_notices` store vanish from `takkub list` | backend reports done while Lead is holding a draft; the notice is parked durably. Cockpit restarts (crash / `takkub restart`). `_recent_done` is initialised empty (1979) and only appended by done()/subagent paths (2649, 8554) — no snapshot/restore path touches it — so the #163 'done (report queued…) | Also iterate `_pending_done_notices[project_ns]` roles (already durable) in `_pending_notice_roles`, or persist `_recent_done` in `write_session_snapshot`. |
| 100 | 0.80 | `orchestrator.py:11472` | Snapshot writes session_uuid but restore_teammates never reads it — restored panes always cold-boot and get the whole task re-pasted | Cockpit restart (npm update / Restart button) with a teammate mid-task. `_collect_pane_snapshot_data` persists `session_uuid` '(#9) … so restore_teammates can … resume the session', but `restore_teammates` (11556-11716) only reads role/cwd/last_task/worktree fields; `_recent_exits` is stamped withou | Either restore `self._ps(key).session_uuid = entry.get("session_uuid")` before spawn (and send `_STUCK_RESUME_NUDGE` instead of the full task when `last_spawn_resumed`), or delete the field from the s |
| 101 | 0.80 | `orchestrator.py:12069` | _venv_incident_notified is never cleared after a successful repair — a second python.exe clobber is repaired silently with no Lead notice | The #341/#696 incident happened twice in one day. First clobber: repair OK, key `<path>::ok` added, Lead told. Hours later a pane clobbers `venv/Scripts/python.exe` again: `find_problems` reports it, `repair` succeeds, but `key in notified` → `continue` at 12070-12071, so Lead never learns that pane | Clear `_venv_incident_notified` (or drop the `::ok` key) in the `if not problems: if was_broken:` healthy-again branch at 12048-12052 so each new clobber episode notifies once. |
| 102 | 0.85 | `orchestrator.py:13565` | getattr(pane, "provider", None) is always None — AgentPane has no such attribute — so provider-specific feedback-prompt gating/skip-key are dead and the generic regex path is always used | `_check_feedback_prompts` (13565), `_check_stale_markers` (12723) and `_check_stuck_panes` (14120, 14133) all read `pane.provider`/`pane._provider`. Neither exists (the only provider attribute is `pane.model.provider_name`, agent_pane_model.py:155; grep of *pane*.py finds no `provider` property). `i | Resolve the provider like the other watchdogs: `effective_provider_for(role, project=project_name)` (falling back to `pane.model.provider_name`), and pass that to `is_at_feedback_prompt` / `feedback_p |
| 103 | 0.45 | `orchestrator.py:15422` | Pane-header × closes by role name in the projects.json active project instead of the pane's own project (project_ns computed then dropped) | Two project tabs open. `_on_pane_close_clicked` derives `project_ns` from the emitting pane via `_project_ns_for_pane` (15417) and uses it for the confirm dialog, then calls `self.close(role_name)` with no project → `close()` → `_resolve_project(None)` → `active_project()` from projects.json. If tha | `self.close(role_name, project=project_ns)`. |
| 104 | 0.60 | `orchestrator_text.py:291` | tiny-scope screenshot bypass counts untracked new style/text files as 0 lines | scope=tiny, pane creates a new 400-line .css/.md file (untracked) and touches nothing else. touched_files = that path (style/text OK) but `git diff --numstat HEAD` never lists untracked files -> filtered_numstat empty -> total_lines 0 <= 20 -> is_tiny True -> the UI screenshot gate is skipped regard | If any touched file is absent from numstat (untracked/new), treat the diff as not tiny, or count its lines via `git diff --numstat --no-index /dev/null <file>`. |
| 105 | 0.60 | `pane_env.py:527` | Profile/home injection helpers swallow every exception with bare `pass`, so a pane can silently spawn against ~/.claude or the OS-wide provider home | inject_user_profile_env (523-528), inject_curated_claude_config_dir (543-547), inject_provider_home_env (651-654, 658-665): any failure (corrupt profiles.json, link creation failing on a locked dir, provider_config_dir_for raising) leaves CLAUDE_CONFIG_DIR / CODEX_HOME / XDG_* unset with no log line | Replace `pass` with _log.exception(...) plus a `provider_capability_fallback`-style _log_event so doctor/events.log show the degradation. |
| 106 | 0.45 | `pane_env.py:744` | _ensure_opencode_no_autoupdate_config rebuilds opencode.json from {} when the existing file is unreadable or unparseable, wiping the seeded OpenCode config | Prod build with an isolated opencode home seeded from the user's real ~/.config/opencode/opencode.json (providers, keybinds, MCP servers). On a later opencode spawn, if read_text raises OSError (file momentarily locked on Windows) or json.loads raises (user added a JSONC comment/trailing comma, whic | On OSError/ValueError skip the write (log and leave the file alone); write via tmp + os.replace. |
| 107 | 0.85 | `pane_guard.py:2313` | Instance-guard 30 s caches are dead-by-construction (one-shot hook process) and evaluate_instance_guard runs twice per Bash call | Every candidate Bash call (any `>`/`2>&1`/`.md` mention — `_CANDIDATE_MUTATE_RE` line 2964) re-scans tempdir, TCP-probes each port file and imports psutil in a fresh `takkub _guard` subprocess; the #658 comment about 'the Qt main thread (1.5 s captured)' describes a caller that no longer exists. cli | Drop the module-level caches and `clear_instance_guard_cache`, and have `classify()` skip `evaluate_instance_guard` when the caller already ran it (pass a flag), or persist the probe result on disk wi |
| 108 | 0.80 | `performance_settings.py:166` | from_dict / validate / PerformanceSettings.to_dict have no production callers since #515 (load() derives from preset()) | grep across src (excluding this module) finds no from_dict/to_dict/validate use for performance settings; load() now returns preset(get_mode()) so the parsing path is test-only. | Delete from_dict/validate/to_dict and their tests, or keep validate only if a future UI re-exposes numeric fields. |
| 109 | 0.90 | `pipeline_config.py:358` | `with_providers` / `provider_disabled_targets` have no production caller (kept only for tests) | Docstring says the consuming dialog (`pipeline_dialog`) was removed 2026-07-10; grep of src finds no caller of either function. | Delete both functions and their tests, or move to the settings page bridge that actually needs them. |
| 110 | 0.85 | `plugin_installer.py:306` | list_marketplaces has no callers | settings_management/repositories/plugins.py uses list_installed and install_by_id only; nothing calls `claude plugin marketplace list --json`. | Delete. |
| 111 | 0.85 | `preview_controller.py:199` | Dead API: set_device / set_approved / check_navigation have no callers; nav_block_counts is always 0 | `takkub preview` only supports open-url/open-file/close/status (orchestrator.py:10419-10437); PreviewHost calls the pure navigation_allowed directly, never controller.check_navigation. So `_nav_block_counts` is never incremented and the `takkub doctor --workspace` row read at orchestrator.py:10334 a | Either route PreviewHost.navigation_allowed through controller.check_navigation (so doctor's count is real) and add device/approve actions to preview_command, or delete the three methods and the count |
| 112 | 0.50 | `project_explorer.py:292` | _repaint_badges resolves every tree item on the Qt main thread (Path.resolve under RESOLVE_LOCK) on every git status refresh | Expand a few thousand rows in the Explorer. Every `statusChanged` (fired after each directory listing and after every editor save via gitRefreshNeeded, debounced 1.5s) recurses the whole tree calling `_apply_badge` → `_safe_resolve(Path(path_str))` = one GetFinalPathNameByHandle syscall per row, on  | Store the pre-computed repo-relative key on each item at creation (paths came from a resolved listing already) and skip resolve() in _apply_badge. |
| 113 | 0.90 | `project_rules.py:46` | `if claude is None` branch is unreachable (find_claude_executable raises) and generate_project_rules() blocking wrapper has no callers | config.find_claude_executable never returns None — it raises RuntimeError at config.py:1329 — so the None check and its message never fire (the RuntimeError text shown to the user is config's, not this one). project_wizard.py:41-43 uses generate_project_rules_proc + collect_result; the blocking `gen | Remove the None branch and the blocking wrapper. |
| 114 | 0.90 | `provider_bootstrap.py:226` | Dead guard in _seed_opencode: dest_data.exists() is always False from the only caller | ensure_provider_home returns False at line 285-286 whenever dest_data exists, so _seed_opencode's `dest_data.exists() and src_data.resolve() == dest_data.resolve()` branch can never be taken; the resolve() calls are wasted and the intent (refuse to seed from itself) is not actually enforced when src | Drop the dest_data.exists() term (compare resolved parents / strings as _seed_codex does) or delete the branch. |
| 115 | 0.60 | `provider_config.py:530` | effective_provider_for reads the quota-reset file uncached (exists+read_text) on every watchdog tick per pane, despite the module note that TTL-cached this exact path for stalls | orchestrator._check_stuck_tool_panes (L14309) and _check_idle_teammates (L12380) call effective_provider_for(role, project) for every working non-claude pane on the 5s tick. Since #572 that path ends in provider_state.is_quota_ready -> load_quota_resets (provider_state.py:142-146: `path.exists()` +  | Make provider_state.load_quota_resets use cached_read.read_cached(quota_path(), json.loads, missing={}) and invalidate in its writer (L164-165 tmp.replace) — or cache is_quota_ready per provider with  |
| 116 | 0.85 | `provider_config.py:912` | lead_capability_gap() has no production caller (docstring claims Settings UI + remote API use it) | Settings uses lead_capability_gap_for_provider (settings_management/pages/roles_page.py:263) and remote/api.py:834 uses lead_missing_capability; lead_capability_gap itself is referenced only by tests/test_provider_config.py:746-768. | Delete lead_capability_gap and its tests, or have the Lead-row badge actually call it. |
| 117 | 0.85 | `provider_models.py:99` | provider_models.all_models() has no production caller | Referenced only by tests/test_provider_models.py:41-78; every runtime consumer uses model_for/set_model/clear_model/path (settings_window.py:515/1579/2152/3090, settings_management/repositories/providers.py:78/186/188). | Delete all_models() and its test assertions (replace with _load() in tests), or leave as documented test seam. |
| 118 | 0.85 | `provider_spec.py:1940` | is_ready_marker_calibrated() is test-only; its own docstring says no spawn-time warning is wired | Only tests/test_auth_failure_detection.py:415-426 call it; orchestrator uses uncalibrated_providers() instead (orchestrator.py:9987). The spawn-time Lead warning the docstring defers ('whoever wires that follow-up should call this') was never wired, so a cursor pane (empty ready_rules) still silentl | Either call it in spawn_engine/lead_inbox to emit a one-shot Lead notice for an uncalibrated provider at spawn, or delete it and keep uncalibrated_providers(). |
| 119 | 0.85 | `provider_update.py:203` | eligible_providers(), _TERMINAL_STATUSES and UpdateOutcome.ok are dead in production | boot_flow.py (the only production consumer) calls provider_update.eligibility_gap/update_provider/STATUS_* directly; eligible_providers() is referenced only by tests/test_provider_update.py:59. _TERMINAL_STATUSES (L120) has no reference anywhere in src/ or tests/. UpdateOutcome.ok (L152) is never re | Delete eligible_providers, _TERMINAL_STATUSES and UpdateOutcome.ok (and the test for eligible_providers), or wire boot_flow to eligible_providers if the docstring's 'Callers MUST have already filtered |
| 120 | 0.60 | `provider_usage.py:992` | gemini cache discovery stat()s outside try: a file replaced between glob and stat turns the whole card into 'unexpected error' for 5 min | Antigravity rewrites its quota cache (delete+create). fetch_gemini_usage globs candidates, then `p.stat()` in the sort key raises FileNotFoundError; only fetch_provider_usage's catch-all (L1420-1422) catches it -> ProviderUsage(error, 'unexpected error') until the next 300 s poll, and the live RPC p | Stat each candidate inside try/except OSError and drop missing ones before sorting. |
| 121 | 0.70 | `provider_usage.py:1492` | 2-second status-bar tick does uncached filesystem work on the Qt main thread (codex home glob/resolve + per-project profile read) | status_header._status_timer (setInterval 2000, status_header.py:651) -> _refresh_active_provider_usage (limit_panel.py:126) -> config_dir_for -> user_profile.profile_for -> path.read_text (user_profile.py:402, NOT cached unlike the #658-cached registry) and _refresh_limit_label -> get_all_account_us | Memoize codex_usage_targets() with a short TTL / stat-validated cache and route profile_for through cached_read like _load_registry. |
| 122 | 0.80 | `qa_gate.py:652` | Full-gate lock is per-DATA_HOME, not machine-wide as documented — dev and prod cockpits can run full gates concurrently | User runs the dev cockpit and the installed prod cockpit at once (two DATA_HOMEs). A full `takkub qa-gate` from a pane of each resolves a different `RUNTIME_DIR/locks/qa-gate-full`, so both acquire slot-0 and run simultaneously — exactly the CPU pile-up #472 introduced the lock to stop; `_active_ful | Put the lock under a per-user OS temp/state dir (e.g. `Path(tempfile.gettempdir()) / "takkub-qa-gate-lock"`) shared by every instance. |
| 123 | 0.60 | `remote/__init__.py:193` | check_ingress_mismatch runs for quick/ngrok modes too, producing a spurious 'hostname mismatch' heads-up when an old named-tunnel config.yml is still on disk | User switched from a named Cloudflare tunnel to Quick tunnel (or ngrok fixed). runtime/tunnel/config.yml from the named setup remains; _start_blocking compares its ingress hostname with the new public_url and sets hostname_mismatch_note, which _remote_start_warning shows in a 'Remote enabled — heads | Gate the check on `self.config.tunnel.type == "cloudflared"` (named) or delete config.yml when the mode changes. |
| 124 | 0.90 | `remote/api.py:853` | Resume response returns transcript messages without the #441 secret redaction that /api/lead/history applies | Phone resumes a Lead session whose transcript contains a value a pane once printed from an env file. read_resume_session_messages calls scanner.read_messages directly (notify.py:1987) and the raw list is returned to the phone and painted immediately (app.js:2476-2489); the same messages fetched via  | Return `_redact_messages(scanner.read_messages(...))` from read_resume_session_messages. |
| 125 | 0.50 | `remote/diagnostics.py:69` | Loopback/public probes use urllib's default opener, which honours system/env proxies, so a proxied Windows box gets a false 'Local loopback probe failed' warning on every Enable/boot | Machine with a system proxy (IE/WinHTTP settings or HTTP_PROXY env). probe_local GETs http://127.0.0.1:<port>/... through the proxy (urllib's `<local>` bypass only covers dot-less hosts), the proxy refuses loopback, probe_http returns (False, ...), RemoteControl sets local_probe_note and the Setting | Build the loopback probe with `urllib.request.build_opener(urllib.request.ProxyHandler({}))`. |
| 126 | 0.85 | `remote/http_server.py:658` | /api/team-preset GET/POST and api.team_preset_status/set are unreachable from the shipped PWA | No client calls them: app.js has no 'team-preset'/'preset' string at all (only tests exercise the route). The bridge branches at http_server.py:306-321 and api.py:696-746 are dead weight in this subsystem. | Either wire a Team card into the PWA (Pulse page) or delete the route/api functions and their tests. |
| 127 | 0.75 | `remote/notify.py:1791` | _read_from_conversation_store_v2 can never return data (no writer for the Lead conversation) and would break the session-exact mirror if it ever did | read_recent_lead_messages first consults ConversationStore for `conv-<project>-lead`. The only writer is core.conversation.facade.on_pane_done, invoked from orchestrator.done() for teammate roles (orchestrator.py:7936-7948, args=(project_ns, from_role)) and the subagent path (:2574-2579); Lead can n | Remove the read-through (and the redact detour) or key it by the pane's session_uuid via bind_provider_session before enabling it. |
| 128 | 0.80 | `report_builder.py:234` | --title containing a backslash crashes the build (re.sub replacement escapes) and is not HTML-escaped | `takkub report build --title "คู่มือ C:\Reports\v2"` -> re.sub treats the f-string replacement as a template -> `re.error: bad escape \R` -> 'build failed: bad escape'. A title with `&`/`<` is also injected raw into <title>. | Use a callable replacement: `re.sub(pat, lambda m: f"<title>{html.escape(title)}</title>", head, count=1)`. |
| 129 | 0.70 | `resource_governor.py:296` | Dead code: BackgroundSampler.stop and ResourceGovernor.overloaded() have no callers; git_changes_service.changes_sync has no src caller | grep across src (excluding the defining modules) finds no `_sampler.stop(`/`.stop()` on the sampler (the daemon thread is never stopped on shutdown), no `.overloaded()` calls (callers read snapshot()['overloaded']), and no `changes_sync(` (workers use _changes_sync_diag). Tests may reference them (t | Either wire BackgroundSampler.stop into orchestrator shutdown (it is the only way to stop the psutil thread) or remove it; drop overloaded() and changes_sync (or keep changes_sync only if a test-only  |
| 130 | 0.55 | `resource_lock.py:140` | Lock held by a dead pane is not reclaimed until the 30-minute TTL — no pid liveness check | A pane takes `takkub lock web-build`, then the watchdog kills it (#655) or it crashes mid-build. try_acquire only reclaims when `expired(now)` (acquired_ts + ttl_s, default 1800 s) or same holder, so the next pane's `takkub lock web-build --wait 600` polls for 10 min and fails, or a different role w | In try_acquire/release/format_lock_line treat a lock whose `pid` is not alive (os.kill(pid, 0) on POSIX / OpenProcess or psutil.pid_exists on Windows) as stale, keeping the stdlib-only constraint with |
| 131 | 0.50 | `role_messages.py:84` | read() maps any OSError to [] and every writer read-modify-writes the whole file → a transient read failure clobbers the entire message log | On Windows an AV/indexer sharing violation makes `open()` raise PermissionError for one tick. `read()` returns `[]`; the very next `append`/`_update`/`abandon_unconfirmed_for_role` writes back only its own record, deleting every earlier message for the project. | Distinguish 'missing' from 'unreadable' (raise or skip the write on OSError other than FileNotFoundError); append with `open(path, 'a')` instead of rewriting. |
| 132 | 0.60 | `settings_accounts.py:397` | Accounts refresh latch drops refreshes requested while a probe is in flight — rows that land can predate an add/remove/login | macOS (keychain `security` probe up to 5s per claude account) or slow disk: open Accounts (refresh #1 running) → immediately '+ เพิ่มบัญชี' → `_accounts_reload_shared_state` → `_accounts_refresh()` returns at 397-398 because `_accounts_refresh_busy` is True → job #1 lands with rows computed before t | Set a `_accounts_refresh_pending` flag when busy and start another job in `_on_accounts_refreshed` if it is set. |
| 133 | 0.70 | `settings_knowledge_design.py:552` | Context Strategy / theme write-through ignore the save() bool — UI shows the new choice while nothing was persisted, no feedback | Knowledge → click 'Deep' while the config file is unwritable (locked/OneDrive) → `core_v2_settings.save_context_strategy` returns False (core_v2_settings.py:196-225 swallows exceptions) → buttons flip to Deep, next open shows the old value. Same shape at settings_window.py:2025-2029: `theme_settings | Check the bool and show the same QMessageBox.warning the machine-mode/team-language handlers already use (2004, 2039). |
| 134 | 0.95 | `settings_management/feature_flags.py:25` | Module docstring says unknown TAKKUB_SETTINGS_UI values fall back to NEW; resolve() returns LEGACY | Doc/code drift only — a reader trusting the module docstring expects `TAKKUB_SETTINGS_UI=compare` to open the new window; it opens legacy. | Fix the docstring to say LEGACY. |
| 135 | 0.85 | `settings_management/feature_flags.py:47` | settings_management package is dead by default (flag LEGACY); window placeholder branch dead by construction | user_actions._open_settings_window (user_actions.py:333-337) is the only production router and opens the new window only when feature_flags.resolve() is NEW; resolve() returns LEGACY for unset/unknown, so the whole package (window, 5 pages, 5 repositories, services, transaction) is unreachable in re | Either flip the default and delete the legacy Providers&Roles view, or remove the package (and the 'Back-compat for external readers (settings_management...)' aliases in custom_roles/pane_tools_policy |
| 136 | 0.90 | `settings_management/pages/providers_page.py:162` | toggle_provider_requested hook is never wired by the shell → new Providers page toggle skips orchestrator.toggle_provider (no Lead broadcast) | TAKKUB_SETTINGS_UI=new: user disables codex on the Providers page and hits Save. _save() calls self.toggle_provider_requested(...) which is still the class default returning None, so it falls back to providers_repo.update → provider_state.set_disabled only. orchestrator.toggle_provider's `[system] c | In user_actions._open_settings_management_window set `win.providers_page.toggle_provider_requested = lambda name, disabled: self.orch.toggle_provider(name, disabled)`. |
| 137 | 0.95 | `settings_management/window.py:124` | 'coming soon' placeholder branch is unreachable — every sidebar entity is in _WIRED | _WIRED == set(_SIDEBAR_ENTITIES), so the placeholder loop at 124-131 adds nothing and the `else: setCurrentIndex(self._placeholder_index[name])` at 173-174 can never run; the '(soon)' label logic at 68 is dead too. | Drop _WIRED, _placeholder_index and both branches. |
| 138 | 0.65 | `settings_window.py:1144` | Status-strip 'Template: X' tooltip points to a control that does not exist — nothing in the UI can change activeTemplate, and the Pipeline page opens on row 0 with no active marker | User reads 'เปลี่ยนได้ที่ Templates / Pipeline Builder', opens Pipeline, finds only Duplicate/Delete/hop editing; the list preselects row 0 (5092), not the active template, and shows no active indicator, so hop edits may go to a template that never runs. | Add a 'Set active' button (writes `activeTemplate` through `_persist_pipeline_payload`), preselect the active template, and mark it in the list. |
| 139 | 0.95 | `settings_window.py:1185` | Foldable-section sidebar code is dead by construction (_FOLDABLE_SECTIONS is empty) | `_FOLDABLE_SECTIONS = frozenset()` (263) means the `QSettings(...)` read at 1185, the fold branch 1191-1219, `_on_nav_section_toggled` 1258-1270, `_nav_section_bodies/_toggles` and the 1302-1306 branch in `_goto_view` can never execute; the QSettings object is still constructed on every Settings ope | Delete the fold machinery and the QSettings read, or leave a one-line TODO; separate deletion audit decides. |
| 140 | 0.80 | `settings_window.py:1525` | Save & Apply rollback snapshot omits the team-preset file — a later write failure reports 'rolled back ทุก store' while the preset change stays on disk | Pick a team-size card + toggle an MCP → Save & Apply → tools-policy write fails (1706/1710 raise OSError) → `_rollback()` restores routing/pipelines/policies/models, but `_team_preset.set_current` (1637) and `note_manual_roles_change` (1670) already rewrote the project's team-preset file, so the pro | Add the team-preset file path (team_preset's per-project raw store) to `snapshot_paths`. |
| 141 | 0.80 | `settings_window.py:3121` | New Role and Add MCP dialogs copy the Settings stylesheet without the #settingsWindow objectName — dialog background stays native/light in dark theme | Dark theme → Roles page → '+ ตำแหน่งเฉพาะโปรเจค' (or Tools → '+ Add MCP server') → dialog chrome/background renders in the OS palette with dark panels inside, unreadable Cancel/labels — exactly the defect the `_RolePermissionsDialog` docstring (settings_knowledge_design.py:162-169) fixed by using `C | Derive these dialogs from `cockpit_theme.CockpitDialog` (or set objectName 'cockpitDialog') instead of copying the stylesheet. |
| 142 | 0.80 | `shared_dev_tools.py:391` | _clear_stale_singleton_locks is a no-op on Windows (Chromium's Windows singleton is a named mutex, not SingletonLock files) — comment and #39 rationale are wrong for the platform it targets | Called on every browser-role spawn (browser_profile_mcp_config_path :517). On Windows Chromium never creates SingletonLock/SingletonCookie/SingletonSocket (POSIX-only, process_singleton_posix.cc); the profile-in-use error on Windows comes from a still-live chrome.exe holding the named mutex, which u | Delete the helper (and the doctor docstring's 'clears singleton-lock files' caveat, doctor.py:1170-1175) or replace with a real check for a live Chromium holding the profile. |
| 143 | 0.85 | `skill_audit.py:152` | audit_new_role_text and audit_existing_role have no callers (docstrings claim the New-Role dialog / Skill Catalog use them) | settings_window.py and settings_management/repositories/skills.py only import `load_all_role_docs`; no code path computes role overlap for a new/selected role, so the '✓ won't overlap' warning described in the docstrings does not exist in the UI. | Wire them into the New-Role dialog / catalog as documented, or delete them. |
| 144 | 0.50 | `skill_scan.py:280` | Legacy skill migration runs `git ls-files` synchronously on the Qt main thread at every project-tab open (incl. boot restore) | main_window._open_project_tab → _ensure_project_skill_links → migrate_legacy_project_skills → _git_tracked_skill_names runs a git subprocess with a 15s timeout on the GUI thread for every tab whose `<root>/.claude/skills` exists. On a repo on a network mount / under AV scanning / with a wedged git,  | Run the migration/link repair on a worker (QRunnable) and only block the spawn on its completion, or cache the tracked-name set per root. |
| 145 | 0.70 | `spawn_engine.py:2016` | _launch_session TOCTOU re-defer drops resume_uuid, so a codex/gemini Lead resumed from the mobile picker silently comes back as a fresh session | remote/api.py:857 calls orch.spawn(LEAD.name, ..., resume_uuid=<picked>) for a Lead on codex/gemini. If the final InSendMessageEx re-sample fails, _launch_session calls _toctou_redefer WITHOUT resume_uuid (the claude branch at 3883-3892 passes it). The retry spawns with resume_uuid=None -> no `resum | Pass resume_uuid=resume_uuid to _toctou_redefer in _launch_session (the parameter already exists and the claude branch uses it). |
| 146 | 0.50 | `spawn_engine.py:3456` | Any exception while writing hook-settings.json silently spawns the claude pane with NO hooks: no Stop/idle signal, no Bash/Edit/Write guard, no rtk, remoteControlAtStartup left to Claude's auto-on default | ensure_hook_settings_file does config.ensure_runtime() + tmp.write_text + tmp.replace on the first spawn after a content change (rtk toggle, concise/rc roster change) — on Windows a sharing violation (AV/indexer holding the freshly written file) or an unwritable runtime dir raises PermissionError/OS | Log the exception with _log_event('hook_settings_write_failed', ...) and _log.exception, and fall back to the last known-good settings path (or an inline minimal settings JSON) instead of no hooks. |
| 147 | 0.85 | `status_header.py:322` | _project_combo is built and hidden; _refresh_project_list/_on_project_changed only maintain it | Combo hidden at :589; main_window.py:1317-1339 keep repopulating it and comment 'nothing routes here anymore'. | Remove the combo, _refresh_project_list and _on_project_changed (verify no other caller of _refresh_project_list). |
| 148 | 0.90 | `status_header.py:569` | _btn_claude_auth is built, styled, wired and hidden but never placed in the bar | Dead widget kept 'for easy future restore'; its click handler _on_add_user_clicked is already reachable from the Accounts menu. | Remove the widget block and the commented tuple entry. |
| 149 | 0.95 | `status_header.py:1706` | _refresh_rtk_button is a no-op `pass` still called from 5 sites | Called at status_header.py:634 and main_window.py:951/1339/1537/1684; does nothing. | Delete the method and its 5 call sites. |
| 150 | 0.80 | `task_delivery.py:206` | DeliveryManager._deliveries is never pruned; snapshot() dumps the full history into the session snapshot every write | Every assign creates a TaskDelivery (payload included) that stays in `_deliveries` forever; `snapshot()` (orchestrator.py:10204) serialises all of them each time the session snapshot is written, and `cancelled_deliveries`/`expire_stale` scan the whole dict every tick. | Drop terminal deliveries older than a TTL (e.g. in expire_stale) or cap per pane. |
| 151 | 0.85 | `task_delivery.py:249` | DeliveryState.WAITING_RESOURCE / create(waiting_resource=True) has no caller | No call site passes `waiting_resource=True` (grep), so the state is never entered; it only pads `_IN_FLIGHT_STATES`/`_UNDELIVERED_STATES` and the state enum. | Remove the parameter and state, or wire the resource governor's queued path to create deliveries with it. |
| 152 | 0.90 | `task_delivery.py:453` | supersede_for_session can never cancel anything (every delivery is kind='task') → _RESEND_ELIGIBLE_STATES branch and send()'s [delivery-cancelled] notice are dead | The only `DeliveryManager.create` call is lead_inbox.py:1456 (default kind='task'). In `_cancel_for_session(only_delivered=True)` every kind=='task' delivery either `continue`s or goes to `kept`, so `cancelled` is always empty. orchestrator.py:5097-5131 (`[delivery-cancelled]` notice, `_last_deliver | Either delete `_RESEND_ELIGIBLE_STATES` + the 459-466 branch and the dead send() notice, or make send() create kind='send' deliveries if that path is meant to be live. |
| 153 | 0.60 | `task_dock.py:865` | With no tab open, TaskDockWidget renders a card for any project whose ledger changes | User closes the last tab (dock cleared via set_project(None)); a background ledger write (pending done-notice reaper, remote `task reconcile`) emits ledgerChanged for project X and the Task List dock shows X's card even though X has no tab. | Return early in `refresh_project` when `self._project is None`. |
| 154 | 0.85 | `task_ledger.py:733` | `flush_index_writes` has no caller — INDEX.md background writes are never drained at shutdown | Assign/done then quit the cockpit immediately: the daemon `ledger-index-writer` thread may be killed before `_atomic_write` runs, so INDEX.md lags `.ledger-state.json` until the next assign/done. Function exists for exactly this but nothing (app shutdown, `end_session`) calls it. | Call `task_ledger.flush_index_writes()` from app shutdown / `end_session` (next to `flush_events_log`), or delete it and make the state save regen INDEX synchronously on exit. |
| 155 | 0.85 | `task_scope.py:676` | Fix-loop ceiling API (`MAX_FIX_LOOP_ATTEMPTS`, `FixLoopDecision`, `check_fix_loop_ceiling`, `FixLoopTracker`) and its routing_planner re-exports have no production caller | The #585 round-3 fix-loop ceiling is never consulted by the orchestrator's verify-fail handoff path; the 2-round ceiling the docs promise is enforced only by prose in Lead docs, not by code. | Either wire `FixLoopTracker` into `_build_verify_fail_handoff` (count attempts per failure signature and stop at MAX_FIX_LOOP_ATTEMPTS) or delete the block, the re-exports and tests/test_fix_loop_ceil |
| 156 | 0.90 | `terminal_widget.py:577` | TerminalWidget.clear() and request_buffer_text() have no production callers | Dead by construction: `clear()` (L577-581) is never called (only `clear_view`/`reset` are); `request_buffer_text()` (L673-687) is referenced only by tools/spike_pane_discard_ram.py:125. | Either wire `request_buffer_text` into `_export_buffer` (see the export finding) and delete `clear()`, or delete both. |
| 157 | 0.80 | `terminal_widget.py:671` | Wheel/Ctrl+0 zoom not stored in _font_px, so discard/reattach reverts the font (and idle flag is never re-asserted) | User Ctrl+wheel-zooms a teammate pane to 14 pt, switches project tab for >25 s (pane discarded), switches back -> page reloads -> `_on_page_ready` re-applies `self._font_px` (still the pre-zoom value from `_restore_font_size`/None) -> font snaps back. Same reload resets JS `idle` to false but `Agent | In `_on_font_zoomed` set `self._font_px = int(pt * 1.333)`; in `_on_page_ready` also re-send idle/lock/newline state (or have AgentPane reset `_last_idle = None` when the terminal reports a fresh page |
| 158 | 0.85 | `token_meter.py:169` | session_project_dir_for_cwd has no callers | Only referenced in docstrings (spawn_engine.py:481, remote/notify.py:757); every real caller uses session_project_dirs_for_cwd / find_session_by_uuid. | Delete and update the two docstrings. |
| 159 | 0.60 | `update_panel.py:698` | npm self-update click path compares versions by string equality (unlike the chip) → offers an 'update' when metadata is unreadable ('?') or the local build is newer | Installed build where `importlib.metadata.version('agent-takkub')` fails (current='?') or local wheel is ahead of the registry (fresh publish lag / prerelease): chip correctly stays green (`compare_versions(cur, latest) < 0` at 780) but clicking it shows 'agent-takkub vX is available (you have ?)' a | Use `compare_versions(current, latest) >= 0` in `_checked` and treat `current == '?'` as 'cannot determine' (warning, no install). |
| 160 | 0.95 | `update_worker.py:100` | Dead: ClaudeUpdateCheckWorker + the Claude-CLI native update dialog chain in update_panel.py + 10 helpers in claude_update.py have no caller | Nothing instantiates `ClaudeUpdateCheckWorker` (only its own docstring mentions it), so `update_panel._on_claude_update_check_done` (429), `_show_claude_update_dialog` (459), `_count_live_claude_panes` (554), `_confirm_and_apply_claude_update` (570), `status_header._claude_update_busy` (479) and, in | Delete the worker, the four update_panel methods, `_claude_update_busy`, `_run_update_check`, and the ten claude_update helpers (keep the live seven); a separate audit should drop the tests that pin t |
| 161 | 0.50 | `usage_ledger.py:184` | _UsageLock spins forever (no sleep, no deadline) when os.open keeps raising PermissionError and no lock file exists | Ledger dir (RUNTIME_DIR/usage or TAKKUB_USAGE_LEDGER_DIR) exists but is write-denied (protected folder / read-only mount / ACL). os.open raises PermissionError -> stat() raises FileNotFoundError -> `continue` bypasses both the deadline check and the sleep -> 100% CPU busy loop. Reached on the Qt mai | On stat failure fall through to the deadline check and sleep (or break fail-open immediately). |
| 162 | 0.90 | `usage_ledger.py:435` | Quota sample dedup only compares the last row, so multi-window providers append duplicate samples every poll that reuses cached data | ProviderUsageStore polls claude every 300 s; fetch_usage_shared often returns the SAME cached UsageData (same fetched_at) -- always during a 429 backoff. _record_quota_ledger writes windows in order five_hour, seven_day, seven_day_sonnet; on the next poll the last row is seven_day_sonnet so five_hou | Dedup per window (scan tail rows until a row with the same window is found, or keep a per-(provider,account,window) last-ts map in _record_quota_ledger). |
| 163 | 0.85 | `user_profile.py:479` | default_provider() is never read in production; set_default_provider() writes a 'default_provider' field nothing consumes | user_actions.py:1059 calls set_default_provider(project, provider) on the 'Lead ใช้ provider' switch, but the only readers of default_provider() are tests (test_user_actions_provider_switch.py:42-79, test_user_profile.py:260-264). The actual routing effect comes from provider_config.save_role_overri | Remove default_provider/set_default_provider and the call at user_actions.py:1059 (routing.json already carries the Lead provider), or make status_header/provider_config read it if it is meant to be a |
| 164 | 0.40 | `user_profile.py:1083` | bootstrap_default_profile rmtree's an existing prod claude-config that has no marker and no .credentials.json — wipes sessions/todos for API-key users or pre-marker installs | Installed cockpit whose DATA_HOME/claude-config was created by the older synchronous full-tree copy (no .bootstrap-complete marker) or by a user who authenticates via ANTHROPIC_API_KEY / has logged out (no .credentials.json). On the next boot dest.exists() is True, neither guard file exists, and on  | Treat a dest that contains any real user data (projects/*.jsonl, todos/, settings.json) as a real profile: write the marker and return False instead of deleting; only rmtree when dest is empty or cont |
| 165 | 0.40 | `vault_mirror.py:235` | In-process first-line dedup drops the ENTIRE done note (sessions, knowledge base, vault, Lead brief) for any later note with the same first line | A role whose done notes start with a fixed template line ('## สรุป', 'Done:', 'qa-gate FAIL:' …). After the first report, every later `takkub done` from that (project, role) hashes the same first line → orchestrator._save_decision_note (line 8920) returns None → no runtime/sessions file, no knowledg | Hash the whole scrubbed note (or first ~200 chars) and add a time window (e.g. 60s) instead of a process-lifetime set. |
| 166 | 0.85 | `venv_integrity.py:61` | PROBLEM_DESCRIPTIONS has no reader in src | The dict is defined and mentioned in a docstring (line 71) but no caller maps a problem key to its description; doctor.py:1715-1722 and orchestrator.py:12044-12060 use the raw key / repair() message instead. | Remove it, or use it in doctor's finding text for the venv check. |
| 167 | 0.80 | `verify.py:60` | `takkub verify` Python branch runs bare `python` (no venv/`python3` resolution) and the command is no longer referenced by any role or Lead doc | `detect_stack` emits `["python", "-m", "pytest", …]`/`ruff` with a bare `python`; on macOS (no `python` shim) or Windows with the Store stub, `run_checks` (l.429) raises FileNotFoundError → every Python check reports FAIL. The only remaining references to `takkub verify` are docs/ARCHITECTURE.md:105 | Either retire `takkub verify` (delete cmd_verify, the python branch of detect_stack, run_checks/format_summary, and tests/test_verify*.py, keeping node_checks/pm_* used by qa_gate) or make it delegate |
| 168 | 0.60 | `worktree_manager.py:383` | _stuck_git_op assumes `.git` is a directory — silently disabled when the primary checkout is itself a linked worktree | Lead's project root is a `git worktree add`-created checkout (e.g. the manually named prodtest-taktempmain base checkout from memory). There `.git` is a file, so `Path(git_root)/'.git'/'MERGE_HEAD'` is never a file → merge_isolated/clean_isolated never refuse on a stuck merge (#575 protection off) a | Resolve the gitdir via `self._run(["-C", git_root, "rev-parse", "--git-path", name])` (or `--absolute-git-dir`) instead of hardcoding `<root>/.git/<marker>`. |
| 169 | 0.85 | `worktree_manager.py:1730` | Dead code: safe_remove / force_remove / _unlink_links / is_dirty / is_dirty_at / uncommitted_count / uncommitted_count_at / _expand_dir_entry / WorktreeConfig.is_empty have no callers outside tests — and safe_remove carries a latent branch-loss bug | No src caller for any of: safe_remove (:1730), force_remove (:2284), _unlink_links (:1180, only used by those two), is_dirty (:1229) / is_dirty_at (:1233), uncommitted_count (:1377) / uncommitted_count_at (:1372), _expand_dir_entry (:2444, superseded by _expand_dir_entries), WorktreeConfig.is_empty  | Delete safe_remove/force_remove/_unlink_links/is_dirty/is_dirty_at/uncommitted_count/uncommitted_count_at/_expand_dir_entry/is_empty and the tests that only cover them; if a caller is ever added, comp |
