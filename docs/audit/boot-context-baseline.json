{
  "_comment": "issue #516 point 5 — CI ceiling baseline for tests/test_boot_context_ceiling.py. Generated by `PYTHONPATH=src py -3 -m agent_takkub.cli doctor --boot-context --project agent-takkub` (see docs/audit/2026-09-07-boot-context.md for method + why these are lower-bound estimates, NOT real billed tokens). Regenerate by re-running that command and updating the numbers below when a role file diet intentionally shrinks a role — the test fails loudly on any INCREASE past the allowance, by design. Re-baselined 2026-09-07 after #516 F1 (native_project_memory removed from every non-Lead role's report — now per-role, None until that role is respawned under its new CLAUDE_CODE_PROJECT_DIR_NAME) and F2 (chars/4 replaced by token_estimate's Thai-weighted estimator). #516b addendum (same day): the gated field is now `repo_controlled_est_tokens`/`repo_controlled_chars` — the sum EXCLUDING boot_context.DYNAMIC_STATE_CATEGORIES (learned_notes, project_memory_pointer, native_project_memory*), which are per-machine runtime state a pane's own `takkub done` writes during the day, not repo content; gating on them produced a false-positive red ceiling on a dev machine that had simply accumulated notes since the baseline was captured. `native_project_memory` below is kept for reference only (Lead's own file, measured by test_native_project_memory_measurable_not_gated as a smoke check, never gated). Re-baselined AGAIN 2026-09-07 (#516 CI-blocker fix): the previous numbers here (frontend 4725/backend 4684/mobile 4797/...) were captured by running `doctor --boot-context` from INSIDE a git worktree checkout, where `default_cwd_for_role` could not resolve the registered project's real cwd — `repo_claude_md` (the project root CLAUDE.md Claude auto-loads, real repo content) silently measured 0 for every role, undercounting the true repo-controlled cost by ~724 tok. Also moved `graft_caveats` into `DYNAMIC_STATE_CATEGORIES` (see boot_context.py's comment on it): whether a role gets it depends on the per-machine `SETTINGS_HOME/pane-tools.json` operator override, not repo content, and gating on it made `doctor` (real machine settings) permanently disagree with `test_boot_context_ceiling.py` (always runs isolated, override-free) by a fixed +631 tok regardless of which machine or checkout ran it. These numbers are now captured from the actual registered main checkout, with the fix applied.",
  "generated": "2026-09-07 (re-baselined after #516 CI-blocker fix — graft_caveats now dynamic-state, baseline captured from the registered main checkout not a worktree)",
  "project": "agent-takkub",
  "allowance_ratio": 1.15,
  "roles": {
    "frontend": { "repo_controlled_chars": 13130, "repo_controlled_est_tokens": 5449 },
    "backend": { "repo_controlled_chars": 12966, "repo_controlled_est_tokens": 5408 },
    "mobile": { "repo_controlled_chars": 13417, "repo_controlled_est_tokens": 5521 },
    "devops": { "repo_controlled_chars": 14575, "repo_controlled_est_tokens": 6182 },
    "qa": { "repo_controlled_chars": 15177, "repo_controlled_est_tokens": 6332 },
    "reviewer": { "repo_controlled_chars": 13463, "repo_controlled_est_tokens": 5575 },
    "critic": { "repo_controlled_chars": 14001, "repo_controlled_est_tokens": 6045 }
  },
  "native_project_memory": { "chars": 14743, "est_tokens": 5937 }
}
