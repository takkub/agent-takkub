# Batch release 2.0.x — review รอบ 3 (ยืนยันการแก้จากรอบ 2), 2026-09-07

**ผล: ✅ 16 / ❌ 0 — HIGH ที่ค้างจากรอบ 2 หายครบทั้ง 2 ข้อ → ไม่มีตัวบล็อก push จากส่วน B**
พบของใหม่ระหว่างยืนยัน: **MED 1 · LOW 2** (ไม่ใช่ตัวบล็อก — ให้ Lead ตัดสินว่าจะเก็บรอบนี้หรือยกไป #504)

ขอบเขต: เฉพาะข้อในส่วน B ของ `docs/audit/2026-09-07-batch-2.0.x-review-round2.md` + MED ของ gemini รอบ 2 — **ไม่ได้รีวิวชุดใหม่ทั้งหมด**
HEAD ที่ตรวจ: `b114137` (ได้จาก `rtk proxy git log` — `git log` ธรรมดาในเชลล์นี้คืนผลค้างที่ `0c75199`)
ไม่ได้แก้ไฟล์โค้ด/เทสใดๆ · probe ทุกตัวรัน isolate ผ่าน `AGENT_TAKKUB_HOME`/`TAKKUB_USAGE_LEDGER_DIR` ชี้ tmp — ไม่แตะ DATA_HOME จริง
เทสรันด้วย `PYTHONPATH=src rtk proxy python -m pytest` เท่านั้น · `snyk` ยังไม่มีบน PATH เครื่องนี้ → ข้ามขั้น dependency scan (เหมือนรอบ 2)

หลักฐาน: `runtime/exports/2026-09-07/agent-takkub/reviewer/` — `probe_rollover.py`, `probe_routing_source.py` (ของเดิม รันซ้ำบน HEAD นี้), `round3_targeted.log`, `round3_batch18_run3.log` · probe ใหม่ 3 ตัวรันจาก scratchpad (`probe_r3_scan.py`, `probe_r3_unlink.py`, `probe_r3_seenids.py`)

---

## ตารางผล

| ข้อ | ผล | หลักฐาน (รันเอง / file:line) |
|---|---|---|
| **B-H1** rollup คูณ 2 ตอนขึ้นเดือน | ✅ หาย | `probe_rollover.py` บน HEAD: Sep #1 = Sep #2 = **Oct** = `turns:2, input:300, output:30` (รอบ 2 ได้ Oct = `turns:4 / 600 / 60`) · กลไก: `usage_ledger.py:963-976` merge เฉพาะ row ที่ `request_id not in prior_seen` แทนการบวกทั้งไฟล์ · snapshot ต่อเดือนที่ `:958-960`, `:974-976` |
| **B-H2(a)** `role_models._save` ไม่ mirror routing | ✅ หาย | `probe_routing_source.py`: หลัง `role_models.set_provider('frontend','gemini')` → v2 `routing.json` global = `frontend: gemini` (รอบ 2 ค้างที่ `codex`) · `provider_config.dual_write_routing_mirror():219-245` ถูกเรียกจาก `role_models._save()` |
| **B-H2(b)** ด่าน #502 ตาบอด | ✅ หาย | probe รอบ 3 (`probe_r3_scan.py`): เขียน `role-models.json` ตรงๆ ข้าม dual-write → `scan_v1_only_writes()` = `role-models/missing_mirror` + `role-providers:global/stale_mirror (lag 3.1s)` · เขียนผ่าน `set_provider` (dual-write) → `[]` ⇒ จับเฉพาะกรณีไม่ mirror จริง ไม่ false-positive · เทสในรีโป `tests/test_core_storage_v1_only_write.py:188,208,245` |
| **B-H2(c)** ladder เขียน `{}` ทับ v2 | ✅ หาย | `probe_routing_source.py`: `_routing_sources()` = `global → role-models.json` · `_routing_payload()['global']` = `frontend: gemini` (รอบ 2 = `{}`) |
| **B-H2** self-heal ตอน boot | ✅ มีเทสจริง ไม่ vacuous | `tests/test_auto_migrate_boot.py:367-412` `test_stale_routing_global_self_heals_on_next_boot_no_rollback` — ตั้ง mirror ค้างแบบ prod แล้ว assert ว่า `apply_pending()` re-apply `role-agent`, ไม่ rollback, ไม่แตะ V1 และเทียบ `routing.json['global']` กับ `role-models.json` จริง 8 role |
| **B-M4** unlink ล้ม → บวกซ้ำ | ✅ หาย | `probe_r3_unlink.py` (patch `Path.unlink` ให้ throw ทุกครั้ง): Oct rollup #1/#2/#3 = `turns:2, input:300, output:30` เท่ากันหมด ทั้งที่ raw file ยังอยู่ทุกรอบ + `LOG WARNING rollup_daily: could not prune raw month file …` ครบ 3 ครั้ง · `usage_ledger.py:987-992` |
| **B-M1** `takkub team set` จาก lead | ✅ หาย | `cli_server.py:1376-1405` reject เมื่อ `from_role_norm == "lead"` และ preset ปลายทางมี `lead_may_implement` · ฝั่ง client ส่ง `from` จริง (`cli.py:1481`) ⇒ gate ไม่ถูกข้าม · `tests/test_cli_server_role_gate.py:366-424` 6 เทส (solo-lead/pair reject · full ผ่าน · caller ที่ไม่ใช่ lead ผ่าน · clear-override ผ่าน · preset มั่วตกไป error เดิม) |
| **B-M2** TestAccountsView flake | ✅ หาย (ดู R3-N1) | batch 18 ไฟล์เดิม รัน **3 รอบ ผ่านหมด** (`round3_batch18_run3.log` — 906 dot / 0 F / exit 0) · `settings_accounts.py:44-61` `aboutToQuit → waitForDone(3000)` · `tests/test_settings_window.py:1490,1546,1589` drain pool ก่อนแตะ registry |
| **B-M3** legacy transcript dir | ✅ หาย | `token_meter.py:153-164` — `base_role` ประกอบชื่อ `…-<role>` เมื่อไม่มี `project_dir_name` และ append `takkub-project-<ns>` เปล่าเป็น candidate สุดท้ายเสมอ · ส่งต่อจริงที่ `:232`, `:274` · `spawn_engine.py:460` เพิ่ม `project_ns=` ให้ `_resume_uuid_matches_cwd` ⇒ resume ของ teammate เห็น dir ก่อน F1 |
| **B-L1** stub `_on_install_rtk_clicked` | ✅ หาย | grep `src/agent_takkub/**/*.py` → 0 hit ทั้ง `_on_install_rtk_clicked` และ `_btn_install_rtk` |
| **B-L2** dual_write ไม่มี caller | ✅ หาย | `core/storage/dual_write.py:157, 179, 342` — ทั้งสามฟังก์ชันเขียนกำกับว่า "no production caller left … kept until #504" |
| **B-L3** docstring อ้าง `settings_core_v2` | ✅ หาย | grep `settings_core_v2` ใน `src/**/*.py` เหลือ hit เดียวที่ `settings_window.py:171` (ประโยคที่อธิบายว่าไฟล์ถูกลบ = ถูกต้อง) |
| **B-L4** read API มี side effect | ⚠️ **ยังอยู่ตามที่ตกลง — acceptable ชั่วคราว** | `exec_mode.current():65` ยังเรียก `_archive_legacy_file()` · `provider_config.load_providers():189` ยังเรียก `_migrate_legacy_global_overrides_once()` · **แต่** ต้นทุนต่อ call หลัง migrate รอบแรกเหลือ `Path.exists()` ตัวเดียว (`provider_config.py:137-139` return ทันทีเมื่อไฟล์ไม่มี) และมี re-entry guard `_migrating_legacy_overrides` (`:134-140`) ปิดช่องวนไม่รู้จบที่ `dual_write_routing_mirror` เปิดขึ้นใหม่ ⇒ ยอมรับได้รอบนี้ ยกไป #504 ตามที่ระบุ (ดู R3-N3) |
| **B-L5** `input - cached` ติดลบ | ✅ หาย | `usage_ledger.py:685-686` `input_val = max(0, raw_input - cached)` |
| **B-L6** archive ชนชื่อแล้วไม่ย้าย | ✅ หาย | `config.archive_settings_file:145-168` — dest ชน → เติม suffix `%Y%m%d%H%M%S` แล้ว `source.replace(dest)` เสมอ (ครอบ gemini MED-1 ด้วย) |
| **B-L7** naive timestamp → local | ✅ หาย | `remote/notify.py:118-123` และ `core/conversation/ingest/codex_adapter.py:65-70` — `if parsed.tzinfo is None: parsed.replace(tzinfo=UTC)` ทั้งคู่ |
| **gemini MED** `role_models` provider-only entry | ✅ หาย | `role_models._set_field:215` เปลี่ยนเป็น `if merged.get("provider") or merged.get("model") or merged.get("effort")` — entry ที่มีแค่ provider ไม่ถูก pop ทิ้งอีก (`139c35f`) |
| **gemini MED** resume fallback | ✅ หาย | รวมอยู่ใน B-M3 (`token_meter.py:162-164` + `spawn_engine.py:460`) |
| **gemini MED** `archive_settings_file` ย้ายเสมอ | ✅ หาย | รวมอยู่ใน B-L6 |

**สรุป: ✅ 16 · ❌ 0 · ⚠️ 1 (B-L4 = defer ตามที่ตกลงไว้)** — ไม่มี HIGH ค้าง

---

## ของใหม่ที่เจอระหว่างยืนยัน (ไม่ใช่ตัวบล็อก push)

| Sev | File:line | ปัญหา | หลักฐาน | แก้ยังไง |
|---|---|---|---|---|
| **R3-N1** MED | `user_profile.py:326-329` (`remove_profile`) · `:308-311` (`add_profile`) · `settings_accounts.py:322-328` | **กดลบ/เพิ่มบัญชีแล้วอาจเงียบหาย ทั้งที่ UI ไม่บอกอะไร** — `_atomic_write` ล้ม (`OSError`) ถูก `except OSError: pass` กลืน ไม่มี return code ไม่มี log ไม่มี dialog · คอมเมนต์ของเทสที่แก้ B-M2 (`tests/test_settings_window.py:1580-1589`) ระบุ root cause นี้เอง ("`os.replace` can raise, and `remove_profile` swallows that `OSError` silently") แต่**แก้เฉพาะฝั่งเทส** (drain pool ก่อนเขียน) ฝั่ง production ยังไม่ได้แตะ — และ concurrency ที่ทำให้ race นี้เป็นไปได้จริงบน Windows คือ `_AccountsRefreshWorker` ที่เพิ่ง introduce ในแบตช์นี้เอง (M11/M4 รอบ 1) | อ่านโค้ด: `remove_profile` ไม่ return อะไรและไม่ raise · `_render_accounts_rows` refresh หลังลบจึงเอาบัญชีเดิมกลับมาโชว์โดยไม่มีข้อความบอกผู้ใช้ · เทสเลี่ยง race ด้วย `QThreadPool.globalInstance().waitForDone(5_000)` แทนการปิดต้นเหตุ | ให้ `add_profile`/`remove_profile` คืนผลสำเร็จ (หรือ raise) แล้ว `settings_accounts` โชว์ error box · หรือปิด race ที่ต้นทาง: รอ/ยกเลิก `_AccountsRefreshWorker` ก่อนเขียน registry เหมือนที่เทสทำ |
| **R3-N2** LOW | `usage_ledger.py:250-251, 320-333, 948-985` (`_seen_ids.json`) | **`_seen_ids.json` ไม่เคยถูกตัด — โตไม่จำกัด** · `_RETENTION_MONTHS = 0` แปลว่าทุกเดือนที่ผ่านไปถูก prune แล้ว request id ทั้งเดือนถูกดองไว้ถาวร · ไฟล์นี้ถูก `json.loads` **ทั้งก้อน** ทุก `rollup_daily` (docstring เองบอกว่า "cheap enough to call on every `takkub usage` read") และทุก `record_turn` ที่ cache ยังไม่มีคีย์ (`_existing_request_ids:351`) | `probe_r3_seenids.py`: 24 เดือน × 3,000 turn/เดือน (บัญชีเดียว) → `_seen_ids.json` **3.31 MB** · `rollup_daily` = **45.4 ms** เทียบกับ **16.5 ms** เมื่อ seen_ids ว่าง (+29 ms ต่อการอ่าน usage หนึ่งครั้ง และโตเชิงเส้นต่อไปเรื่อยๆ) · ไม่ stall Qt main thread (`usage_history` อยู่ใน `_OFF_THREAD_ACTIONS`) จึงเป็น LOW | ตัด entry ที่เก่ากว่าขอบเขตที่ "เดือนกลับมา" ยังเป็นไปได้จริง (เช่น 6-12 เดือน) ตอน `_save_seen_ids` — เคสที่ snapshot นี้ป้องกัน (session ข้ามเดือน / cursor หาย / source rotate) ล้วนเป็นเดือนล่าสุด ไม่ใช่ปีที่แล้ว |
| **R3-N3** LOW | `provider_config.py:121-126` | docstring ของ `_migrate_legacy_global_overrides_once` ยังบอกว่าไฟล์ถูก "freshly recreated empty by the existence-check below it" — หลัง #515 `load_providers()` ไม่สร้าง `role-providers.json` ขึ้นมาใหม่แล้ว (`:210-217` อ่านจาก `role_models.all_models()` ล้วน) ประโยคนี้จึงบรรยายพฤติกรรมที่ไม่มีอยู่จริง | อ่าน `load_providers` ทั้งฟังก์ชัน — ไม่เหลือ write path ไปยัง `role-providers.json` | ตัดวรรค "(or freshly recreated empty …)" ออก |

---

## เทสที่รันจริงรอบนี้

`PYTHONPATH=src rtk proxy python -m pytest -q` เท่านั้น (rtk ธรรมดาสรุปผลเพี้ยน exit 0 — บทเรียนเดิม)

- **targeted 13 ไฟล์** (`usage_ledger · settings_usage · config · role_models · token_meter · resume_session_picker · cli_server_role_gate · auto_migrate_boot · core_storage_v1_only_write · core_migration_v1_steps · provider_config · core_conversation_ingest · remote_notify`) → **exit 0, 0 failed** (`round3_targeted.log`, 696 dot)
- **batch 18 ไฟล์เดิมของรอบ 2** (แทน `test_settings_core_v2.py` ที่ถูกลบใน #515 ด้วย `test_role_models.py`) → **รัน 3 รอบ ผ่านทั้งหมด, exit 0, 0 failed** (`round3_batch18_run3.log`, 906 dot) ⇒ B-M2 ไม่ reproduce อีก

---

## สรุปให้ Lead

**ส่วน B ของรอบ 2 เคลียร์แล้วทั้งหมด — ไม่มี HIGH ค้าง จากมุมรีวิวเวอร์ push ได้**

ค้างจาก **รอบ 1** ที่ยังไม่มีใครแตะและอยู่นอกขอบเขตงานรอบนี้: **M8 — account pool ยังบังคับ spawn ที่ `core/accounts/facade.py:55` แต่ไม่มี UI ให้แก้** ตัดสินใจได้ 2 ทางเหมือนเดิม (โชว์ read-only ในหน้า Accounts หรือถอน pool path ออกไปพร้อม #504)

ของใหม่ 3 ข้อไม่บล็อก release: **R3-N1** เป็นข้อเดียวที่ผู้ใช้เห็นผลตรงๆ (กดลบบัญชีแล้วเงียบ) แนะนำเก็บรอบนี้ถ้ามีเวลา · **R3-N2 / R3-N3** ยกไป #504 ได้
