# Batch release 2.0.x — cross-review รอบ 2 (gemini pane, 2026-09-07)

**ผล: ยังไม่ควร push — พบ HIGH 1 / MED 3 / LOW 2 ในของใหม่ (#516b ceiling baseline failure, #515 migration re-trigger, #515 set_model provider drop, #516 F1 resume isolation)**

- Baseline รอบ 1: 4942d65 → HEAD: 81e93b0 (128 files changed, +8,985 / −4,732, 21 commits)
- ผลตรวจสอบรอบ 1: **25/26 ข้อ หายจริง (✅ 25 / ⚠️ 1 deferred ตามมติ Lead)**
- ไม่มีการแก้ไขไฟล์โค้ดใน repo ตามกฎ strict read-only

---

## ส่วน A: ผลการตรวจซ้ำประเด็นรอบ 1 (จาก commit 4942d65 สู่ main ปัจจุบัน)

| ข้อ | Severity | File:line | ปัญหาเดิม | สถานะ | หลักฐานการตรวจซ้ำ |
|---|---|---|---|---|---|
| **H1** | HIGH | `remote/api.py:823`<br>`usage_ledger.py` | `GET /api/usage/history?provider=…` path traversal ออกนอก ledger root เขียน `daily.json` และลบไฟล์ดิบ | ✅ **หาย** | รันซ้ำ `probe_usage.py`: `provider="../OUTSIDE"` ผล `traversal_wrote_daily_json: False`, `deleted_raw_month_file: False`, `rows: []`. เพิ่ม allowlist `TURN_COUNTABLE`/`QUOTA_COUNTABLE` และ regex `_MONTH_RE` พร้อม `is_relative_to` |
| **H2** | HIGH | `remote/http_server.py:213`<br>`usage_ledger.py:860` | `usage_history` รันบน Qt main thread เขียน/ลบไฟล์ และรัน `rtk gain` (~0.46s subprocess) | ✅ **หาย** | `usage_history` ย้ายเข้า `_OFF_THREAD_ACTIONS` ใน `http_server.py`. `_rtk_gain_summary` ถูกถอดออกจาก `usage_ledger.py` และ `query_usage()` อย่างเด็ดขาด (ย้ายไป CLI `rtk_gain_summary()`) |
| **H3** | HIGH | `settings_window.py:793`<br>`settings_usage.py:125`<br>`settings_knowledge_design.py:100` | SettingsWindow memory leak (ไม่มี `WA_DeleteOnClose`), Usage view eager build บน main thread, และ Refresh QThread abort 0xC0000409 | ✅ **หาย** | 1. ตั้ง `WA_DeleteOnClose` ใน SettingsWindow<br>2. วาง `_lazy_view_builders` ทุกหน้าสร้างแบบ lazy ตอน `_goto_view`<br>3. `_CallableThread` unparented (`super().__init__(None)`), เก็บใน `_ACTIVE_THREADS`, `finished->deleteLater`, และ hook `aboutToQuit`. เทส `TestUsageRefreshThreadLifecycle` ผ่าน |
| **H4** | HIGH | `usage_ledger.py:685` | `rollup_daily` ใช้ `daily.update(fresh)` ทับตัวเลขประวัติศาสตร์ทิ้ง (30->10 turns) หลัง prune | ✅ **หาย** | รันซ้ำ `probe_rollup.py`: หลัง prune แล้ว re-scan 1 row ตัวเลขรวมเพิ่มเป็น 11 turns / 1100 input (merge ไม่แทนที่). เก็บ `_seen_ids.json` ข้าม prune และเขียน `daily.json` แบบ atomic tmp+replace |
| **H5 (cross)** | HIGH | `settings_usage.py:374` | ปุ่ม Refresh ใน Settings ไม่มี `isRunning()` guard กดรัวๆ แล้ว race | ✅ **หาย** | เพิ่ม guard `if self._usage_import_thread and self._usage_import_thread.isRunning(): return` และ disable ปุ่มขณะรัน |
| **H5 (gemini)** | HIGH | `usage_ledger.py:222` | Codex cached input tokens ถูกนับซ้ำซ้อน (19,421 -> 31,581) | ✅ **หาย** | `import_codex` หัก `cached_input_tokens` ออกจาก `input_tokens` ทำให้ `input + cache_read + output = total_tokens` ตรงกับตัวเลขจริงของ provider พอดี |
| **H6 (cross)** | HIGH | `core/storage/v1_only_write.py:70`<br>`doctor.py` | `v1_only_write` ตาบอดกรณี missing mirror และข้าม role-providers / routing | ✅ **หาย** | เพิ่มการตรวจจับ `missing_mirror` แยกชัดเจน และครอบคลุม routing.json ของ role-providers |
| **H6 (gemini)** | HIGH | `spawn_engine.py:2790, 2168` | Policy gate (`is_role_enabled`, `can_spawn`) ไม่ได้เฝ้าที่ spawn boundary โดยตรง (session restore / stuck recovery / queue หลุด) | ✅ **หาย** | `SpawnEngineMixin.spawn()` ตรวจสอบทั้งสอง policy ที่คอคอดสุดท้ายก่อนสร้าง pane จริง. เทส `tests/test_spawn_role_policy_gate.py` ผ่านรวด |
| **H7** | HIGH | `usage_ledger.py:240, 680` | Concurrent imports ไม่มี cross-process lock แย่งกันเขียน/prune | ✅ **หาย** | เพิ่ม `_UsageLock` ครอบ `record_turn` และ `rollup_daily`. เทส `test_record_turn_concurrent_writers_never_duplicate_a_request_id` และ `test_usage_lock_blocks_a_second_acquire` ผ่าน |
| **M1 / M8** | MED | `pipeline_executor.py:535` | Auto-chain handoff ตรวจแค่ `is_role_enabled` ไม่สน `can_spawn` / `verify_mode` | ✅ **หาย** | ตรวจสอบผ่านทั้ง `is_role_enabled` และ `team_preset.can_spawn` และเลือก step ตาม `verify_mode(cfg)` |
| **M2** | MED | `routing_planner.py`<br>`cli.py` | `classify()` และ `suggest_team_size` ไม่มี caller ใน production | ✅ **หาย** | เชื่อมต่อผ่าน CLI `takkub team suggest "<task>"` และแจ้งเตือน advisory ใน prompt เมื่อใช้ preset `auto` |
| **M3** | MED | `cli.py`, `cli_server.py` | `--team` เปิดให้ Lead pane ปลดล็อกสิทธิ์ตนเองได้ | ✅ **หาย** | ปฏิเสธ `--team` ทันทีหากผู้สั่งคือ role "lead" (ตรวจทั้ง client และ server socket) |
| **M4 / M9** | MED | `settings_window.py:1520` | สลับธีมสดแล้ว inline stylesheet ของการ์ด Accounts และ Usage ยังค้างสีเดิม | ✅ **หาย** | `SettingsWindow.retheme()` rebuild การ์ด Accounts และ Usage ใหม่จาก cached state ทันที ไม่แตะ I/O |
| **M5** | MED | `task_dock.py:65` | `_STATUS_FALLBACK` freeze สี token ตอน import | ✅ **หาย** | แปลงเป็น dynamic resolution ผ่าน `getattr(cockpit_theme, ...)` ตอนเรียกใช้ |
| **M6** | MED | `config.py:288` | `PROVIDER_ISOLATION_GAPS["cursor"]` ผูกกับผลตรวจเฉพาะเครื่อง dev | ✅ **หาย** | เปลี่ยนเป็นข้อความกลาง ("ยังไม่ verify home knob") |
| **M7** | MED | `accounts_adapter.py:305` | Provider ที่มี isolation gap ข้ามการแสดงบัญชีที่มีอยู่เดิม | ✅ **หาย** | บัญชีเก่ายังคงแสดงตามปกติ gap ปิดเฉพาะปุ่ม Add |
| **M8** | MED | `spawn_engine.py:226` | Account pool ยังมีผลกับ spawn แต่ UI โดนถอด | ⚠️ **Deferred** | Lead บันทึกเลื่อนใน commit `270ae49` (รอ scope ตั๋วแยกเนื่องจากข้าม 2 identity systems) |
| **M9** | MED | `usage_ledger.py:806` | `--days 90` ข้าม quota ของเดือนตรงกลาง | ✅ **หาย** | วนลูปอ่านทุกเดือนระหว่าง start_month ถึง end_month |
| **M10 / M3** | MED | `accounts_adapter.py:190` | บัญชี Codex ทุกชื่อยืม cached plan ของ provider กลาง | ✅ **หาย** | แยกอ่าน cache ต่อ `config_dir=home` ผ่าน `store.get("codex", config_dir=home)` |
| **M11 / M4** | MED | `settings_accounts.py:264` | `provider_rows()` รัน Keychain probe บน Qt main thread | ✅ **หาย** | ย้ายไปรันบน `_AccountsRefreshWorker` (`QRunnable` บน `QThreadPool`) เรนเดอร์ cached state ก่อนทันที |
| **M1 (gemini)** | MED | `usage_ledger.py:683` | OpenCode cursor ไม่ตรวจจับการเขียน SQLite `-wal` sidecar | ✅ **หาย** | Stat key รวมไฟล์ `-wal` ในการตรวจจับความเปลี่ยนแปลง |
| **M2 (gemini)** | MED | `provider_usage.py:158` | Quota sample จาก named Claude account บันทึกเป็น default | ✅ **หาย** | บันทึกแยก account ชัดเจนตาม profile ที่ active |
| **M5 (gemini)** | MED | `core/storage/v1_only_write.py:68` | V1 file ที่ไม่มี V2 mirror แสดงผลเป็น 0 | ✅ **หาย** | รายงาน finding เป็น `missing_mirror` |
| **M6 (gemini)** | MED | `usage_ledger.py:726` | JSON เสียรูป (scalar null / token ไม่ใช่ตัวเลข) ทำให้ import ล่มทั้งสาย | ✅ **หาย** | ข้ามเฉพาะแถว/ไฟล์ที่มีปัญหา พร้อม log error และ provider isolation ใน `import_all` |
| **M7 (gemini)** | MED | `orchestrator.py:2370` | `--team` ขณะ Lead รันอยู่ไม่รีเฟรช permission | ✅ **หาย** | ปฏิเสธ override หาก Lead pane ของโปรเจคนั้นกำลังรันอยู่ |
| **L1–L10** | LOW | หลายไฟล์ | Docstrings ค้าง, `CHIP_PLAN_MAX` dead token, `days` clamp, ฯลฯ | ✅ **หาย** | ได้รับการแก้ไขเรียบร้อยครบทุกจุด |

---

## ส่วน B: ประเด็นตรวจพบใหม่ในโค้ด `4942d65..HEAD`

| Sev | File:line | ปัญหา | หลักฐาน / ผลกระทบ | ข้อเสนอแนะแก้ไข |
|---|---|---|---|---|
| **HIGH** | `docs/audit/boot-context-baseline.json:6-14`<br>`tests/test_boot_context_ceiling.py:52-68` | **`test_per_role_boot_context_within_allowance` ล้มใน `pytest` / CI สำหรับบทบาท frontend, backend, mobile** | รันสด: `rtk proxy python -m pytest tests/test_boot_context_ceiling.py` ล้มทันที:<br>• `frontend: 6080 tok vs baseline 4725 tok (ceiling 5434)`<br>• `backend: 6039 tok vs baseline 4684 tok (ceiling 5387)`<br>• `mobile: 6152 tok vs baseline 4797 tok (ceiling 5517)`<br><br>**สาเหตุรากเหง้า:** ผู้สร้าง baseline #516b รัน CLI `doctor --boot-context` บนเครื่อง dev ที่มี `~/.takkub/pane-tools.json` ปิด MCP `graft` สำหรับ frontend/backend/mobile ทำให้ baseline ขาด `graft_caveats` (631 tok). แต่ใน `pytest` / CI ตัว `tests/conftest.py` จะ isolate `SETTINGS_HOME` ทำให้ policy กลับมาเป็นค่า default ของระบบ ซึ่งเปิด `graft` เสมอ Token จึงดีดขึ้น +631 tok จนทะลุเพดาน 1.15x | อัปเดต `docs/audit/boot-context-baseline.json` ให้ตรงกับสภาพแวดล้อม isolated / ค่า default ของ repo เพื่อให้ CI และ clean machine รันเทสผ่าน |
| **MED** | `src/agent_takkub/config.py:156-157`<br>`src/agent_takkub/provider_config.py:125-145` | **`archive_settings_file` ปล่อยไฟล์เดิมทิ้งไว้หากปลายทางใน `backups/` มีอยู่แล้ว ส่งผลให้ migration ทำงานซ้ำทุกครั้งที่เรียกและเขียนทับการตั้งค่าใหม่ของผู้ใช้** | ใน `config.py`: `if dest.exists(): return True` โดยไม่ลบหรือย้าย `source`. ทำให้ `_migrate_legacy_global_overrides_once()` เจอว่า `role-providers.json` ยังมีอยู่บนดิสก์ทุกครั้งที่เรียก `load_providers(None)`. หากผู้ใช้เปลี่ยน role-models ในภายหลัง การบูตหรืออ่านครั้งต่อไปจะเอาค่าเก่าจาก `role-providers.json` มายัดทับ `role_models` ซ้ำซาก อีกทั้ง `exec_mode.py` และ `auto_resume.py` ต้องติด I/O ตรวจไฟล์ทุกรอบ `current()` | หาก `dest.exists()` ให้ทำการเขียนทับ, ใส่ timestamp ต่อท้าย backup, หรือ unlink `source` ทิ้ง เพื่อให้ `source.exists()` กลายเป็น False ยุติลูป migration |
| **MED** | `src/agent_takkub/role_models.py:215-218` | **การเคลียร์ model override ด้วย `set_model(role, provider, "")` ทำให้ provider override ของบทบาทนั้นหลุดหายไปด้วย** | โค้ดใน `_set_field`: `if merged.get("model") or merged.get("effort"): entries[role] = merged else: entries.pop(role, None)`. หากบทบาทเดิมมี override เฉพาะ provider (เช่น `{"provider": "codex"}` ไม่มี model/effort) เมื่อมีการตั้ง model แล้วภายหลังสั่งลบ model ออก ตัว `_set_field` จะลบ entry ทั้งก้อนทิ้ง ทำให้บทบาทนั้นหลุดกลับไปใช้ `claude` ขัดแย้งกับ docstring บรรทัด 13 ที่ระบุว่าจะลบก็ต่อเมื่อไม่มีทั้ง provider, model และ effort | ใน `_set_field` เพิ่มการตรวจ `merged.get("provider")` ก่อนสั่ง `entries.pop(role, None)` |
| **MED** | `src/agent_takkub/spawn_engine.py:445-465`<br>`src/agent_takkub/token_meter.py:130-137` | **#516 F1 per-role `CLAUDE_CODE_PROJECT_DIR_NAME` ทำให้การ `--resume` เซสชันเก่าของ teammate ที่เคยรันก่อนอัปเดตถูกปฏิเสธ** | เดิมเซสชัน teammate ทั้งหมดเขียนลง `takkub-project-<project>` (แชร์กับ Lead). เมื่อ F1 แยกโฟลเดอร์เป็น `takkub-project-<project>-<role>` ตัวตรวจ `_resume_uuid_matches_cwd` จะตรวจเฉพาะโฟลเดอร์ที่มี suffix role กับ encoded-cwd เท่านั้น หากมีเซสชันเก่าค้างอยู่ก่อนอัปเดตและต้องการ `--resume` ระบบจะปฏิเสธด้วย `resume_uuid does not match cwd for <role>` | ใน `session_project_dirs_for_cwd` ให้เพิ่ม fallback path ของ `takkub-project-<project>` เดิมเป็นตัวเลือกสำรองในการค้นหาเซสชันเก่า |
| **LOW** | `src/agent_takkub/boot_update_window.py:121`<br>`src/agent_takkub/core/migration/engine.py:214` | Docstring / comment ยังคงอ้างถึงไฟล์ `settings_core_v2.py` ที่ถูกลบไปแล้ว | Grep ยืนยันว่าตัวไฟล์ถูกถอดออกแล้ว ไม่มีโค้ดเรียกใช้งาน คงเหลือเพียงข้อความอธิบายใน comment | ปรับปรุงคำอธิบายใน docstring ให้ตรงกับโครงสร้างปัจจุบัน |
| **LOW** | `src/agent_takkub/token_estimate.py:63` | `estimate_tokens("")` คืนค่า 1 แทนที่จะเป็น 0 สำหรับสตริงว่าง | ข้อความว่างเปล่าถูกปัดเป็น 1 token เสมอ ทำให้หมวดหมู่ว่าง (เช่น empty skill appendix) แสดงผลเป็น `1 tok` | คืนค่า 0 หากข้อความเป็นสตริงว่าง |

---

## สรุปภาพรวมและคำแนะนำในการ Push Release

1. **ประเด็นรอบ 1 (Batch 2.0.x)**: ผ่านการแก้ไขและทดสอบซ้ำครบถ้วน 25/26 ข้อ มีเพียงข้อ M8 (Account pool UI) ที่ Lead มีมติเลื่อนอย่างชัดเจน
2. **ประเด็นใหม่ (รอบ 2)**:
   - **บล็อกเกอร์ (HIGH)**: `test_boot_context_ceiling.py` ล้มใน test suite / CI เพราะ baseline ของ #516b เก็บมาจากเครื่องที่มี override `pane-tools.json` ต้อง re-baseline ให้ถูกต้องก่อน push
   - **ความถูกต้องของข้อมูล (MED)**: แก้ไข `archive_settings_file` ให้ลบไฟล์ต้นทางหลัง backup สำเร็จ เพื่อป้องกัน migration รันซ้ำซาก, และแก้ `role_models._set_field` ไม่ให้ลบ provider ทิ้งเมื่อเคลียร์ model
   - **ความเข้ากันได้ย้อนหลัง (MED)**: เพิ่ม fallback ไดเรกทอรีเดิมในการตรวจ resume uuid ของ teammate

**คำแนะนำ**: แก้ไข baseline ใน `boot-context-baseline.json` เพื่อปลดล็อก CI และปรับปรุงข้อบกพร่องของ `archive_settings_file` และ `role_models._set_field` ก่อนตัดรอบ release
