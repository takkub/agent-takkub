# Review batch เช้า — ก่อนออก 2.1.9

## Verdict

**ยัง ship ไม่ได้: BLOCKER 0 / HIGH 4 / MEDIUM 4 / LOW 0**

ตรวจ `d0fd4bef..b320d6e965d1f85d33464543194c2b177275c197` บน main (35 ไฟล์)
โดยอ่าน `git diff d0fd4bef..HEAD -- <file>` แยกไฟล์ และอ่านบริเวณ caller ที่เกี่ยวข้อง
อ่าน issue #607 #608 #609 #610 #611 #612 #613 #615 #620 #621 ผ่าน `gh issue view` ครบ
ไม่แก้ production code ไม่ commit/push และไม่รัน qa-gate หรือ full suite ของโปรเจค
เพิ่มเฉพาะ `tests/test_batch_219_trust_boundaries.py` ตามคำสั่ง scope=deep
`CHANGELOG.md` มีงานค้างตั้งแต่เริ่ม review และไม่ได้แตะ

**ผลทดสอบ:** targeted เดิม 680 + ข้ามฟีเจอร์ 394 = **1,074 ผ่าน**;
regression ใหม่ **13 failed** ซึ่ง assert พฤติกรรมที่ควรปลอดภัยและเปิดช่องโหว่ด้านล่าง
ไม่ได้ใช้ xfail หรือเปลี่ยน assertion ให้ยอมรับ bug

หลักฐานทั้งหมดอยู่ที่
`runtime/exports/2026-09-15/agent-takkub/batch-2.1.9-review/`
เรียกโฟลเดอร์นี้ว่า **ART** ในรายงาน

## HIGH

### H1 — worktree ของตัวเองยังลบ stash ของทั้ง repository ได้ (#609)

**ตำแหน่ง:** `src/agent_takkub/pane_guard.py:1749` และ `:1444`

`if not in_worktree` ยกเว้น mutating stash ทุกชนิด แต่ `refs/stash` ใช้ร่วมกันระหว่าง
linked worktrees จึงไม่ใช่ state ส่วนตัวของ pane แม้ตรวจ ownership ของ cwd ถูกต้องแล้วก็ตาม
`git stash clear`/`drop` จาก worktree ของ frontend สามารถลบ stash ที่ backend หรือ Lead
เก็บจากอีก checkout ได้

**พิสูจน์จริง:** clone แยกด้วย `git clone --shared --no-checkout` (refs แยกจาก repo งาน),
สร้าง stash จากการแก้ `pyproject.toml` ใน clone แล้วเพิ่ม linked worktree ของ frontend
เรียก classifier ก่อนรัน `git stash clear` จาก worktree นั้น ได้ `allowed=True`;
ก่อนรัน clone เห็น `stash@{0}` หลังรัน `git stash list` ว่างทันที
ไม่ได้เรียก git commit และไม่ได้ลบ stash ของ repository งาน

**ผลกระทบ:** recovery point ของงานที่ยังไม่ commit หายจาก stash list ของทุก pane;
object อาจกู้ด้วยวิธีระดับ Git object ได้ชั่วคราว แต่ไม่ใช่การป้องกัน data-loss

**แนวแก้:** ห้ามการลบ/แก้ shared stash refs ใน worktree carve-out ด้วย
อย่าใช้การมี worktree เป็นเหตุผลอนุญาต stash ทุก subcommand
ข้อบกพร่องนี้เป็นส่วนที่ #609 ยังปิดไม่ครบ ไม่ใช่พฤติกรรมที่ baseline เคยป้องกันไว้

หลักฐาน: `ART/stash_probe.py`, `ART/stash-cross-worktree.json`
เทส: `test_own_worktree_cannot_delete_repository_shared_stashes` (clear/drop)

### H2 — shell wrapper, quoted path และ worktree target หลุด destructive-Git guard (#609)

**ตำแหน่ง:** `src/agent_takkub/pane_guard.py:1285`, `:1444`, `:1749`
และ `_CMD_START` ที่ `:408`

คำสั่งที่รันได้จริงแต่ classifier อนุญาต:

| รูปแบบ | ช่องโหว่ |
|---|---|
| `git -C "C:/shared project" restore .` | `_GIT_SUBCMD_GAP` ใช้ `\S+` จึงไม่อ่าน quoted path ที่มีช่องว่าง |
| `cmd /c git restore .` | ไม่ unwrap shell |
| `pwsh -c "git restore ."` | ไม่ unwrap shell |
| `git.exe restore .` | regex ไม่รับ executable suffix |
| `git -c alias.discard=restore discard .` | ไม่ resolve alias / global config argument |
| cwd เป็น worktree แต่ `git -C C:/shared restore .` | `_in_worktree` OR cwd กับ target จึงไม่ยกเลิกสิทธิ์เมื่อ target ออกนอก worktree |

ยิ่งกว่านั้น cwd `.../worktrees/proj/backend-123` ของ frontend ก็ผ่าน:
ไม่มีการตรวจ role/assignment ownership และไม่มีการยืนยันว่า path เป็น worktree จริง

**พิสูจน์จริง:** ใน Git repo ทดลอง stage baseline ของ `work.txt` แล้วเขียนงานใหม่ลง
working file; รูปแบบทั้งหกใน `ART/real_probes.py` ได้ guard allow, Git exit 0 และ
เนื้อหางานใหม่ถูกทับกลับ baseline จริง การทดสอบ pure classifier แยกอีกชุดยืนยัน
`-C` แบบไม่มีช่องว่างจาก worktree ไป shared tree จึงไม่ใช่เพียงผลของ quoted-path bug

**แนวแก้:** parse argv/quoted argument และ shell wrapper ที่รองรับให้ได้ก่อนตรวจ
ตรวจ effective target ของ Git แต่ละ invocation กับ worktree ที่ assignment เป็นเจ้าของ
ส่วน alias ที่ resolve ไม่ได้ต้องไม่ถือเป็น read-only โดยปริยาย
helper บางตัวมีมาก่อน batch; finding นี้คือช่องที่กฎ #609 ใหม่ยังไม่คุ้มครอง

หลักฐาน: `ART/real-probes.json`; regression 7 เคสแรกของไฟล์เทสใหม่

### H3 — log ที่มี assertion fail จริงปน worker error ถูกจัดเป็น infra และล้าง FAIL ได้ (#607)

**ตำแหน่ง:** `src/agent_takkub/qa_gate.py:875`, `:896`, `:1837`, `:1853`
และการเขียน log ทับที่ `:1074`

signature ต้องการแค่ `[vitest-pool]` หรือข้อความ timeout พร้อมข้อความ `N passed`
ไม่ได้ตรวจว่ามี `N failed`/assertion failure อยู่ด้วย การรันหลาย workspace ยิ่งมี
summary ของ package ที่ผ่านปนกับ package ที่ fail ได้ตามปกติ

**พิสูจน์:** regression ป้อน `FAIL authorization.test.ts`, `1 failed | 41 passed`,
worker timeout ในผลเดียว ได้ `_node_worker_timeout_flake=True`
ทดลองเพิ่มด้วย **Vitest จริง**: มีหนึ่ง test ผ่านและหนึ่ง assertion fail พร้อม inject
ข้อความ worker-timeout ผ่าน stderr; `_run_step` คืน fail แต่ classifier อนุญาต retry
เมื่อเปลี่ยน environment ให้ assertion ผ่านใน retry, `_run_step` คืน pass และเขียนทับ
`test.log` เดิมจนหลักฐาน `1 failed` หาย (`ART/mixed-retry.json`)

การทดลองนี้จำลองข้อความ worker error และเงื่อนไขที่ผ่านเมื่อ retry อย่างชัดเจน;
ไม่ได้อ้างว่าทำให้ worker timeout จริงหรือพิสูจน์ race ของโปรเจค production
ส่วน caller ที่แทน `step = retry_step` เป็นการไล่ production code และมี existing test
ยืนยันการแทนค่าอยู่แล้ว ไม่ได้เรียก qa-gate จริงเพื่อพิสูจน์

**สถานการณ์ที่พัง:** concurrency regression ทำให้ assertion แดงในรอบแรก พร้อม worker
error ในอีก workspace; serial retry ผ่าน → gate กลายเป็นเขียวทั้งที่ regression ยังอยู่
ถ้า assertion ยัง fail ใน serial จะยังแดง ถูกต้อง; ปัญหาอยู่ที่ชนิดที่ serial กลบได้

**แนวแก้:** mixed assertion failure ต้องคง FAIL/ห้ามจัดว่า infra-only;
เก็บ log ของ attempt แรกและ retry แยกกัน และอย่าลบ failure เดิมออกจากรายงาน

หลักฐาน: `ART/runner_probes.py`, `ART/mixed-first.log`, `ART/mixed-retry.json`
เทส: `test_real_assertion_failure_with_worker_error_is_not_infra_only`

### H4 — flag ที่ฉีดบน Windows ทำให้ Vitest 4 รันไม่ได้ตั้งแต่ parse CLI (#607)

**ตำแหน่ง:** `src/agent_takkub/verify.py:247`,
`src/agent_takkub/qa_gate.py:913` และ `:1639`

direct script `vitest run` ได้ `--poolOptions.forks.maxForks=2` เมื่อ limit concurrency;
retry ใช้ option ตระกูลเดียวกัน แต่ **Vitest 4 ไม่รับ `--poolOptions`**
บน Windows gate เปิด limit ทุกครั้ง จึงทำให้โปรเจคที่เทสผ่านอยู่กลายเป็นรันเทสไม่ได้
โดยไม่ต้องเกิด worker timeout ก่อน

**A/B จริง:** Node 24.13.0 + Vitest 4.0.0 + Vite 7.1.7, ไฟล์ `smoke.test.js` หนึ่งเทส
ใช้ argv จาก `verify.node_checks()` ทั้งสองฝั่ง:

- `limit_concurrency=False`: exit 0, 1 test passed
- `limit_concurrency=True`: exit 1, `CACError: Unknown option --poolOptions`, ยังไม่เริ่มเทส

**แนวแก้:** เลือก flags ที่ runner version รองรับ เช่นตรวจและใช้ maxWorkers ที่เหมาะสม
ทดสอบกับ binary จริงของเวอร์ชันที่รองรับ ไม่ใช่เพียง assert รายการ argv ใน fake runner
ชุดเดิม fixture ระบุ Vitest 2 และไม่ได้ launch Vitest จึงตรวจไม่พบ

หลักฐาน: `ART/vitest-real.json`, `ART/node-probe/vitest-limit-False.txt`,
`ART/node-probe/vitest-limit-True.txt`, `ART/node-probe/package-lock.json`
ตอน setup ครั้งแรก dependency Vite ที่ resolve มาเกิด `getBuiltins` error;
pin Vite 7.1.7 แล้วรัน A/B ใหม่ทั้งคู่ ผลด้านบนเป็นคู่หลังแก้ fixture แล้ว

## MEDIUM

### M1 — `.spec.` suffix ทำให้ source auth เลี่ยง Lead deep-category ได้ (#611)

**ตำแหน่ง:** `src/agent_takkub/pane_guard.py:921`, `:1043`–`:1047`

`src/auth/verify.spec.ts` แก้ `return verifySignature(value);` เป็น `return true;`
ได้ allow แม้เป็นการข้าม signature verification: suffix ทำให้ตัด path signal ทั้งหมด
และ content regex ไม่จับ camelCase `verifySignature` หรือ `return true`
ไฟล์ `.spec.ts` สามารถถูก import โดย production entrypoint ได้ ชื่อไฟล์ไม่ได้รับรองว่า
เป็น test-only module การเปลี่ยนแบบเดียวกันที่ `src/security/check.ts` ถูก deny ตามคาด

ผลนี้ยืนยันด้วยการเรียก guard จริง แต่ไม่ได้อ้างว่ามีไฟล์ production ชื่อนี้ใน repo
เป็น counterexample ต่อ trust boundary ที่ Lead ระบุให้ตรวจ จัด MEDIUM เพราะเป็น
ช่องของนโยบาย delegation ไม่ใช่ช่องโจมตีระบบ auth ที่รันอยู่จากภายนอกโดยตรง

**แนวแก้:** จำกัด exemption ตามขอบเขต test ที่เชื่อถือได้ และให้ auth/security source
ยังต้อง delegate เมื่อแก้ control flow; อย่าใช้ suffix เป็นสิทธิ์เลี่ยงหมวด sensitive

เทส: `test_sensitive_production_module_cannot_opt_out_by_spec_suffix`
controls ที่ยัง deny: fixture `tests/test_auth_token.py` ที่ใส่ `API_KEY` literal,
`src/security/`, manifest/lockfile/migration แม้อยู่ใต้ tests (`ART/controls.json`)
literal ใน fixture เป็นข้อมูลจำลอง ไม่ใช่ credential จริง

### M2 — dedupe ข้าม task/shard และเก็บภาพที่ไม่ได้อ้าง ทำให้ valid done ถูกปฏิเสธ (#610)

**ตำแหน่ง:** `src/agent_takkub/orchestrator.py:6061`, `:6069`, `:6100`, `:6108`, `:6122`

history key คือ `(project, base_role)` ไม่มี task_id; shard ก็ถูกตัดทิ้ง
history แทนที่ด้วย batch ล่าสุด แต่ **ไม่ reset เมื่อเปลี่ยน task**
หน้าเดิมที่ต้องการยืนยันว่าไม่เปลี่ยน ถ่ายใหม่แล้ว bytes เท่ากันแต่ชื่อใหม่จึง reject
ทั้งที่ mtime อยู่ในช่วง assignment ใหม่ทั้งหมด

นอกจากนี้ scan ไม่ได้จำกัดเฉพาะไฟล์ที่ note อ้าง และ fallback สแกนทั้ง project exports
code reviewer จึงถูก reject จากภาพซ้ำของ QA ที่ไม่ได้อ้างในรายงานเลย
กฎรันกับ failed/blocked reports ด้วย จึงขวางการรายงานปัญหาและ queue forwarding ได้

**พิสูจน์จริงกับ filesystem/hash:** task A ผ่าน; render fixture PNG อีกครั้งสำหรับ task B
หลัง assign ใหม่ ได้ reject เทียบชื่อ task A; `qa#2` โดน history เดียวกัน
reviewer ที่ note ไม่มีรูป ถูกภาพ QA สองชื่อใน shared screenshots บล็อกเช่นกัน
ภาพเป็น deterministic PNG fixture ไม่ใช่ browser screenshot ของโปรเจคจริง

**แนวแก้:** scope evidence/history ตาม task และ shard; gate เฉพาะภาพที่อ้างหรือเป็นของ
assignment นั้นจริง ภาพเหมือนกันอย่างเดียวควรเป็น warning ให้ Lead ตัดสินตาม issue เดิม
ไม่ใช่ข้อสรุปว่าไม่ได้ถ่ายใหม่ `--force` และ `[no-ui]` bypass ได้ แต่ไม่แก้ false reject

หลักฐาน: `ART/real-probes.json`; regression สองเคสท้ายของไฟล์เทสใหม่

### M3 — language directive ยังไม่ถึงทุกเส้นทางที่อ้างว่ารองรับ (#621)

**ตำแหน่ง:** `src/agent_takkub/spawn_engine.py:2455`,
`src/agent_takkub/codex_agents_md.py:413`, `src/agent_takkub/orchestrator.py:2286`

- non-Claude specialist ใช้ `ensure_agents_md(extra=directive)` ทางเดียว;
  ถ้าโปรเจคมี user-owned AGENTS.md helper คืน `False, user-owned` และ caller ไม่ส่ง
  directive ทางอื่น พิสูจน์ด้วย helper จริง: หลัง spawn-context preparation ไม่มี directive
- native subagent capsule ใน `_register_subagent` ไม่เติม directive เลย และไม่ได้เข้า
  `spawn_engine` ที่แก้ใน batch นี้ การส่ง capsule อย่างเดียวให้ child จึงไม่รับรองภาษา
  ไม่ควรพึ่งว่า native provider ทุกตัวส่งต่อ parent system context เหมือนกัน

**แนวแก้:** มีช่อง context ต่อ session ที่ไม่ต้องทับ AGENTS.md ของผู้ใช้ และเติม directive
ลง task capsule โดยตรง ตรวจ prompt artifact ทุก branch

Claude specialist และ Lead มีจุดฉีดจริง; generic provider กับ managed AGENTS.md
ทดลอง generate แล้วมี directive จริง (`ART/controls.json`)
shell branch return ก่อนส่วนนี้ เป็น raw shell จึงไม่มี model prompt ให้ฉีด;
ผู้เปิด provider เองจาก shell ไม่ได้การรับประกันนี้
ไม่ได้เปิด provider CLI จริงใน review นี้ ข้อสรุป subagent เป็น static trace

หลักฐาน user-owned: `ART/real-probes.json`, `ART/context/AGENTS.md`

### M4 — #609 แก้ role docs แล้ว แต่ AGENTS.md ที่ส่งให้ non-Claude ยังอนุญาต bare stash

**ตำแหน่ง:** `src/agent_takkub/spawn_engine.py:2455`,
`src/agent_takkub/codex_agents_md.py:219` (ไม่เปลี่ยนใน batch), `docs/roles/common.md:20`

generic provider branch ไม่อ่าน version-control prose จาก `.claude/agents/<role>.md`
แต่ render `CODEX_AGENTS_MD` ซึ่งยังมี `git stash` ในรายการคำสั่งที่อนุญาต และไม่มี
ข้อห้าม stash/restore/clean ใหม่ non-Claude ยังไม่มี PreToolUse guard ตัวนี้คอยชดเชย
จึงยังได้รับคำแนะนำที่ทำให้เกิด incident เดิม แม้เทส role files ทั้งหมดจะเขียว

**พิสูจน์:** เรียก `ensure_agents_md` ลงโฟลเดอร์ว่างจริง ได้ managed AGENTS.md ที่มี
language directive ใหม่ แต่บรรทัด `✅ ... git stash` ยังอยู่
นี่คือ rollout ของ #609 ที่ยังไม่ครบ ไม่ใช่การเพิ่มข้ออนุญาตใหม่ใน batch

**แนวแก้:** อัปเดต template ที่ provider ใช้จริงและ common docs ให้ตรงกับ rule;
ตรวจ generated context ควบคู่กับ role markdown

หลักฐาน: `ART/managed-context/AGENTS.md`, `ART/controls.json`

## LOW

ไม่มี finding ที่ต้องแก้เพิ่มในระดับนี้

## ข้อที่ตรวจแล้ว / ข้อจำกัด

- **#609 controls:** `git stash -u`, `--include-untracked`, `rtk git -C x stash`,
  bare mutating stash/restore และ mixed `stash list && stash pop` ถูก deny;
  `stash list/show` และ `echo "git stash"` ผ่าน ไม่เกิด false positive ในเคสนี้
  bare `git st` ผ่าน classifier; จะเป็น stash จริงเฉพาะเครื่องที่ตั้ง alias นั้นไว้
- **#611 structural:** manifest/lockfile/migration ยัง path-only deny แม้เป็น test path;
  keyword secret/API_KEY ใน test content ยังถูกตรวจ แต่ regex ไม่ใช่ secret scanner
- **#608 --continue:** Turbo 2.5.6 จริงบน fixture สอง workspace: ตัวหนึ่ง exit 1,
  อีกตัวเขียน `completed.txt` ตอน cleanup จบ; aggregate exit 1 ตามต้องการ
  หลักฐาน `ART/turbo-continue.json`, `ART/turbo-fixture/turbo-run.txt`
- **#608 marker:** ไม่ค้างถาวรหลัง completed test step แม้ไม่มี reset script เพราะ
  `qa_gate.py:1856` unlink เสมอ; มีแต่ warning เมื่อไม่มี reset script
  จึงไม่ได้รับรองว่า DB ถูก reset แล้ว กรณี process ถูกฆ่าก่อนถึง unlink จะคง marker ตามตั้งใจ
  ไม่ทดลอง reset DB จริงของผู้ใช้; targeted marker tests ใช้ doubles
- **Windows concurrency:** ระดับ Turbo serial workspace เป็นข้อเลือกตาม issue;
  ไม่ได้วัดชุด 7,386 เทสของโปรเจคต้นเหตุ จึงตอบจำนวนเท่าหรือรับรอง latency จริงไม่ได้
  เชิงโครงสร้างเวลาชุด independent tasks เปลี่ยนจากใกล้ max(duration) เป็น sum(duration)
  ภายใต้ worker/resource contention ตัวเลขจริงอาจต่างมาก ไม่มี performance finding จากการเดา
- **#612:** warning cap อยู่ก่อนเริ่ม wait และ summary บอกค่าที่ clamp;
  existing CLI/wait targeted tests ผ่าน ไม่ได้เปิด wait 3,000 วินาทีจริง
- **#613/#597:** offline CLI parsing ให้ payload `role=qa, mode=e2e`, exit 0;
  server และ `_assign_dispatch` ต่างรับ matching alias mode; dispatch normalize เป็น pane
  ภายใต้ role qa ซึ่ง `resolve_pane_role` chain ยังหาได้ ไม่มี blocker ใหม่จาก trace นี้
  ไม่ได้ assign pane จริง; transport ถูกแทนด้วย capture stub (`ART/controls.json`)
- **#615:** ใช้ `mark_abandoned(..., expired_queued_message)` เก็บ record/body ไว้
  ไม่ลบข้อมูลถาวร; CLI แสดงหมดอายุแยกจากส่งไม่สำเร็จ
- **#620:** marker แค่ first line ชนะ deep keyword จริง แต่ assign ยังผ่าน Lead role/token
  gate (`cli_server.py:490`/`:500`); ไม่พบสิทธิ์ assign ใหม่ให้ specialist
  จัดเป็นการเลือก scope ของ Lead ตาม issue ไม่ใช่ auth bypass ใหม่
- **#621 nudge:** code/log ล้วนเกิน 200 ตัวไม่มีไทยถูกเตือนจริง แต่ไม่ reject และจำกัด
  ครั้งต่อ task; เป็นผลของ heuristic ที่ issue ระบุไว้ ไม่ยกระดับเป็น release blocker
  role docs ภาษาอังกฤษไม่ได้มี directive บังคับให้ตอบอังกฤษ จึงไม่ใช่ conflict ด้วยตัวมันเอง
- **#610 × #604 × #593:** dedupe อยู่ก่อน resource release, delivery retirement,
  state teardown และการ schedule next assignment; reject จึงไม่ทำ pane หาย
  `force=True` ข้าม UI evidence/dedupe/undelivered-task gates ตามโค้ดและ existing test
  ผ่าน รวมถึง close deferral/suppression และ queue targeted tests
  แต่ M2 ทำให้ valid report ไม่ไปถึงการ forward queue ได้
- มีเพียง `done()` เรียก dedupe gate ในปัจจุบัน; `progress()` ไม่อัปเดต digest history
  แม้ข้อความอธิบายของ gate จะกล่าวถึง progress ด้วย

## การทดสอบและการส่งต่อ

ตั้ง `PYTHONPATH=<checkout>/src`, `QT_QPA_PLATFORM=offscreen` ก่อนทดสอบ
ครั้งแรก conftest หยุดเพราะ Python import site-packages; แก้เฉพาะ environment แล้วรันใหม่
ไม่แก้ shared install ผลก่อนแก้ environment ไม่นับเป็น product failure

| ชุด | ผล | หลักฐาน |
|---|---:|---|
| pane_guard / done_evidence / qa_gate / verify / task_scope / role_messages | 680 ผ่าน | `ART/targeted-existing.txt` |
| done gate + close children + CLI/server + wait + queued expiry + subagent + spawn argv | 394 ผ่าน | `ART/cross-feature.txt`, `ART/cross-feature.xml` |
| regression ใหม่เฉพาะ trust boundaries | 13 failed | `ART/trust-regressions.txt`, `ART/trust-regressions.xml` |
| Git restore จริง | 6/6 bypass และทับ fixture dirty content | `ART/real-probes.json` |
| stash clear ข้าม linked worktree จริง | allow แล้ว stash อีก checkout หาย | `ART/stash-cross-worktree.json` |
| Vitest binary จริง A/B | control ผ่าน / injected flags fail | `ART/vitest-real.json` |
| mixed assertion + worker text | classifier รับ retry, log ถูกทับ | `ART/mixed-retry.json` |
| Turbo --continue จริง | aggregate fail + sibling cleanup จบ | `ART/turbo-continue.json` |

เทสที่เพิ่มอยู่ใน `tests/` ตาม convention เดิม ไม่มี production fix แฝง;
scratch scripts, npm fixture และหลักฐานอยู่ใต้ artifacts ที่ cockpit กำหนดเท่านั้น
ก่อน ship ควรปิด H1–H4 แล้วรัน regression เหล่านี้ซ้ำ พร้อม review การแก้เฉพาะจุด
รายงานผ่าน `takkub done --fail` เพราะพบ blocking findings และ regression assertions ยังแดง

## Round 2 — Re-verify หลังแก้ H1–H4 + M1–M4 (d0fd4bef..0f81edf0)

**Verdict รอบนี้: ยัง ship ไม่ได้ — H1/H2 ปิดเฉพาะรูปแบบเดิม ยังมี bypass ชนิดเดียวกันที่ยังไม่ปิดจริง (HIGH ใหม่ 1 คลัสเตอร์ 4 รูปแบบ) ส่วน H3/H4/M1/M2/M3/M4 ปิดจริงครบทั้ง 6 ข้อ**

ตรวจ commit ที่ merge เข้า main ระหว่าง round 1–2: backend `d799ba64`, gemini `f82abd07`
(รวม `7160fbb0`), devops `59911e11` (รวม `396ee524`/`816f8d04`/`01ee8b55`/`b089d130`),
frontend `0f81edf0` (รวม `adb08a58`), และ `af3d25bd` (H3/H4) — ไม่แก้ production code เพิ่ม
ไม่ commit/push หลักฐานทั้งหมดอยู่ที่
`runtime/exports/2026-09-15/agent-takkub/batch-2.1.9-review-reverify/` เรียกว่า **ART2**

### ปิดจริง (ทดสอบซ้ำด้วยของจริง ไม่ใช่แค่เทสเขียว)

- **H3** — ป้อน real Vitest 4 ให้มี assertion fail จริง (`expected undefined to be '1'`) ปนกับ
  `[vitest-pool]: Timeout waiting for worker to respond` ในอีก test เดียวกัน:
  `_node_worker_timeout_flake` คืน `False` (เดิมคืน `True` แล้วโดน retry กลบ) — regression เคสเดิม
  ไม่เกิดซ้ำ หลักฐาน `ART2/mixed-retry.json` (`eligible_for_infra_retry: false`)
- **H4** — real Vitest 4.0.0 binary: `limit_concurrency=False` → ไม่มี flag เพิ่ม, exit 0;
  `limit_concurrency=True` → ใช้ `--maxWorkers=2` (ไม่ใช้ `--poolOptions` อีกแล้ว) exit 0 ทั้งคู่
  หลักฐาน `ART2/real-probes.json` case `vitest-4`
- **M1** — `pane_guard.evaluate_lead_direct_edit` จริงบน `src/auth/verify.spec.ts`,
  `src/payments/charge.spec.ts` (ลบ `verifySignature` call) และ control `src/security/check.ts`
  ถูก deny ทั้ง 3 เคส (`guard_allowed: false` ทุกตัว) หลักฐาน `ART2/m1-m4-probe-results.json`
- **M2** — จำลองครบ 4 สถานการณ์ด้วย hash จริง: (1) same-task อ้างไฟล์ชื่อเดิมซ้ำ (progress→done)
  ไม่ reject, (2) same-task ไฟล์คนละชื่อ bytes เดียวกัน → **reject** จริง (เคสที่เคย false-negative),
  (3) cross-task ไฟล์คนละชื่อ bytes เดียวกัน → **warning เท่านั้น** ไม่ reject, (4) failed report
  (`allow_reject=False`) ที่มี duplicate → ไม่ reject เปลี่ยนเป็น warning เสมอ ตรงตามที่ M2 อ้างครบ
  ทั้ง 4 แกน หลักฐาน `ART2/m2-probe-results.json`
- **M3** — โค้ด `_register_subagent` (orchestrator.py:2298-2305) แทรก `response_language`
  directive ลง capsule ตรงๆ แล้ว (จุดที่เดิมไม่มีเลย); เส้นทาง user-owned AGENTS.md ยืนยันด้วย
  code trace ว่า `pending_lang_directive` ถูกอ่านแล้วเคลียร์เป็น `None` ทันทีใน `assign()`
  (orchestrator.py:3641-3644) จึงไม่ถูกฉีดซ้ำในการ deliver ครั้งถัดไป — ตรงตาม "ครั้งเดียว" ที่ต้องยืนยัน
  เทสเฉพาะทาง 3 ไฟล์ (`test_lead_context_compact.py`, `test_spawn_generic_argv_matches_plan.py`,
  `test_subagent_mode.py`) 21/21 ผ่าน
- **M4** — `codex_agents_md.ensure_agents_md` เรนเดอร์ AGENTS.md จริงแล้วอ่านเนื้อหา: บรรทัด
  อนุญาตเหลือแค่ `git stash list/show` (read-only) และมีบรรทัดห้ามชัดเจนว่า mutating stash
  ทุกรูปแบบเป็น Lead-only เพราะ `refs/stash` ใช้ร่วมกันทุก worktree — ไม่มีข้อความอนุญาต
  bare `git stash` หลงเหลืออีกแล้ว หลักฐาน `ART2/m1-m4-probe-results.json` case
  `m4-agents-md-stash-mention`
- regression suite เดิม `tests/test_batch_219_trust_boundaries.py`: **17/17 ผ่าน**
  (round 1 คือ 1/14 ผ่าน) หลักฐาน `ART2/trust-boundaries-reverify.txt`
- targeted ข้ามทุกไฟล์ที่ commit เหล่านี้แก้ (pane_guard/qa_gate/verify/done_evidence/
  codex_agents_md/task_scope/role_messages): **774 ผ่าน 0 แดง** หลักฐาน `ART2/targeted-reverify.txt`
  — ไม่มี regression ใหม่จากการแก้ทั้ง 6 ข้อนี้

### ยังไม่ปิด — H1/H2 ยังเปิดอยู่ (HIGH ใหม่: bypass คนละรูปแบบ อิมแพคเดียวกับรอบ 1)

รอบนี้ทดสอบ 9 รูปแบบ bypass เพิ่มเติมตามที่ระบุ (wrapper ซ้อน, `--git-dir`/`--work-tree`,
`git -C` แบบ parent, `git worktree remove`/`prune`, `git switch --discard-changes`, env-prefix
`GIT_DIR=x`, pwsh call-operator, `git restore --staged`, `git -c alias.a="!..."`) — 5 รูปแบบถูก
guard บล็อกถูกต้อง (`git -C` parent, call-operator, `restore --staged`, `restore .` เปล่า,
`alias.a="!..."` แบบ shell-out) แต่ **4 รูปแบบยังเปิดจริงและพิสูจน์ด้วยการรันจริง**:

1. **`git --git-dir=<path> --work-tree=<path> restore .`** — `pane_guard.classify` คืน
   `allowed=True`; รันจริงบน fixture ที่มี uncommitted content ทับกลับ baseline สำเร็จ
   (`uncommitted_lost: true`) — อิมแพคเดียวกับ H2 เดิมทุกประการ แค่ใช้ flag คนละคู่กับที่ regex
   คุ้มครอง (`_GIT_SUBCMD_GAP` ไม่รู้จัก `--git-dir=`/`--work-tree=`)
2. **`GIT_DIR=<path> git stash drop`** (env-var prefix ก่อนคำสั่ง) — `classify` คืน `allowed=True`;
   ยืนยันด้วยการรันจริงผ่าน git-bash (shell เดียวกับที่ Bash tool ของ Claude Code ใช้บน Windows
   จริงๆ) กับ stash ของ repo อื่นที่ไม่เกี่ยวกับ worktree เลย — `stash@{0}` หายจริงหลังรัน
   (`before`/`after` ต่างกันจริงใน `ART2/deepdive-results.json`) รุนแรงกว่า H1 เดิมเพราะไม่ต้องพึ่ง
   linked-worktree เลย ชี้เป้า `.git` directory ไหนก็ได้ตรงๆ
3. **`git switch --discard-changes <branch>`** — `classify` คืน `allowed=True`; รันจริงบน
   fixture ที่มี branch `main` จริง ได้ `rc=0` และ uncommitted work หายจริง
   (`uncommitted_lost: true`) พฤติกรรมทำลายข้อมูลเทียบเท่า `checkout .`/`restore .` ที่มี pattern
   คุ้มครองอยู่แล้ว แต่ `switch --discard-changes` ไม่ถูกจับ
4. **`git worktree remove --force <target>`** (และ `prune`) — `classify` คืน `allowed=True` โดย
   ไม่เช็ค ownership ของ **target** เลย (เช็คแค่ cwd ของผู้เรียก) พิสูจน์ด้วยการรันจริง: pane
   frontend (อยู่ worktree ของตัวเอง) สั่ง `git -C <repo> worktree remove --force <backend worktree>`
   ได้จริง ไฟล์ `SECRET UNCOMMITTED WORK` ที่ยังไม่ commit ใน worktree ของ backend หายไปทั้งไดเรกทอรี
   — เป็นช่องทำลายงานของ role อื่นโดยตรง คนละกลไกกับ stash แต่ผลลัพธ์เดียวกัน (data loss ข้าม role)

**หมายเหตุ (ไม่ใช่ finding ยืนยันแล้ว):** wrapper ซ้อนสองชั้น
(`cmd /c "pwsh -c \"git restore work.txt\""`) ก็ได้ `guard_allowed=True` เช่นกัน (
`_unwrap_shell_wrappers` เหลือ escaped quote ค้างและไม่รู้จักว่าเป็นคำสั่ง git) แต่การรันจริงด้วย
quoting แบบนี้กลับเป็น no-op (pwsh ตีความ string ที่หลุด quote มาเป็น literal ไม่ใช่คำสั่งที่ execute)
จึงไม่ทำให้ข้อมูลหายในรูปแบบที่ทดลอง — เป็นช่องโหว่ระดับ classifier logic จริง แต่ยังไม่มี syntax ที่
ทดลองแล้วใช้งานได้จริง ลด severity เป็น LOW/ต้องตรวจเพิ่ม ไม่รวมในคะแนน HIGH ด้านบน

**แนวแก้ที่แนะนำ:** อย่า resolve เป้าหมายจริงของ Git invocation ด้วย pattern เฉพาะ `-C`/subcommand
อย่างเดียว — ต้อง parse `--git-dir=`/`--work-tree=`/`GIT_WORK_TREE`/`GIT_DIR` (ทั้ง flag และ env
var) เป็นส่วนหนึ่งของ effective target เสมอ, เพิ่ม `switch --discard-changes` (และ `switch -f`)
เข้ากลุ่ม pattern เดียวกับ `checkout`/`restore`, และให้ `worktree remove`/`prune` ตรวจว่า path
เป้าหมายเป็นของ role/assignment เดียวกับผู้เรียกก่อนอนุญาต ไม่ใช่เช็คแค่ cwd ปัจจุบัน

หลักฐาน: `ART2/real-probes.json` (case `git-dir-worktree`, `git-switch-discard`,
`env-prefix-stash-drop`, `worktree-remove`, `worktree-prune`, `nested-cmd-pwsh`),
`ART2/deepdive-results.json` (execution จริงของทั้ง 4 ข้อ พร้อม `uncommitted_lost`/
`stash_actually_dropped`/ไดเรกทอรีหาย), `ART2/gitdir-stash-results.json`
สคริปต์ทำซ้ำได้: `ART2/reverify_real_probes.py`, `ART2/reverify_deepdive.py`,
`ART2/reverify_gitdir_stash.py`

### สรุป

| ข้อ | สถานะ round 2 |
|---|---|
| H1 (shared stash ข้าม worktree) | **ยังเปิด** — ปิดเฉพาะ `stash clear`/`drop` ตรงๆ, ยังเปิดผ่าน `GIT_DIR=` env-prefix |
| H2 (shell wrapper/quoted path/target) | **ยังเปิด** — ปิด 6 รูปแบบเดิมครบ, ยังเปิดผ่าน `--git-dir=`/`--work-tree=`, `switch --discard-changes`, `worktree remove/prune` ข้าม role |
| H3 (mixed assertion ถูก retry กลบ) | ปิดจริง |
| H4 (Vitest 4 `--poolOptions` พัง) | ปิดจริง |
| M1 (`.spec.` เลี่ยง sensitive) | ปิดจริง |
| M2 (dedupe ข้าม task/shard) | ปิดจริง |
| M3 (language directive ไปไม่ถึง) | ปิดจริง |
| M4 (AGENTS.md ยังอนุญาต bare stash) | ปิดจริง |

**ผล:** ยัง ship ไม่ได้ — เหลือ HIGH เดิม 2 ข้อ (H1/H2) ในรูปแบบใหม่ที่ยังไม่ถูกปิด แม้
regression suite ที่ commit ไว้จะเขียว 100% เพราะ suite นั้นครอบคลุมเฉพาะ syntax ที่พบใน round 1
รายงานนี้ผ่าน `takkub done --fail`
