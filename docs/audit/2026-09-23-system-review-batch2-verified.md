# System review — ชุด 2: บั๊ก HIGH ที่ผ่านการค้าน (verified)

วันที่ 2026-09-23 · verify 39 ข้อ high จากชุด 1 ด้วย 2 ทีมอิสระต่อข้อ (tracer ไล่ path จริง + skeptic พยายามหักล้าง, หลายข้อ repro จริงบน venv) · ผล: **ยืนยัน 38** (severity หลัง verify: high 29 / med 9) · disputed 0 · refuted 1

> ทุกข้อในตารางนี้ = ทั้ง tracer และ skeptic เห็นตรงกันว่าเป็นบั๊กจริงและถึงตัวผู้ใช้ได้ในการใช้งานปกติ

## ยืนยันแล้ว

| # | sev | file:line | เรื่อง | repro (tracer) | เหตุผล skeptic | fix ที่เสนอ |
|---|---|---|---|---|---|---|
| 1 | high | `boot_flow.py:901` | A 'skipped' run_boot_stage result is rendered as a successful migration (page D) — 'ลองใหม่' after a rollback and the disk-space gate both show fake success with the plan's counts | On a machine whose DATA_HOME is still v1 layout, either (a) pre-seed the retry-guard: write {"rolled_back_for_version": "<current __version__>"} into auto_migrate_boot's state file (load_state()/_save_state target), or (b) fill the disk until free < 2x estimat | Confirmed end-to-end in current code. auto_migrate_boot.py:633-634 returns _done("skipped","previously-rolled-back") with empty reports whenever state["rolled_back_for_version"] == running version (written at :659 after  | Treat `action == 'skipped'` with a non-None plan as `ok=False` (error=result.reason, data_intact=True, promoted/archived/junk_deleted empty) or add a distinct 'skipped' outcome the window renders as ' |
| 2 | high | `boot_flow_window.py:2221` | GUI boot never runs auto_migrate_boot.run_boot_stage() on a fully-migrated (v2) install → version-marker never re-pinned, new ladder steps and drift-repair never applied (prod proof: marker stuck at 2.1.0 while venv is 2.1.31) | On an installed (non-dev) cockpit already on the v2 layout (layout_state()=='v2'): 1) upgrade the package (npm i -g / pip) to any newer version; 2) launch the cockpit normally (TAKKUB_BOOT_UPDATE unset) and let the boot wizard pass through to the main window;  | Confirmed, not refuted. On a fully-migrated (v2) install with the wizard enabled (default), boot goes app.py:1039 run_boot_flow_gate -> BootFlowWindow -> _proceed_to_migration_check -> plan_migration() returns None (boot | In `_proceed_to_migration_check`'s worker (or in `_on_plan_ready` when plan is None) call `auto_migrate_boot.run_boot_stage()` (via `flow.run_migration(None)`) before `accept()`, mirroring app._run_au |
| 3 | high | `boot_flow_window.py:2349` | Boot wizard's worker QThreads are parented to a QDialog that run_boot_flow_gate drops on return → 0xC0000409 abort whenever the gate exits with a worker still running (X/Esc during provider check, #640 deadline, Ctrl+C) | Windows, wizard enabled (TAKKUB_BOOT_UPDATE unset). (A) User path: make the boot provider check slow (e.g. unplug network so the npm-view lookups run toward the 30 s cap), launch the cockpit, and while the header shows the provider-update-check message press E | Confirmed, not refuted. In v2.1.32 all five boot-wizard workers are QThreads parented to the dialog (src/agent_takkub/boot_flow_window.py:1540,2148,2162,2207,2215) and the file contains no wait()/quit()/terminate()/reque | Keep the wizard alive past the gate (module-level ref or parent it to the QApplication) and `wait()`/`quit()` every entry in `_workers` before returning; give workers no parent (hold them in `_workers |
| 4 | high | `config.py:627` | load_projects() fail-open to an empty registry is persisted by the next writer (set_open_tabs/clear_active_project) → every project wiped from the V2 registry | On a migrated (v2) install: 1) quit the cockpit; 2) corrupt the V2 registry (truncate DATA_HOME .../projects/registry.json mid-file, simulating post-power-loss damage — or, live variant, have another process open it with share-mode None while the cockpit runs) | Confirmed by executable repro against current main (v2.1.32). config.py:627-631 catches OSError/UnicodeDecodeError/JSONDecodeError on the V2 registry read and falls through to the V1 projects.json fallback; post-#504/#56 | Make load_projects distinguish 'file exists but unreadable' from 'no file' (raise or return a sentinel), and have save_projects_json/set_open_tabs/clear_active_project refuse to persist when the read  |
| 5 | high | `core/migration/promote_v1.py:248` | _SHARED_DIR_LEGACY_GLOBS 'agents/*.md' archives the live custom-role .md files spawn reads from | On an installed (non-dev, fully v2-migrated) cockpit: 1) Role Manager -> create a custom role "myrole" (writes DATA_HOME/agents/myrole.md and agents/custom/registry.json; the dialog's template even tells the user to edit that .md). 2) Optionally edit DATA_HOME | Confirmed in current code (v2.1.32, no fix in #702-#710; promote_v1.py last touched by #605). src/agent_takkub/core/migration/promote_v1.py:248 puts "agents/*.md" in _SHARED_DIR_LEGACY_GLOBS; _shared_dir_legacy_candidate | Remove 'agents/*.md' from _SHARED_DIR_LEGACY_GLOBS, or only archive an agents/<role>.md when the V1 custom-roles.json source still exists and the role is not present in the V2 registry; never archive  |
| 6 | high | `core/migration/promote_v1.py:1220` | A late-write-kept entry leaves the archive WAL forever: resume never rescans, re-created live files are re-deleted or clobber the archived V1 snapshot, validate() stays red every boot | On a migrated (v2-layout) install where backups/archive-v1-legacy-wal.json still exists with SOURCE_PRUNED entries (prod is in this state): 1) confirm a WAL-listed file was re-created live, e.g. DATA_HOME/remote.json (re-pair the phone once to create it); 2) r | Confirmed, not refuted. promote_v1.py:1220-1221 skips ledger.clear() when late_write_kept is non-empty, and nothing else clears the WAL while _pending() is true. On the next apply pass, apply_copy_only (2675-2678) resume | When every WAL entry is SOURCE_PRUNED at resume time, clear the WAL and take a fresh candidate scan; treat a source that reappears AFTER its entry was PRUNED as a new file (never refresh dest / never  |
| 7 | high | `core/migration/promote_v1.py:2449` | ArchiveV1LegacyStep sweeps LIVE top-level state (remote.json, theme, graft store, cloudflared bin) into the archive on every apply pass | On an installed build (DATA_HOME=~/.agent-takkub, layout v2, no backups/archive-v1-legacy-wal.json): 1) enable Remote pairing and change the theme so the app writes DATA_HOME/remote.json and theme-settings.json (live paths); 2) close the cockpit and run `takku | Confirmed in current code (v2.1.32). promote_v1.py:2449-2453 `_archive_candidates()` archives EVERY DATA_HOME top-level entry not in a static protect set; _ARCHIVE_SKIP_NAMES (175-193) and _V2_TOP_LEVEL_NAMES (140-156) o | Default-deny: archive only names known to be V1 (LEGACY_MAPPING sources + _SHARED_DIR_LEGACY_GLOBS + an explicit allowlist), never 'everything not in a skip list'. At minimum add every live SETTINGS_H |
| 8 | high | `custom_roles.py:160` | save_custom_roles never invalidates cached_read → every read within 3s is stale (registry lost-update, delete no-op, create not registered) | 1) Start the cockpit with no custom roles. Settings -> Role Manager -> create role 'aaa' (succeeds: registry.json did not exist, so nothing was cached). 2) Wait >5 s (same session). Create a second role 'bbb'. New UI: creation fails with 'เขียน MCP/plugin poli | CONFIRMED, not refuted. Chain verified in current code (custom_roles.py unchanged since v2.1.11 per git log; #702-#710 batch did not touch it): (1) src/agent_takkub/custom_roles.py:117 reads via legacy_reader.read_json - | In save_custom_roles call `from .cached_read import invalidate; invalidate(path())` right after write_json_atomic (same contract v2_target.write_data already follows), or route the write through v2_ta |
| 9 | high | `editor_widget.py:153` | UTF-8 BOM file gains one extra BOM on every Ctrl+S (data corruption) | 1) On Windows, create a BOM file: powershell `Set-Content -Path t.ps1 -Value 'Write-Host hi' -Encoding UTF8BOM` (head bytes EF BB BF). 2) Launch the cockpit, open the project, double-click t.ps1 in the workspace Explorer to open it in the Monaco editor. 3) Pre | Confirmed in v2.1.32 code and by live repro with the project venv. src/agent_takkub/editor_widget.py:153 `text = resolved.read_text(encoding="utf-8")` keeps the leading U+FEFF (only utf-8-sig strips it), while editor_ser | In read_file_for_editor use `encoding="utf-8-sig"` (or `text.lstrip("﻿")`), and in editor_service._encode_for_write strip a leading U+FEFF from `text` before prepending _UTF8_BOM. |
| 10 | high | `editor_widget.py:628` | Re-opening an already-open file silently discards unsaved edits | 1) Launch the cockpit, open a project tab. 2) In the Explorer, double-click any text file; type a few characters in the editor (tab shows dirty), do NOT save. 3) Double-click the same file in the Explorer again (or click its row in the CHANGES panel). 4) The t | Confirmed in current code (v2.1.32). editor_widget.py:628 open_file never checks _open_paths for an already-open path: it re-reads the file off-thread and _on_file_read (line 684) always calls editorOpenFile. In static/e | In open_file, if the resolved path is already in _open_paths: skip the worker, run_js to activate the tab (and dispatch _on_diff_requested when show_diff) instead of editorOpenFile; alternatively make |
| 11 | high | `lead_inbox.py:1761` | Feedback-prompt auto-skip in _send_when_ready._check has no cooldown/attempt cap → '0\r' written every 150 ms poll; delivery stays prompt-blocked | 1) Cockpit with a gemini/agy teammate pane whose CLI shows its periodic survey ("How's the CLI experience so far? ... [0] Skip", field-confirmed #509) — or any pane whose visible screen matches both generic regexes (pty_session.py:327-334), since _prompt_block | CONFIRMED, not refuted. src/agent_takkub/lead_inbox.py:1761-1780 (v2.1.32 main): when `_reason == "feedback"`, `_check` writes `_skip_key` (default "0\r") unconditionally on every poll (`_READY_POLL_INTERVAL_MS = 150`, l | Reuse PaneState.feedback_prompt_dismiss_ts/attempts here (skip if now-ts<3.0 or attempts>=3), and do not count 'feedback' as prompt_blocked once a skip has been sent within the last few seconds. Also  |
| 12 | high | `limit_autoresume.py:109` | Weekly-window quota hit is declared a false positive: deny verdict checks only five_hour / codex primary | 1) On a Claude Max profile whose 7-day window is exhausted (seven_day utilization >=100) but whose current 5-hour window is under 95% (normal after a week of heavy use, e.g. the weekly cap trips early in a fresh 5h window), assign a task to a claude teammate p | Confirmed, not refuted. (1) Reachable: the weekly banner IS detected — provider_spec.py:1869-1877 GENERIC_QUOTA_MARKERS includes a weekly-limit reached phrase, so signal (a) fires and sets rate_limited_until. (2) The den | Only return 'denied' when EVERY known window (five_hour, seven_day, seven_day_sonnet; codex primary AND secondary via p_usage.windows) is below threshold; treat any window >= threshold as 'confirmed'. |
| 13 | high | `main_window.py:975` | Preset roles spawn into whichever project is active 15 s after boot, and boot-restored tabs steal focus/active | 1) In projects.json set project A active with "presets": ["frontend"] and open_tabs containing A and B (both folders exist). 2) Launch the cockpit and do not touch it for ~20 s. 3) Observe: the window ends focused on B's tab, active project is now B, and at ~1 | Confirmed in v2.1.32 code. src/agent_takkub/main_window.py:975 schedules preset spawns at 15s+ with no project arg; spawn_engine.py:2215 -> orchestrator.py:2209 resolves None to the active project AT FIRE TIME. Meanwhile | Capture `initial_project` and pass `project=initial_project` to the preset spawn lambdas; give `_open_project_tab` a `make_current: bool` parameter and pass False from the boot restore loop (then re-s |
| 14 | high | `main_window.py:1062` | Lead never spawns while the cockpit is not the foreground app, and the gate poll floods events.log at 20 lines/s | 1) Launch the cockpit on Windows. 2) Before the main window gains focus (or right after), alt-tab to another app or lock the screen and stay away. 3) Observe the Lead pane stays empty for as long as the cockpit is not the foreground app. 4) Open runtime/events | Confirmed in v2.1.32 source. src/agent_takkub/main_window.py:_spawn_lead_when_quiet requires QApplication.applicationState()==ApplicationActive (line 1062); the blocked branch (1068-1078) zeroes the quiet streak and resc | Drop `app_active` from the gate (or accept ApplicationInactive after N turns / a timeout), and log `boot_lead_gate_blocked` once per reason change instead of every poll tick. |
| 15 | high | `main_window.py:1588` | Closing a project tab leaves its teammate panes registered in the orchestrator; reopening the project can never spawn those roles again | 1) Open project A; from the Lead pane run: takkub assign --role qa "small task" (qa pane appears). 2) Close project A's tab (tab close button, confirm Ok) — main_window._close_project_tab clears tab.teammate_panes (line 1588) before close_all_teammates, so the | Confirmed in current code. main_window.py:1588 clears tab.teammate_panes BEFORE close_all_teammates (1589), so when orchestrator.close() synchronously emits paneClosed (orchestrator.py:6363) the deferred _teardown (main_ | In `_close_project_tab`, after `close_all_teammates`, call `self.orch.unregister_pane(role, project=tab.project_name)` for every pane in the saved `teammate_panes` list (or pop the whole `_panes_by_pr |
| 16 | high | `orchestrator.py:14628` | Stuck-recover respawn reads last_spawn_resumed before a deferred/queued spawn has run → full task re-pasted into a --resume'd conversation (Bug-5 duplicate work) | 1) takkub assign --role backend "<long task>" and let the pane wedge until the stuck watchdog fires _auto_recover_stuck (events.log: stuck_recover_close). 2) Immediately open a modal in the cockpit (Settings dialog or any QMenu) and keep it open past the 2s re | Confirmed in v2.1.32. src/agent_takkub/orchestrator.py:14627-14631 reads _ps_after.last_spawn_resumed immediately after spawn() returns, but spawn() returns ok=True WITHOUT creating a session on two paths: src/agent_takk | Decide replay-vs-nudge after the session actually attaches: move the `snap_task`/`_STUCK_RESUME_NUDGE` choice into a callback keyed on the spawn completing (e.g. check `msg` for 'deferred'/'queued' an |
| 17 | high | `orchestrator.py:15042` | Quota banner still on screen after reset is re-detected and rolled +24h (or re-added duration) → phantom new quota episode, Lead notice, park/reroute | 1) Settings: turn auto-resume (🌙) OFF. Assign a task to a claude teammate and let it hit the 5-hour limit so the banner with a clock-time reset ("...resets 3pm") is on screen; observe the first [system] hit-quota notice and events.log `rate_limit_detected`. 2) | Confirmed in v2.1.32. orchestrator.py:15030-15037 clears rate_limited_until at expiry with no latch; the next 5s tick (call sites 12184/12237) rescans the full visible screen (pty_session.py:2893) and, banner still on sc | After a reset (both in `_emit_rate_limit_reset` and the `now >= existing` branch of `_rate_limit_suppressed`) set `ps.quota_false_positive_armed = True` (or a dedicated `quota_reset_seen_marker`) so t |
| 18 | high | `pane_guard.py:827` | strip_takkub_text_payload splits on newline BEFORE blanking quotes — multi-line assign/done text is classified as commands (#649 regression) | 1) Cockpit running with any teammate pane in 'working' state (runtime machine-state.json mtime <=30s). 2) In the Lead pane, run via the Bash tool: takkub assign --role backend "step 1: do X npm run build step 2: report" (real newlines inside the quoted spec).  | CONFIRMED, not refuted. src/agent_takkub/pane_guard.py:827 `re.split(r"(&&\|\\|\\|\|[;\|]\|\n)", cmd)` splits on \n before line 829 applies `_QUOTED_RUN.sub('""', segment)`, so line 2+ of a multi-line quoted payload land | Split with the quote-aware `_split_chain_segments` (line 2207) or apply `_QUOTED_RUN.sub('""', ...)` to the whole command when `_TAKKUB_TEXT_CMD` matches at a segment start; then strip once at the top |
| 19 | high | `pane_guard.py:4135` | full_suite / scope_tiny / browser / git rules run on the raw command — a `takkub done` note quoting a runner or git verb is denied | 1) Start the cockpit and open any project so a guarded specialist pane exists (e.g. backend, claude provider — the PreToolUse Bash hook is wired on spawn). 2) In that pane, have the agent run: takkub done "ran:\npytest\nall green" (an actual multi-line quoted  | Confirmed in v2.1.32 working tree. strip_takkub_text_payload is called only at src/agent_takkub/pane_guard.py:838 (inside _is_heavy_build_or_suite); the scope_tiny qa-gate/full-suite checks (4046/4052), _BROWSER_PATTERNS | Compute `code = strip_takkub_text_payload(cmd)` once after `_unwrap_shell_wrappers` (line 3980) and evaluate every rule against `code` (keep `raw_cmd` only for the GIT_* env scans). |
| 20 | high | `pane_tools_policy.py:234` | save_policy never invalidates cached_read: regen + CLI verify read stale policy, `takkub mcp allow/deny` reports 'this is a bug' and variant file keeps the old MCP set | In the Lead pane: (1) `takkub mcp allow --role qa fetch` — first-ever override creates pane-tools.json and succeeds. (2) Wait >=5 s. (3) `takkub mcp deny --role qa playwright`. Observe: ok=False "deny 'playwright' for qa did not change the effective allowlist  | Confirmed in current code (v2.1.32). src/agent_takkub/pane_tools_policy.py save_policy writes via registry_copy_step.write_json_atomic at line 234 (and unlinks at line 198 for empty policy) with no cached_read.invalidate | In save_policy call `from .cached_read import invalidate; invalidate(path())` immediately after write_json_atomic (L234) and after `path().unlink(missing_ok=True)` (L198), before _regen_role_variants_ |
| 21 | high | `remote/__init__.py:172` | Boot-time remote start builds the Qt bridge on a worker thread: every bridged /api/* route answers 504 after each cockpit boot | 1) In the cockpit: Settings -> Remote -> Enable (works, pairing OK). 2) Quit and relaunch the cockpit (remote.json now has enabled=true, so boot takes the run_off_thread prepare() path). 3) From the phone PWA, or: curl -H "Authorization: Bearer <token>" http:/ | Confirmed in current code. Boot path: main_window.py:927 -> _start_remote_off_thread (main_window.py:1047 cli.run_off_thread) -> plain daemon threading.Thread (cli_server.py:309) runs RemoteControl.prepare(). prepare() - | Create `_Bridge` on the Qt thread: give RemoteHttpServer a `bridge=None` slot, construct/attach it in RemoteControl._start_qt() (or call `self._server.bridge.moveToThread(QCoreApplication.instance().t |
| 22 | high | `remote/tunnel.py:461` | Quick-tunnel / ngrok-random URL is only ever harvested by the Settings-dialog 6-second poll; after a cockpit restart the pairing URL is the previous run's dead hostname (or blank) | 1) Settings -> Remote: tunnel type = Quick, click Enable; wait for the pairing URL (https://<random>.trycloudflare.com/...) — it is saved into remote.json public_url. Pair a phone and confirm the URL works. 2) Quit and relaunch the cockpit (remote enabled=true | Confirmed, not refuted. (a) Restart staleness is deterministic: Enable persists the random quick-tunnel URL (user_actions.py:750-751/:895-896 save captured_url into remote.json public_url); next boot _start_blocking (rem | For quick/ngrok-random, always start with captured_url=None (ignore the stale public_url) and have RemoteControl expose a 'tunnel_url_ready' callback/timer used by both boot (main_window) and Enable p |
| 23 | high | `role_messages.py:112` | _write_all never invalidates cached_read → lost `takkub send` records and mark_delivered silently no-ops (→ spurious replays) | In a running cockpit with a backend pane busy on a task: (1) Lead runs `takkub send --role backend "note A"` and within 3 s `takkub send --role qa "note B"` (same project) → `takkub messages --role backend` no longer lists note A (record overwritten). (2) Send | CONFIRMED, not refuted. src/agent_takkub/role_messages.py has no cached_read.invalidate anywhere (grep: zero matches); every read goes through read_cached (role_messages.py:84) and every write goes through _write_all's b | In `_write_all`, after write_text call `from .cached_read import invalidate; invalidate(path)` (same pattern as backlog.py:130 / provider_config.py:128). Optionally write via tmp+os.replace. |
| 24 | high | `service_spawner.py:242` | service-stop / service-list trust a bare PID: PID reuse kills an unrelated process tree | 1) In any project pane (Lead), run: takkub spawn-service --name demo -- ping -n 2 127.0.0.1 — note the recorded pid P in the reply. 2) Wait for ping to exit (~2 s). 3) To simulate Windows PID reuse deterministically, open notepad.exe, note its PID, and edit RU | Confirmed in current code (v2.1.32). src/agent_takkub/service_spawner.py: _pid_alive (96-108) is bare psutil.pid_exists/os.kill(pid,0); stop() (240-252) kills psutil.Process(pid) plus all recursive children with no creat | Record `psutil.Process(proc.pid).create_time()` in ServiceRecord at spawn; in _pid_alive/stop treat the row as dead unless `abs(Process(pid).create_time() - rec.create_time) < 1.0`. |
| 25 | high | `settings_window.py:1641` | Save & Apply under solo-lead/pair persists rolesEnabled=False for every position; later switching to full/auto shows all positions OFF, flips the project to 'custom' or leaves assign rejected | 1) Set a project's team preset to 'ทำเอง' (solo-lead) via the status-bar team chip. 2) Open Settings → Providers & Roles (default legacy window); note all position toggles render OFF. 3) Change any role's provider combo (nothing else) → Save & Apply. 4) Inspec | Confirmed end-to-end in v2.1.32. settings_window.py:2549 renders each position toggle checked=_row_enabled_now(), which under solo-lead/pair is False for every position (team_preset.py:100/107 roles all False; _can_spawn | Only persist rolesEnabled for toggles the user actually changed (diff against the values rendered at build time), or render the toggle from `is_role_enabled` alone and show preset exclusion as a separ |
| 26 | high | `settings_window.py:4376` | Autoskills scan/install QThreads are parented to the WA_DeleteOnClose SettingsWindow (#688 pattern) — closing Settings mid-scan kills the whole cockpit | 1) Start the cockpit, open Settings, go to the Skills page. 2) Click "ดึง skill ตาม stack" (autoskills scan; the subprocess can run up to ~60s — a project whose scan is slow, or autoskills CLI missing/slow network, widens the window). 3) While the status still | Confirmed in v2.1.32 working tree. src/agent_takkub/settings_window.py:4376 `self._as_preview_thread = _AutoskillsPreviewThread(roots[0], self)` and :4420 `self._as_install_thread = _AutoskillsInstallThread(roots[0], sel | Construct both autoskills threads unparented, keep them alive in a module-level set (mirror `_CATALOG_THREADS` at 841/1071-1073), connect `finished` → discard + `deleteLater`; PyQt auto-drops the `res |
| 27 | high | `settings_window.py:4420` | autoskills QThreads parented to WA_DeleteOnClose SettingsWindow — close mid-run = Qt fatal abort (#688 class) | 1. Start the cockpit with an active project tab. 2. Open Settings -> SKILL view. 3. Click 'ดึง skill ตาม stack' (status shows 'กำลังสแกน stack…' while _AutoskillsPreviewThread runs npx autoskills, up to 60 s — on a slow network this is guaranteed dwell time).  | Confirmed in v2.1.32. settings_window.py:956 sets WA_DeleteOnClose on SettingsWindow; lines 4376 and 4420 parent _AutoskillsPreviewThread/_AutoskillsInstallThread to that dialog (`self`). Both threads run blocking subpro | Parent both threads to None (or the main window), keep the reference on self, and in SettingsWindow.closeEvent either wait() on a running thread or disconnect resultReady and let it finish detached; s |
| 28 | high | `shared_dev_tools.py:1325` | ensure_user_mcps() deletes MCPs the user added via `takkub mcp add` / Tools dialog on next boot (prunes everything not in ~/.claude.json) | 1) `takkub mcp add context7 npx --args "-y @upstash/context7-mcp"` (cockpit running or not) — runtime/shared-mcp.json now contains context7 and role variants include it. 2) `claude mcp add -s user demo npx -y any-credential-free-mcp` so ~/.claude.json gains on | Confirmed by code trace and live repro. All user add paths (cli.py:5267 takkub mcp add, settings_window.py:4129 Tools dialog, design_integrations.py:341 reference-21st) write only shared-mcp.json via add_mcp_server with  | Track provenance: only prune entries that were themselves merged from ~/.claude.json (e.g. tag merged entries with `"_source": "claude.json"` or keep a runtime/user-mcps-merged.json list) and leave ad |
| 29 | high | `task_ledger.py:155` | Ledger state saved without invalidating cached_read → second assign within ~3s overwrites the first (row + open pointer lost) | With the cockpit running and a project whose ledger was not written in the last ~2s, have Lead fire two assigns within 3s of each other (e.g. `takkub assign --role frontend "task A" && takkub assign --role backend "task B"`, or any --plan/fan-out batch). Open  | Confirmed by code reading AND live repro against the current working tree. src/agent_takkub/task_ledger.py:155-156 _save_state writes via _atomic_write and never calls cached_read.invalidate; _load_state (line 124) reads | In `_save_state` after `_atomic_write(...)` add `from .cached_read import invalidate; invalidate(_state_path(project))` (mirror backlog.py:128-130). Optionally also invalidate in `_atomic_write` calle |
| 30 | med | `core/accounts/facade.py:74` | V2 account override clobbers the curated CLAUDE_CONFIG_DIR (#563 skill gate) on every claude spawn | 1) On any machine with the cockpit, leave TAKKUB_SKILL_GATE and TAKKUB_V2_ROUTER unset (defaults). 2) Open a real project tab (e.g. agent-takkub) so a claude pane spawns (Lead or `takkub assign --role qa --provider claude`). 3) In that pane run: echo $CLAUDE_C | Confirmed in current code, not refuted. spawn_engine.py:3342-3349 order: inject_curated_claude_config_dir (pane_env.py:537-545, applies when project != 'default' and TAKKUB_SKILL_GATE != '0') sets CLAUDE_CONFIG_DIR to DA | Either (a) in spawn_engine.py move `_apply_v2_account_env_override` BEFORE `inject_curated_claude_config_dir` and pass the resolved `account.config_dir` as `base_dir` to `ensure_curated_claude_config_ |
| 31 | med | `core/migration/registry_copy_step.py:39` | write_json_atomic never invalidates the read cache -> post-apply validate() reads pre-write data for 3 s | On a migrated install where a V1 source still exists because archive prune is pending (e.g. DATA_HOME/role-models.json present alongside a populated <v2>/models/aliases.json): 1) edit role-models.json so its JSON differs from aliases.json's 'data'; 2) wait >2s | CONFIRMED, not refuted. src/agent_takkub/core/migration/registry_copy_step.py:39-63 write_json_atomic has no cached_read.invalidate() call, and Grep shows zero 'invalidate' matches anywhere under core/migration. All Regi | Call agent_takkub.cached_read.invalidate(path) at the end of write_json_atomic (same rule the memory note already mandates for every cached_read writer), or have each step's validate() read targets wi |
| 32 | med | `core/scheduling/facade.py:128` | RAM-derived max_panes_global is sampled once at cockpit boot and never refreshed; Settings/cache invalidation never reaches the live governor | On a 16GB machine with Settings → Scheduler never opened (max_panes_global unset, the default) and TAKKUB_V2_SCHEDULER at its default ON: 1) open enough apps (browser etc.) that available RAM at launch is ~5GB (balanced reserve 20% of 16GB = 3.2GB → cap = floo | Confirmed in v2.1.32. src/agent_takkub/core/scheduling/facade.py:128 substitutes the RAM-derived max_panes_global, and its docstring (lines 74-77) promises the figure is 'sampled fresh on every call' to 'track current he | Re-evaluate the RAM cap at request time: in `_denial_reason` (or a cheap throttled helper) recompute `max_panes_global` when the policy leaves it None instead of freezing it in `__init__`; keep the pa |
| 33 | med | `core/storage/v2_target.py:210` | read_data serves a 3s-TTL cached object to read-modify-write callers → cross-process lost update / duplicate issue numbers on state/issues/local.json | 1) Make gh fail fast: temporarily rename gh.exe off PATH (or disconnect network). 2) Process A (simulates cockpit; venv python REPL at repo root): ensure DATA_HOME v2/state/issues/local.json exists and its mtime is >2 s old, then prime the cache: `from agent_t | Mechanism confirmed in code: core/storage/v2_target.py:210 read_data -> legacy_reader.read_json -> cached_read.read_cached, whose stat-free fast path (cached_read.py:82-83, _STAT_TTL_S=3.0) returns the cached parse witho | Read-modify-write stores must bypass the TTL: have _load_local_issues (and any writer path) call cached_read.invalidate(target) before read_data, or add a `fresh=True` parameter to read_data/read_json |
| 34 | med | `main_window.py:1524` | Lead-spawn failure on the only/new tab destroys the shared usage corner; status ticks raise forever and later tab switches abort before set_active_project | 1) Make Lead spawn fail: rename the claude CLI off PATH (or point the lead provider at a missing binary). 2) Start the cockpit, close every project tab (empty state; the close path detaches the usage corner correctly). 3) Click + and open any project: status b | Confirmed by tracing every link. Failure branch of _open_project_tab (src/agent_takkub/main_window.py:1514-1533) removes and deleteLater()s the failed tab with no corner detach, while _on_tab_switched (1671-1672) had alr | Factor the corner detach (`setCornerWidget(None)`, `_usage_corner.setParent(None)`, `_limit_label_host = None`) into a helper and call it in the `_open_project_tab` failure branch (and in `_on_tab_swi |
| 35 | med | `qa_gate.py:1461` | Node auto-targeted tier: `vitest related` without --passWithNoTests fails the gate when a changed file has no related spec | In any project with vitest in devDependencies (or vitest.config.*), edit one source file that no spec imports (e.g. add a comment to an unreferenced util .ts), leave it uncommitted, then run `takkub qa-gate --auto` in that project. Step `test:vitest-related` r | Confirmed in current code (src/agent_takkub/qa_gate.py:1461): the vitest branch of _node_related_test_cmd omits --passWithNoTests while the jest branch (line 1460) includes it. Vitest's default passWithNoTests=false make | `pm_exec(pm, "vitest", "related", "--run", "--passWithNoTests", *files)`. |
| 36 | med | `release.py:401` | release rollback leaves the annotated tag behind → every retry refused with 'tag already exists' | 1) In the dev checkout, ensure CHANGELOG.md '## [vNEXT]' has at least one entry. 2) Make the wheel build fail, e.g. temporarily 'pip uninstall build' in .venv (or break [build-system] in pyproject after the guards would pass — simplest is removing the build mo | Confirmed in current code (v2.1.32). src/agent_takkub/release.py: tag created at line 391 before build_wheel at line 398 (default path: do_tag=True, do_build_wheel=True; cli.py:4477 defaults build_wheel True). build_whee | Track `tag_created = True` after l.391 and in the except block run `_git(repo_root, "tag", "-d", tag)` (best-effort) before the reset; or build the wheel before tagging. |
| 37 | med | `settings_knowledge_design.py:295` | Second click on Knowledge Refresh / Design Tools Refresh / Test raises RuntimeError (deleted _CallableThread) — button does nothing and an auto-issue is filed | 1) Launch the cockpit, open Settings -> the Knowledge & Design page -> Knowledge tab. 2) Click Refresh and wait until the status rows fill in. 3) Click Refresh again: nothing changes on screen; boot.log records RuntimeError 'wrapped C/C++ object of type _Calla | Confirmed in current code (v2.1.32): _CallableThread._cleanup (settings_knowledge_design.py:146-148) runs deleteLater() on `finished`, while the mixin never clears _kd_knowledge_thread/_kd_design_thread to None, so the s | Wrap the three `isRunning()` checks in try/except RuntimeError that sets the attribute to None (copy settings_usage.py:591-597), or have `_cleanup` also clear the owner's reference. |
| 38 | med | `spawn_engine.py:4410` | Crash auto-respawn replays the task even when spawn() only deferred/queued, so a later --resume gets the task twice | 1) In a project, assign a real task: takkub assign --role backend "create file X then commit". 2) While the pane is working, open the cockpit Settings dialog and keep it open (modal → spawn gate blocked). 3) In the backend pane, type /exit (unexpected exit → a | Confirmed in v2.1.32 working tree. spawn_engine.py:2401/:2428 return ok=True ('spawn deferred (gate blocked)' / 'spawn queued (arbiter busy)') before the resume decision at :3768-3778 runs; last_spawn_resumed is stamped  | Only replay when the spawn actually attached a session in this call (e.g. return a structured result / check pane.session.is_alive and that last_spawn_resumed was stamped by THIS spawn), or defer the  |

## Refuted (ไม่ใช่บั๊กในโค้ดปัจจุบัน)

- `claude_auth_config.py:177` Claude Auth override (base_url/api_key/extra_env) silently ignored: pane reads it from the curated CLAUDE_CONFIG_DIR, Settings saves it to the base profile dir — tracer: The cited defect is real but currently MASKED by finding index 5's bug, so the scenario as written is not reachable in default day-to-day use. Verified at v2.1.32: (1) src/agent_takkub/claude_auth_con · skeptic: Refuted for current code (v2.1.32). The claim ignores spawn_engine.py:3348-3349: _apply_v2_account_env_override runs on every claude spawn (v2_router_enabled() is effectively always on — TAKKUB_V2_ROU

## วิธีอ่านต่อ

- รายละเอียด scenario/evidence เต็มของทุกข้อ: `docs/audit/2026-09-22-system-review-batch1-findings.md` (ชุด 1)
- ชุดถัดไป: dead code/dead tests (ชุด 3-5) แล้วชุดแก้ (ชุด 6) บน branch + PR
