# Batch release 2.0.x — review รอบ 2 (ก่อน push/CI), 2026-09-07

**ผล: ยังไม่ควร push — HIGH ใหม่ 2 / MED ใหม่ 4 / LOW ใหม่ 7 · ของรอบ 1 เหลือค้าง 2 ข้อ (H4 กลับมาในรูปใหม่, M8 ยังไม่แตะ)**

ช่วงที่ตรวจ: `4942d65..HEAD` (`81e93b0`) — 11 commit, 128 ไฟล์ (+8,985 / −4,732)
ไม่ได้แก้โค้ดหรือเทสใดๆ · probe ทุกตัวรันจริงบนเครื่องนี้ · เทสรันด้วย `rtk proxy python -m pytest` + `PYTHONPATH=src` เท่านั้น
`snyk` ไม่ได้ติดตั้งบน PATH ของเครื่องนี้ → ข้ามขั้น dependency scan (บันทึกไว้ให้ Lead ตัดสินใจ)

หลักฐานทั้งหมดอยู่ที่ `runtime/exports/2026-09-07/agent-takkub/reviewer/` (probe รอบนี้: `probe_rollover.py`, `probe_routing_source.py` — รอบ 1: `probe_usage.py`, `probe_rollup.py`, `probe_dlg.py`, `probe_days.py`)

---

## ส่วน A — ข้อของรอบ 1 หายจริงไหม

รวม 2 รายงาน (`-cross.md` ของ reviewer + `-gemini.md`) ยุบข้อที่ซ้ำกันแล้วเหลือ **HIGH 7 · MED 14 · LOW 11**

### HIGH

| # | หัวข้อ (cross / gemini) | ผล | หลักฐาน |
|---|---|---|---|
| H1 | traversal + read-only GET เขียน/ลบไฟล์ (cross H1 · gemini H1) | ✅ หาย | `usage_ledger.py:114` `_KNOWN_PROVIDERS = frozenset(TURN_COUNTABLE) | frozenset(QUOTA_COUNTABLE)` · `_all_accounts:984-1019` reject ก่อน join + `is_relative_to(root)` ซ้ำอีกชั้น · `_valid_month:117` regex `^\d{4}-(0[1-9]\|1[0-2])$` · `remote/api.py:873-901` ส่ง `refresh=False` ทั้ง `query_usage` และ `daily_series` + clamp `days` ที่ `_MAX_DAYS` |
| H2 | usage อยู่บน Qt main thread + `rtk gain` ใน hot path (cross H2 · gemini H2) | ✅ หาย | `remote/http_server.py:213-215` `_OFF_THREAD_ACTIONS` มี `usage_history` แล้ว · `rtk_gain_summary()` (`usage_ledger.py:1067`) แยกออกจาก `query_usage` และ `settings_usage.py:297-303` เขียนข้อความชี้ไป CLI แทน — ไม่มี subprocess ใน render path อีก |
| H3 | Settings สร้าง Usage แบบ eager + dialog ไม่เคยถูกลบ (cross H3 · gemini H2/H3) | ✅ หาย | `settings_window.py:793` `WA_DeleteOnClose` · `_lazy_view_builders`/`_ensure_view_built` (`:877, 1089-1102, 1197-1213`) — VIEW_USAGE build ตอน `_goto_view` ครั้งแรกเท่านั้น |
| H4 | rollup ทับตัวเลขย้อนหลัง (cross H4 · gemini H4) | ❌ **แก้เคสเดิมได้ แต่สร้างเคสใหม่ที่หนักกว่า** | ดู **B-H1** ข้างล่าง — เคสที่รายงานรอบ 1 (partial rescan หลัง prune) ผ่านแล้วจริง (`tests/test_usage_ledger.py:510,556`) แต่ merge branch ทำให้ **ทุกเดือนถูกนับซ้ำ 2 เท่าตอนขึ้นเดือนใหม่** |
| H5 | codex cached_input นับซ้ำ (gemini H5) | ✅ หาย | `usage_ledger.py:679-687` `input_val = raw_input - cached` พร้อมเหตุผล/ตัวเลขจริงใน comment · TURN_FIELDS สี่ช่องแยกกันแล้ว |
| H5' | QThread ของ Refresh ไม่มี lifecycle (cross H5 · gemini H3) | ✅ หาย | `settings_usage.py:374-376` `isRunning()` guard + ปิดปุ่ม · `settings_knowledge_design._CallableThread` เปลี่ยนเป็น **unparented** + `_ACTIVE_THREADS` registry + `aboutToQuit → _wait_for_active_threads(3000)` → ไม่มี "destroy while running" อีก |
| H6 | spawn-boundary policy gate (gemini H6) | ✅ หาย | `spawn_engine.py:1802-1825` เช็ค `is_role_enabled` + `team_preset.can_spawn` **ในตัว `spawn()` เอง** หลัง early-return ของ pane ที่ยัง alive → restore/recovery/queue-drain ครอบหมด · `tests/test_spawn_role_policy_gate.py` (268 บรรทัด) ผ่าน |
| H6' | v1_only_write ตาบอด 3 จุด (cross H6 · gemini M5) | ⚠️ **แก้แล้ว แต่ #515 ทำให้ตาบอดจุดใหม่** | `v1_only_write.py:29-100` มี `reason="missing_mirror"` แล้ว + `_fanout_hit` คลุม role-providers → **แต่** #515 archive ไฟล์ที่ fan-out นั้นชี้อยู่ ดู **B-H2** |
| H7 | ไม่มี lock ข้าม process (gemini H7) | ✅ หาย | `usage_ledger.py:136-195` `_UsageLock` (O_CREAT+O_EXCL + stale reclaim 30s + fail-open 10s) ครอบทั้ง `record_turn` และ `rollup_daily` · `daily.json` เขียนแบบ tmp+`os.replace` (`:958-961`) |

**สรุป HIGH: 6/7 ✅ · 1 ❌ (H4) + 1 ⚠️ (H6' ถูก #515 ทำให้กลับมาตาบอด)**

### MED

| # | หัวข้อ | ผล | หลักฐาน |
|---|---|---|---|
| M1 (cross) / M8 (gemini) | auto-chain ไม่รู้จัก preset | ✅ | `pipeline_executor.py:525-600` resolve ผ่าน `verify_mode(cfg)` + `can_spawn` ทั้ง devops และ checker (qa/reviewer/self) |
| M2 (cross) | `classify()`/`suggest_team_size` ไม่มี production caller | ✅ | `cli.py:1464` (`takkub team suggest`) + `orchestrator.py:2421` (แนบ advisory ให้ Lead assign แรกเมื่อ preset=="auto") |
| M3 (cross) / M7 (gemini) | `--team` ปลดล็อกสิทธิ์ Lead เอง / ไม่ refresh Lead ที่รันอยู่ | ✅ (มีเศษเหลือ) | `orchestrator.py:2363-2385` ปฏิเสธเมื่อ Lead pane ยัง alive · `cli.py:570-583` + `cli_server.py:538-563` ปฏิเสธเมื่อ caller เป็น lead — **เศษเหลือ → B-M1** |
| M4 (cross) / M9 (gemini) | retheme ไม่ครอบ inline stylesheet | ✅ | `settings_window.py:1761-1787` `retheme()` เรียก `_render_accounts_rows(cache)` + `_retheme_usage()` · `settings_usage.py:306-318` rebuild จาก `_usage_last_result` ไม่ query ใหม่ |
| M5 (cross) | `task_dock._STATUS_FALLBACK` capture token ตอน import | ✅ | `task_dock.py:63-71` เก็บเป็น **ชื่อ attribute** แล้ว resolve ด้วย `getattr` ตอนใช้ |
| M6 (cross) | ข้อความ gap ของ cursor เป็นข้อเท็จจริงของเครื่อง dev | ✅ | `config.py:306-318` เขียนใหม่เป็น "ยังไม่ verify" ไม่อ้างเครื่อง |
| M7 (cross) | provider ที่มี gap ซ่อน account ที่มีอยู่ | ✅ | `accounts_adapter.provider_rows()` list account เสมอ · gap คุมแค่ `can_add`/`add_hint` |
| M8 (cross) | pool ยังบังคับ spawn แต่ไม่มี UI แก้ | ❌ **ยังอยู่** | `core/accounts/facade.py:55` `AccountPoolRegistry().all()` ยังถูกอ่านทุก spawn · grep ทั้ง `src/` ไม่มี UI ไหนแตะ pool แล้ว (`accounts_adapter.py` ไม่มีคำว่า pool เลย) · ไม่มี commit ไหนใน `4942d65..HEAD` อ้างถึง M8 |
| M9 (cross) | quota query ข้ามเดือนกลาง | ✅ | `usage_ledger._months_between:1100-1115` ไล่ครบทุกเดือน |
| M10 (cross) / M3 (gemini) | codex plan ระดับ provider ให้ทุกบัญชี | ✅ | `accounts_adapter._codex_plan_cached:190-213` ใช้ `store.get("codex", config_dir=home)` สำหรับ named home |
| M11 (cross) / M4 (gemini) | `provider_rows()` บน Qt main thread | ✅ | `settings_accounts._accounts_refresh:288-303` render cache ก่อน แล้วยิง `_AccountsRefreshWorker` เข้า `QThreadPool` |
| M1 (gemini) | opencode cursor ไม่เห็น WAL | ✅ | `usage_ledger._opencode_stat_key:463-486` รวม `-wal` size/mtime |
| M2 (gemini) | quota sample ของ named account ถูกเก็บเป็น `default` | ✅ | `provider_usage._account_name_for_config_dir:1292-1316` map config_dir → ชื่อ profile จริง |
| M6 (gemini) | JSON รูปผิดล้ม import ทั้งชุด | ✅ | `record_turn:391-397` (TypeError/ValueError → skip row) · `import_all:835-857` แยก try ต่อ provider |

**สรุป MED: 13/14 ✅ · 1 ❌ (M8 pool)**

### LOW — ✅ ครบทั้ง 11 ข้อ

`godfile-map.md:142` แก้แล้ว (L1) · docstring plan chip/End Session อัปเดตแล้ว (L2 / gemini L1: `status_header.py:288-296, 493-500`) · `CHIP_PLAN_MAX` ถูกถอดหมด (L3, grep = 0 hit) · `_custom_role_names()` ไม่มี `project` แล้ว (L4, `team_preset.py:162`) · docstring `can_spawn` เขียนจบประโยค (L5) · L6/L7/L8 อยู่ใน `f5ff536` · L9/L10 บันทึกใน `role-and-workflow.md`

---

## ส่วน B — ของใหม่ (`4942d65..HEAD`)

| Sev | File:line | ปัญหา | หลักฐาน (รันเอง) | แก้ยังไง |
|---|---|---|---|---|
| **B-H1** | `usage_ledger.py:900-966` (`rollup_daily`, merge branch `:944-951`) | **ตัวเลข usage ของ "เดือนที่แล้ว" ถูกคูณ 2 ทุกครั้งที่ขึ้นเดือนใหม่** — เดือนที่ยังอยู่ใน retention ถูก `daily.update(month_fresh)` (แทนที่) ทุกรอบ พอ `_month_n_ago(0)` ขยับ เดือนเดิมกลายเป็น `will_prune` → เข้า branch ใหม่ที่ **บวกทับ** ยอดที่เผยแพร่ไปแล้ว | `probe_rollover.py` (แช่นาฬิกาอย่างเดียว ไม่แตะ signature จริง): Sep rollup #1 = `{turns:2, input:300, output:30}` · #2 = เท่าเดิม (idempotent) · **Oct rollup = `{turns:4, input:600, output:60}`** ทั้งที่ raw บนดิสก์มี 2 turn · เทส H4 ที่มีอยู่ (`tests/test_usage_ledger.py:510, 556`) บันทึกเข้าเดือน `_month_n_ago(2)` เสมอ → เข้า merge branch ตั้งแต่ rollup แรก จึงไม่เคยแตะ transition นี้ | merge เฉพาะ **request_id ที่ยังไม่อยู่ใน `_seen_ids[month]`** (ข้อมูลมีครบอยู่แล้วในไฟล์นั้น) แทนการบวกทั้งไฟล์ · เพิ่มเทส: rollup ในเดือน → เลื่อน cutoff → rollup อีกครั้ง → ยอดต้องเท่าเดิม |
| **B-H2** | `provider_config.py:107-145` + `:193-199` · `core/migration/steps_v1.py` (`_routing_sources` / `_routing_payload`) · `core/storage/v1_only_write.py:181-188` | **#515 ย้าย global routing ไป `role-models.json` แต่ทั้ง migration ladder และด่าน #502 ยังชี้ `role-providers.json` ที่เพิ่งถูก archive** ⇒ (a) `role_models.set_provider/_save` (model picker: `settings_window.py:1400`, `provider_model_refresh.py:355`) dual-write แค่ `models/aliases.json` **ไม่เคย** `dual_write_routing` → v2 `config/routing.json` ค้าง (b) `scan_v1_only_writes()` ไม่เห็น เพราะ source ที่มันดูไม่มีอยู่แล้ว (c) `_routing_payload()["global"]` = `{}` ⇒ `migrate apply/validate` จะเขียนทับ global ของ v2 ด้วย `{}` | `probe_routing_source.py` (isolated `AGENT_TAKKUB_HOME`, ไม่แตะ state จริง):<br>`load_providers(None)` → archive ไฟล์เก่าเข้า `backups/` ✔<br>`role_models.set_provider('frontend','gemini')` → `v2 routing.json global` ยังเป็น `{'frontend':'codex'}` **STALE**<br>`ladder _routing_sources()` = `{'global': 'role-providers.json'}` (ไฟล์ไม่มีแล้ว)<br>`ladder _routing_payload()['global']` = `{}`<br>`scan_v1_only_writes()` = `[]` ← **ด่าน exit อ่านว่าสะอาด** | เพิ่ม `role-models.json` เป็น V1 source ของ routing fan-out (ทั้ง `_routing_sources` และ `_routing_payload`) · ให้ `role_models._save()` เรียก `dual_write_routing(load_providers(None), …)` ด้วย ไม่ใช่แค่ `dual_write_role_models` · ก่อนเริ่มนับ 1 สัปดาห์ของด่าน #502 ต้องมีเทส "เปลี่ยน provider ผ่าน model picker แล้วไม่ mirror → `scan_v1_only_writes()` ต้องจับได้" |
| **B-M1** | `cli_server.py:1370-1384` (`cmd == "team"`) · `orchestrator.set_team_preset` | **`takkub team set solo-lead` ไม่มี gate ว่า caller เป็น lead** — M3 ปิด `--team` ไว้ทั้ง client+server แล้ว แต่ verb นี้ทำสิ่งเดียวกัน (ตั้ง standing preset ที่มี `lead_may_implement=True`) แค่มีผลตอน Lead spawn รอบหน้า | อ่านโค้ด: guard M3 อยู่ที่ `cli_server.py:538-563` ผูกกับ `req.get("team")` ของ `assign` เท่านั้น · `cmd == "team"` ที่ `:1370` ไม่เช็ค `req.get("from")` เลย · `set_team_preset` ยัง broadcast `[system] … lead แก้โค้ดเองได้: ใช่` เข้า Lead pane | ใช้ guard เดียวกับ M3: reject `team set` ที่ `from == "lead"` เมื่อ preset ปลายทางมี `lead_may_implement=True` (หรือ reject ทุกกรณี ให้เปลี่ยนจาก Settings/มือถือเท่านั้น) |
| **B-M2** | `tests/test_settings_window.py:1534` (`TestAccountsView::test_remove_account_persists_and_updates_auth_combo`) | **เทสตกแบบไม่ deterministic ใน targeted batch** — full gate ของ qa รันทั้ง suite จึงเสี่ยงแดงแบบสุ่ม | รัน batch 18 ไฟล์ → `FAILED … assert not any(p["name"] == "work" …)` · รันไฟล์เดียว / เทสเดียว / รัน batch เดิมซ้ำ → ผ่านหมด · bisect คู่ (`test_core_storage_dual_write`, `test_core_migration_v1_steps`, `test_doctor`, `test_remote_api`, `test_core_v2_settings`, ชุด remote 6 ไฟล์ + `test_settings_window` เต็ม) ผ่านทุกคู่ ⇒ เป็น timing ไม่ใช่ลำดับไฟล์ | จุดที่เพิ่ง introduce nondeterminism คือ `_AccountsRefreshWorker` บน `QThreadPool.globalInstance()` (`settings_accounts.py:300-303`) — ไม่มีใคร join/cancel ตอนปิด dialog และ pool เป็น global ข้ามเทส · ให้เทสรอ worker จบ (หรือ inject pool) และให้ production รอ job ค้างตอน quit เหมือน `_ACTIVE_THREADS` ของ `_CallableThread` |
| **B-M3** | `token_meter.py:115-137` (`session_project_dirs_for_cwd`) · `pane_env.py:499-504` | **F1: transcript เก่าของ teammate หาไม่เจอหลังอัป** — candidate list มีแค่ `[<ชื่อใหม่ที่มี -role>, encode_path_for_claude(cwd)]` ไม่มี `takkub-project-<ns>` ซึ่งเป็นชื่อที่ teammate ทุกตัวเขียนอยู่ก่อน #516 | grep: `_CLAUDE_PROJECT_DIR_NAME_PREFIX` มีแค่ 2 ที่ (`pane_env.py:56`, `token_meter.py:94`) และ `token_meter.py:132` สร้าง new_name จาก `project_ns` ล้วน ไม่รับ `base_role` · Lead ไม่กระทบ (ชื่อไม่มี suffix) — resume picker / `remote/notify.py:768` เป็น Lead-only จึงปลอดภัย · ข้อมูลไม่หาย แค่ค้นไม่เจอ | เติม `f"{PREFIX}{project_ns}"` เป็น candidate สุดท้าย (legacy) ใน `session_project_dirs_for_cwd` — ราคาเท่ากับที่ทำให้ legacy encoded-cwd อยู่แล้ว |
| **B-M4** | `usage_ledger.py:962-967` | `unlink()` ของ raw month ที่ถูก prune จับ `OSError` แล้ว `pass` — ถ้าไฟล์ถูกล็อก (Windows, เกิดได้จริง) ไฟล์อยู่ต่อ และ merge branch จะ **บวกซ้ำทุก rollup ถัดไปไม่จำกัดจำนวนครั้ง** | อ่านโค้ด: merge branch บวก `month_fresh` ทั้งก้อนเสมอ ไม่เช็ค `_seen_ids` ก่อน — สาเหตุร่วมกับ B-H1 | แก้พร้อม B-H1 (merge เฉพาะ id ที่ยังไม่ seen) แล้วเคสนี้หายไปด้วย |
| **B-L1** | `update_panel.py:1186-1188` · `main_window.py:283, 398` | `_on_install_rtk_clicked` เหลือเป็น stub `pass` ที่ไม่มีใครเรียก และ comment ยังพูดถึง `self._btn_install_rtk` ที่ถูกถอดไปแล้ว — ผิดกติกา "เอาออก = ถอน dead code ทั้งสาย" | grep `src/` + `tests/`: `_on_install_rtk_clicked` 1 hit (นิยามตัวเอง), `_btn_install_rtk` 2 hit (comment ล้วน) | ลบเมธอด + แก้ comment 2 บรรทัด |
| **B-L2** | `core/storage/dual_write.py:152, 167, 323` | `dual_write_exec_mode` / `dual_write_rtk_enabled` / `dual_write_autoresume` ไม่มี production caller แล้ว (เหลือแต่เทส) หลัง #515 ถอด setter ทั้งสาม · docstring ของ `:167` ยังอ้าง `rtk_helper.set_rtk_enabled()` ที่ถูกลบไปแล้ว | grep: hit ทั้งหมดอยู่ใน `dual_write.py` เอง + `tests/test_core_storage_dual_write.py` | commit message บอกว่า "deliberately untouched" — ถ้าตั้งใจให้อยู่ถึง #504 ให้เขียนกำกับใน docstring ว่าไม่มี caller แล้ว |
| **B-L3** | `boot_update_window.py:121` · `core/migration/engine.py:214` | docstring/comment ยังอ้าง `settings_core_v2.py` ที่ถูกลบใน #515 | grep `settings_core_v2` เหลือ 3 hit เป็นข้อความล้วน (อีก 1 ที่ `settings_window.py:171` อธิบายการลบ = ถูกต้อง) | แก้ข้อความ 2 จุด |
| **B-L4** | `exec_mode.py:57-66` · `performance_settings.get_mode` · `provider_config.load_providers:167-168` | **read API ที่มี side effect เขียนไฟล์** — `exec_mode.current()` เรียก `_archive_legacy_file()` ทุกครั้ง, `get_mode()` migrate ตอนอ่าน, `load_providers(None)` migrate + `role_models.set_provider` + `archive_settings_file` ตอนอ่าน · `provider_for` (เรียก `load_providers`) วิ่งทุก ~5s ต่อ pane ตาม comment ในไฟล์เอง และอยู่บน Qt main thread | ตอนรัน `probe_routing_source.py` บรรทัด `provider_config.load_providers(None)` เพียงบรรทัดเดียวย้ายไฟล์เข้า `backups/` และเขียน `role-models.json` ใหม่ | หลัง migrate รอบแรกเหลือแค่ `Path.exists()` ต่อ call (ยอมรับได้) แต่ควรย้าย migration ไป boot path เดียว (`auto_migrate_boot`) แทนการแขวนไว้ใน read API |
| **B-L5** | `usage_ledger.py:684-686` | `input_val = raw_input - cached` ไม่ clamp — ถ้า provider เคยรายงาน `cached > input` จะได้ค่าติดลบเข้า ledger | อ่านโค้ด (ยังไม่พบข้อมูลจริงที่ติดลบ) | `max(0, raw_input - cached)` |
| **B-L6** | `config.archive_settings_file:144-163` | ถ้ามี backup ชื่อเดียวกันอยู่แล้ว → `return True` โดย**ไม่ย้าย** แล้ว `performance_settings.get_mode` เขียนทับไฟล์ legacy นั้นด้วยรูปแบบ slim ⇒ legacy ก้อนที่สองหายโดยไม่ได้ archive | อ่านโค้ด · บนเครื่องนี้ `~/.takkub/backups/` มี `exec-mode.json`, `rtk-enabled.json`, `autoresume.json`, `performance-settings.json` แล้ว (migration รันจริง 2026-09-07 14:09, 3 นาทีหลัง commit `e7cf89d`) | ถ้า dest ชนให้เติม suffix เวลา แทนการปล่อยผ่าน |
| **B-L7** | `remote/notify.py` (`_rec_epoch_ts`) · `core/conversation/ingest/codex_adapter.py` (`_record_epoch`) | `datetime.fromisoformat(ts.replace("Z","+00:00")).timestamp()` — timestamp ที่ **ไม่มี offset** (naive) จะถูกตีเป็นเวลา **local** ⇒ epoch เพี้ยนตาม timezone ของเครื่อง | อ่านโค้ด · ปัจจุบัน claude/codex เขียน `Z` เสมอ (ยืนยันจาก importer เดิม) → เป็น latent | ถ้าไม่มี tzinfo ให้ `replace(tzinfo=UTC)` ก่อน `.timestamp()` |

### ตรวจแล้วไม่พบปัญหา (จดไว้ให้เทียบ)

- **`/api/team-preset`** — GET `http_server.py:658` ผ่าน `_check_bearer` + `_check_password_gate` เหมือน endpoint view-safe อื่น · POST `:808-821` เพิ่ม `allows_control()` 403 ก่อน + parse JSON แบบ 400-on-bad · `api.team_preset_set` validate `preset_id in QUICK_PRESET_IDS` → 400 (ไม่ยอมรับ `custom`) · payload ตอบกลับไม่มี credential
- **redirect VIEW_\* เก่า** — `settings_window.py:198-212` `_VIEW_REDIRECTS` ครบทั้ง 8 ตัว (TEMPLATES, PLUGINS_MATRIX, SKILL_MATRIX, CORE_V2_ACCOUNTS/ROUTING/BRAIN/SCHEDULER, PERFORMANCE, NEW_ROLE) และ slot ที่เหลือเป็น `QWidget` เปล่า ไม่ใช่ page ค้าง
- **#517 timestamps** — ทุก provider ส่ง `ts` (claude/codex = ISO ของ record เอง, opencode = `time_created/1000`, gemini = `None` แบบตั้งใจ + คอมเมนต์อธิบาย, V2 store = `m.created_at` float ที่ `store.py` default เป็น `time.time()` เมื่อ adapter ให้ None) · `SSEBroadcaster._push` `body.setdefault("ts", time.time())` · `app.js formatTs` fallback เป็น `new Date()` เมื่อ `ts` ไม่ใช่เลข · `sw.js` bump v39→v40 (บทเรียน 1.6.15) ✔
- **role file diet (#516)** — pointer `docs/roles/...` ทุกตัวใน `.claude/agents/*.md` ชี้ไฟล์ที่มีอยู่จริงครบ 100% (สคริปต์ตรวจแล้ว 0 missing) · guard suite ผ่านครบ **8 ชุด**: boot_context_pointers, test_conventions, host_network, host_destructive, browser, git_guard, git_commit_guard, pip_editable
- **F1 ฝั่ง Lead** — `claude_project_dir_name(ns, "lead")` คืนชื่อเดิมไม่มี suffix · `remote/notify.py:768` (session picker) และ `spawn_engine._resume_uuid_matches_cwd` ส่ง `base_role` ต่อกันถูกต้อง ⇒ `--resume` / session picker / remote history ของ Lead ไม่กระทบ (teammate ดู B-M3)
- **main thread (#488)** — remote action ใหม่ (`team_preset_status` / `team_preset_set`) อยู่นอก `_OFF_THREAD_ACTIONS` โดยตั้งใจ: เป็น JSON read/write เล็ก + ต้องการ Qt-thread ownership เพื่อ broadcast เข้า Lead pane — ไม่มี subprocess / HTTP / สแกนไฟล์ในเส้นนี้

### เทสที่รันจริงรอบนี้

`rtk proxy python -m pytest` + `PYTHONPATH=src` เท่านั้น (rtk ธรรมดาสรุปผลเพี้ยน exit 0):

- `test_usage_ledger · test_settings_usage · test_spawn_role_policy_gate · test_core_storage_v1_only_write · test_provider_config · test_performance_settings` → **183 passed**
- guard 8 ชุด + `test_boot_context · test_boot_context_ceiling · test_token_estimate · test_role_memory` → **237 passed**
- batch 18 ไฟล์ (#512/#515/#517/remote/settings/migration/dual-write) → **รอบแรก 1 failed (B-M2), รันซ้ำ passed ทั้งหมด**

---

## สรุปให้ Lead

**ห้าม push** — ติด HIGH 2 ข้อ:

1. **B-H1** ตัวเลข usage เพี้ยน 2 เท่าทุกเดือน — ขัดกฎ "ตัวเลขจริงจาก provider เท่านั้น" ตรง ๆ และจะเริ่มเห็นผลวันที่ 1 ต.ค.
2. **B-H2** ด่าน exit ของรอบนี้ (`v1_only_write=0` + `drift=0` ครบ 1 สัปดาห์) จะอ่านว่า "สะอาด" ทั้งที่มองไม่เห็น source จริง — ถ้าปล่อยไป การนับ 1 สัปดาห์จะไม่มีความหมาย และ #504 (ย้ายจริง) จะยืนบนหลักฐานที่พิสูจน์ไม่ได้

ของรอบ 1 ที่ยังค้าง: **M8 (account pool ยังบังคับ spawn แต่ไม่มี UI แก้)** — ไม่มี commit ไหนแตะเลย ตัดสินใจได้ 2 ทาง: โชว์ pool แบบ read-only ในหน้า Accounts หรือถอน pool path ออกจาก spawn ไปพร้อม #504
