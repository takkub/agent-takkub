# Batch release 2.0.x — cross-review (reviewer pane, 2026-09-07)

**ผล: ยังไม่ควร push — HIGH 6 / MED 11 / LOW 10.**

Baseline `4a8c348` → HEAD `4942d65` · 101 files, +8,881 / −1,095 · 8 commits (#502, #103, #506, #505, #507, #510, #512).
ไม่ได้แก้โค้ดหรือเทสใดๆ · ทุกข้อ HIGH มี repro/measurement ของตัวเองแนบไว้ (ไม่ใช่การอ่านโค้ดอย่างเดียว)

> **หมายเหตุเรื่องไฟล์:** งานนี้สั่งให้เขียนลง `docs/audit/2026-09-07-batch-2.0.x-review.md` แต่ไฟล์นั้นถูกเขียนไปแล้วเมื่อ 12:31:59 (2 นาทีก่อน pane นี้เริ่ม) โดย pane คู่ขนาน — จึงเขียนแยกไฟล์นี้แทนเพื่อไม่ให้ Lead เสียรายงานอีกฉบับไปตอนเทียบกัน

---

## Findings

path ทั้งหมด relative กับ `src/agent_takkub/` ถ้าไม่ระบุอย่างอื่น · **HIGH** = ต้องแก้ก่อน push · **MED** = ควรแก้รอบนี้ · **LOW** = จดไว้

| Sev | File:line | ปัญหา | ทำไมสำคัญ (หลักฐาน) | แก้ยังไง |
|---|---|---|---|---|
| **H1** | `remote/api.py:823` · `usage_ledger.py:704-715`, `686-696` | **`GET /api/usage/history?provider=…` เดินออกนอก ledger root แล้ว "เขียนไฟล์ + ลบไฟล์"** — `provider` จาก query string ถูกส่งเข้า `_all_accounts()` ตรงๆ แล้วทำ `usage_root() / prov` โดยไม่ validate | repro จริง (`$TAKKUB_ARTIFACTS_DIR/reviewer/probe_usage.py`): `provider="../OUTSIDE"` → `BEFORE ['2000-01.jsonl']` → `AFTER ['daily.json']` · เขียน `daily.json` ลงไดเรกทอรีนอก ledger **และลบ** `2000-01.jsonl` ทิ้ง · บน Windows `provider` ที่เป็น absolute path (`D:/…`) จะแทนที่ base ทั้งก้อน · มี auth (bearer + password gate) แต่ phone ที่ pair แล้วทุกเครื่องยิงได้ | allowlist `provider` กับ key ของ `TURN_COUNTABLE`/`QUOTA_COUNTABLE` เท่านั้น (reject ค่าอื่นเป็น 400) + `Path.resolve()` แล้วเช็ค `is_relative_to(usage_root())` ก่อนแตะ fs · validate `month` เป็น `YYYY-MM` และ bound `days` |
| **H2** | `remote/http_server.py:213`, `305-315` · `usage_ledger.py:860`, `903`, `732` | **endpoint ที่ docstring บอกว่า "view-mode safe (read-only)" จริงๆ แล้วเขียน/ลบไฟล์ + spawn subprocess บน Qt main thread** — `usage_history` ไม่อยู่ใน `_OFF_THREAD_ACTIONS` (มีแค่ pulse/lead_say/lead_upload/answer_picker) ⇒ วิ่งบน main thread · `query_usage` เรียก `rollup_daily` (เขียน `daily.json` + `unlink` raw) ต่อทุก account แล้ว `_rtk_gain_summary()` ยิง `subprocess.run(["rtk","gain"], timeout=5)` · `daily_series` roll ซ้ำอีกรอบทุก account | **วัดเอง: `rtk gain` = 473 / 465 / 462 ms** (rtk อยู่บน PATH จริงที่ `C:\Users\monch\bin\rtk.EXE`) ⇒ ทุกครั้งที่มือถือเปิด usage sheet cockpit ค้าง ≥0.46 s (เพดาน 5 s) + I/O rollup ×2 รอบ · นี่คือ defect class เดียวกับ #488 ที่ module docstring ของ `v1_only_write.py` เพิ่งเขียนเตือนไว้เอง | ย้าย `usage_history` เข้า `_OFF_THREAD_ACTIONS` · แยก read-only query ออกจาก rollup/prune (remote อ่าน snapshot อย่างเดียว) · ย้าย `_rtk_gain_summary` ออกจาก `query_usage` ให้ caller ที่เป็น CLI เรียกเอง |
| **H3** | `settings_window.py:1143` · `settings_usage.py:210`, `217-223` · `user_actions.py:280` | **เปิด Settings หน้าไหนก็ได้ = จ่ายราคาข้อ H2 ทุกครั้ง** `_build_usage_view()` ถูก build ทันทีตอนสร้าง stack (ไม่ lazy) และมันเรียก `self._render_usage()` → `query_usage()` + `daily_series()` → `rtk gain` บน main thread · ซ้ำร้าย `SettingsWindow(self, …)` มี parent เป็น MainWindow และไม่มี `WA_DeleteOnClose`/`deleteLater` เลย | วัดเอง (`probe_dlg.py`): dialog ที่ parent แล้วปล่อยหลุด scope **ยังอยู่ครบ 5 ตัวจาก 5 ครั้ง** ⇒ เปิด Settings 5 รอบ = SettingsWindow 5 ตัวค้างใน memory (แต่ละตัวสร้าง 17 หน้าเต็ม รวม Usage + Accounts) · docstring ของ `settings_usage.py` อ้างว่า "cheap even at view-construction time" — ไม่จริงเพราะมี subprocess ครึ่งวินาทีอยู่ในนั้น | ทำ Usage เป็น lazy page (build ตอน `_goto_view` ครั้งแรก) หรืออย่างน้อยตัด `rtk gain` + `daily_series` ออกจาก construction path · ใส่ `WA_DeleteOnClose` / `deleteLater` หลัง `exec()` |
| **H4** | `usage_ledger.py:685` (`daily.update(fresh)`) + `:106` (`_RETENTION_MONTHS = 0`) | **rollup ทับตัวเลขย้อนหลังทิ้ง** — retention ลบ raw ของเดือนก่อนทุกเดือน แล้วถ้าภายหลังมี source ไฟล์ของเดือนนั้นถูก re-scan แค่ไฟล์เดียว `fresh[date]` จะมีแค่ row ของไฟล์นั้น และ `dict.update` **แทนที่** bucket ของทั้งวันแทนที่จะ merge | repro จริง (`probe_rollup.py`): rollup รอบแรก `{'turns': 10, 'input': 1000}` → raw ถูก prune → บันทึก turn ใหม่ 1 row ของเดือนเดิม → rollup อีกครั้ง = **`{'turns': 1, 'input': 100}`** · trigger จริงในชีวิตจริง: resume claude session เก่าข้ามเดือน (ไฟล์ jsonl ถูก append), codex ย้าย `sessions/`→`archived_sessions/` (mtime เปลี่ยน → `_file_unchanged` False → re-scan เต็มไฟล์) · module docstring อ้างตรงๆ ว่า "daily.json (which is never pruned) … so nothing is lost" | merge ระดับ `(date, model)` แทน `dict.update` — เก็บ provenance ต่อ source file หรือ accumulate เฉพาะส่วนที่ raw ยังมีอยู่จริง · เพิ่มเทส: rollup → prune → บันทึกเพิ่ม 1 row เดือนเก่า → daily ต้องไม่ลดลง |
| **H5** | `settings_usage.py:347-354` | **QThread ของ Refresh ไม่มี lifecycle เลย** — ไม่มี `isRunning()` guard (กด Refresh รัวๆ = import พร้อมกันหลายตัวเขียน jsonl/cursor/rollup ชุดเดียวกัน — `_append_jsonl` ชน `unlink` ใน `rollup_daily`), ไม่มี `finished → deleteLater`, ไม่มี wait ตอนปิด | ในไฟล์ข้างเคียง `settings_window.py:873` ทำ `thread.finished.connect(thread.deleteLater)` ถูกต้องอยู่แล้ว — ตัวนี้ไม่ทำ · เพราะ dialog ไม่เคยถูก destroy (H3) หน้าต่าง abort เลยเลื่อนไปตอน app quit แต่ **race ของ import ซ้อนกันเกิดได้วันนี้** · เทสที่มีอยู่รอ worker เสร็จก่อนเสมอ จึงไม่ครอบเคสนี้ | guard `if self._usage_import_thread and self._usage_import_thread.isRunning(): return` · `thread.finished.connect(thread.deleteLater)` · disable ปุ่ม Refresh ระหว่างรัน |
| **H6** | `core/storage/v1_only_write.py:66-76` · `doctor.py` `_v1_only_write_finding` | **ตัวจับ #502 ตาบอด 3 จุด แต่รายงานว่า "0 พบ — พร้อมสำหรับ #504"** (a) `_hit` คืน `None` เมื่อ **target ไม่มีอยู่** — เคสแย่ที่สุด (ไม่เคย mirror เลย) อ่านออกมาสะอาด (b) mtime บอกได้แค่ write ล่าสุด: dual-write ที่ถูกต้องรอบถัดไปลบร่องรอย V1-only write ก่อนหน้าทิ้งหมด และ scan เป็น on-demand ⇒ drift ที่เกิดระหว่าง `doctor` สองครั้งมองไม่เห็น (c) `role-providers`/routing.json ถูกข้ามโดยตั้งใจ — ซึ่งเป็นจุดที่ #480 เคย drift จริง | ด่านของรอบหน้าคือ "v1_only_write=0 ครบ 1 สัปดาห์ แล้วค่อยเอา V1 ออก" — ถ้าสัญญาณนี้ false-negative ได้ 3 ทาง การลบ V1 จะยืนอยู่บนหลักฐานที่พิสูจน์ไม่ได้ | ให้ "target หาย" เป็น hit (`Status.WARN` พร้อมเหตุผลแยก) · เปลี่ยนข้อความจาก "พร้อมสำหรับ #504" เป็น "ไม่พบใน snapshot นี้" · ถ้าจะใช้เป็นด่านจริง ต้อง sample เป็นระยะ (หรือ hash) ไม่ใช่ดูแค่ตอนกด doctor |

### MED

| Sev | File:line | ปัญหา | ทำไมสำคัญ | แก้ยังไง |
|---|---|---|---|---|
| M1 | `pipeline_executor.py:525-556` | auto-chain handoff เช็คแค่ `is_role_enabled("qa")` ไม่เช็ค `team_preset.can_spawn("qa")` | preset `pair`/`solo-lead` บล็อก qa ผ่าน `can_spawn` แต่ prompt ยังสั่ง Lead ให้ยิง qa → `assign` reject → เสียรอบเปล่า และข้อความ "reviewer = at PR time" ผิดสำหรับ preset `pair` (checker คือ reviewer) | รวมสองเงื่อนไขเป็นเช็คเดียว แล้วเลือกข้อความตาม `verify_mode(cfg)` |
| M2 | `routing_planner.py:658`, `938` | **`classify()` ไม่มี production caller เลย** — grep ทั้ง `src/` เจอ routing_planner ถูกเรียกแค่ `classify_precondition_mismatch` / `classify_blocked` / `classify_failure` (`orchestrator.py:5108,5116,5127,5703`) | ⇒ `_filter_disabled_roles` (#510) เป็น spec/test เท่านั้น ไม่ใช่ enforcement จริง (ของจริงคือ assign/cli_server reject + prompt text) · และ `suggest_team_size` (#512 item 5) เข้าถึงไม่ได้เลย ทั้งที่ `docs/lead/role-and-workflow.md:51` สั่ง Lead ให้ใช้ `routing_planner.suggest_team_size()` — Lead เรียกฟังก์ชัน Python เองไม่ได้ และไม่มี CLI ไหน expose ให้ ⇒ preset `auto` (ค่า default!) ไม่ให้คำแนะนำอะไรจริงๆ | ถ้าตั้งใจให้เป็น spec ก็เขียนกำกับให้ชัดใน commit/issue · ถ้าอยากให้ `auto` ทำงานจริง ต้อง expose ผ่าน `takkub team suggest "<task>"` หรือฉีดเข้า lead_context |
| M3 | `orchestrator.py:2365-2382` · `lead_context.py:866-873` | `--team` ให้ Lead pane ปลดล็อกสิทธิ์เขียนไฟล์โปรเจคของตัวเองได้ | `render_lead_settings` ตั้ง `deny: []` + allow `Edit/Write` เมื่อ `lead_may_implement` · `takkub assign --role lead --team solo-lead` เป็นคำสั่งที่ Lead รันเองได้ ⇒ guard ที่ user ตั้งไว้ถูกยกโดยฝ่ายที่ถูก guard | จำกัด `--team` ให้มาจาก surface ที่ user เป็นคนสั่ง (Settings / `takkub team set`) หรือ reject เมื่อ `from` role เป็น lead |
| M4 | `settings_window.py:1516-1528` · `settings_usage.py:293-325` | สลับธีมสด ๆ ไม่ครอบ inline stylesheet | `retheme()` ทำแค่ dialog QSS + nav icon · widget ที่ตั้งสีด้วย `setStyleSheet(f"…{cockpit_theme.TEXT_FAINT}…")` ตอน construct (การ์ด Usage, `_fill_grid` ทุก label, Accounts card) ยังคาสีชุด dark บนพื้นสว่างจนกว่าจะสร้างหน้าใหม่ | ให้ `retheme()` rebuild หน้าใน stack (หรือย้ายสีพวกนี้ไป objectName + QSS กลาง) |
| M5 | `task_dock.py:65` | `_STATUS_FALLBACK = ("☐", cockpit_theme.TEXT_MUTED)` — token ถูก capture ตอน import | สแกนทั้ง `src/` แล้ว **เหลือที่นี่ที่เดียว** (ที่อื่นแปลงเป็น `_dock_qss()` / `_sidebar_qss()` แล้ว) ⇒ light mode ได้สี dark | เปลี่ยนเป็นฟังก์ชัน หรืออ่าน `cockpit_theme.TEXT_MUTED` ตอนใช้ |
| M6 | `config.py:288-293` | `PROVIDER_ISOLATION_GAPS["cursor"]` เก็บข้อเท็จจริงเฉพาะเครื่อง dev: "cursor-agent CLI is not installed on this machine (checked PATH … 2026-09-07)" | string นี้ถูกโชว์ให้ user ทุกคนเห็นตรงๆ ใน `takkub doctor` และหน้า Accounts (`can_add_account` → `ProviderRow.add_hint`) · user ที่ลง cursor-agent ไว้จะอ่านว่า "ไม่ได้ติดตั้งบนเครื่องนี้" ซึ่งผิด | เขียน gap ให้เป็นข้อเท็จจริงของ provider ("ยังไม่ verify home knob") แล้วเก็บ probe log ของเครื่อง dev ไว้ใน issue/doc แทน |
| M7 | `accounts_adapter.py:305-323` | provider ที่มี gap → `provider_rows()` ข้ามทั้ง `user_profile` profiles และ `_v2_registry_accounts` | ⇒ บัญชี gemini/cursor ที่มีอยู่แล้วหายจากหน้า Accounts ทั้งที่ docstring บอกว่า "existing records stay visible after that page's removal" (ข้อมูลยังอยู่บนดิสก์ แต่ผู้ใช้มองไม่เห็น) | แสดง account เสมอ แล้วใช้ `gap_reason` ปิดแค่ปุ่ม Add/Login |
| M8 | `spawn_engine.py:226` → `core/accounts/facade.py:55` | pool ยังมีผลกับ spawn จริง แต่ #505 ถอด UI ตัวเดียวที่แก้ pool ได้ทิ้ง | `resolve_account_for` อ่าน `AccountPoolRegistry().all()` ทุก spawn ⇒ กลายเป็น state ที่มองไม่เห็นแต่ยังบังคับพฤติกรรม · `remove_account` ลบ account ที่ pool อ้างถึงอยู่ได้โดยไม่มีที่ให้แก้ pool ต่อ | หน้า Accounts ควรโชว์ pool (อย่างน้อย read-only) หรือถ้าตั้งใจเลิกใช้ ให้ถอน pool path ออกจาก spawn ใน #504 พร้อมกัน |
| M9 | `usage_ledger.py:787` | `months = {start_month, end_month}` เท่านั้น | `takkub usage --days 90` ข้าม quota sample ของเดือนกลางไปเงียบๆ (ตัวเลข % ที่หักน้อยกว่าจริง) | ไล่ทุกเดือนระหว่าง start..end |
| M10 | `accounts_adapter.py:190-203` | `_codex_plan_cached()` คืน plan ระดับ provider เดียวให้ทุก codex account | สอง codex account จะโชว์ plan เดียวกัน — ตัวเลข/ป้ายที่ไม่ตรงบัญชี ขัดกับหลัก "ตัวเลขจริงจาก provider เท่านั้น" | ถ้ายังแยกต่อบัญชีไม่ได้ ให้โชว์ plan พร้อม note ว่าเป็นค่าระดับ provider ไม่ใช่ของบัญชีนี้ |
| M11 | `settings_window.py:2678` → `settings_accounts.py:264` | `provider_rows()` รันบน Qt main thread ตอนสร้าง Settings | อ่าน `.claude.json` (เพดาน 8 MB) + `.credentials.json` ต่อทุกบัญชี และบน macOS default profile ยิง `security` subprocess (`claude_login_status:137`) — เส้นเดียวกับ H3 | ทำ lazy + ย้ายไป worker เหมือน `_PlanProbeWorker` ที่ทำถูกแล้วใน status_header |

### LOW

| Sev | File:line | ปัญหา | แก้ยังไง |
|---|---|---|---|
| L1 | `docs/architecture/godfile-map.md:140` | ยังลิสต์ `_plan_chip_style` / `_plan_chip_tooltip` / `_danger_button_style` เป็นเมธอดที่มีอยู่ (บรรทัด 151 บันทึกการถอดไว้แล้ว แต่บรรทัด 140 ไม่ได้อัปเดต) — map คือสัญญาว่า "อย่า grep มั่วแล้วเดา" | ลบ 3 ชื่อออกจากบรรทัด 140 |
| L2 | `status_header.py:278` · `tutorial_overlay.py:6` · `orchestrator.py:1274` | docstring ค้างอ้าง UI ที่ถอดแล้ว — 2 อันแรก Lead เห็นแล้ว, `orchestrator.py:1274` เป็นของใหม่: "main_window listens to repaint the plan chip without polling" ทั้งที่ badge ตอนนี้ทาสีใหม่จาก timer 2 วิ | แก้ข้อความทั้ง 3 |
| L3 | `cockpit_theme.py:171`, `342`, `447` | `CHIP_PLAN_MAX` ไม่มีผู้ใช้เหลือแล้ว (grep ทั้ง repo เจอแค่ตัว cockpit_theme เอง) แต่ยังอยู่ใน `_THEMED_TOKEN_NAMES` + token dict ทั้งสองชุด | ถอดออกทั้ง 3 ที่ (ponytail: ลบ > เพิ่ม) |
| L4 | `team_preset.py:132-140` | `_custom_role_names(project)` ไม่ได้ใช้ `project` เลย (`custom_roles()` เป็น global) แต่ signature ทำให้คิดว่าเป็น per-project และถูกส่งต่อผ่าน `_position_roles`/`_governed_roles` | ตัด parameter ทิ้ง หรือทำให้ per-project จริง |
| L5 | `team_preset.py:333-337` | docstring `can_spawn` มีร่องรอยคิดออกเสียงค้างอยู่: "…the active checker's OTHER option... no — see below" | เขียนใหม่ให้จบประโยค |
| L6 | `usage_ledger.py:15-17`, `631-635` | docstring บอกว่าเก็บ raw เดือนปัจจุบันของทั้ง `YYYY-MM.jsonl` และ `quota-YYYY-MM.jsonl` แต่ `_month_files` glob `[0-9]{4}-[0-9]{2}.jsonl` ซึ่งไม่แมตช์ `quota-…` ⇒ quota raw ไม่เคยถูก prune (ไฟล์โตไปเรื่อยๆ) | แก้ docstring หรือ prune quota ด้วย (ต้องมี rollup ของ quota ก่อน ไม่งั้นข้อมูลหาย) |
| L7 | `remote/api.py:830-834` | `days=999999999` → `_date.fromordinal` โยน `ValueError` → bridge จับแล้วตอบ 500 (ไม่ crash Qt loop แต่ก็ไม่ใช่ 400) · `days=-1` / `days=abc` เงียบๆ กลายเป็น 7 | clamp `days` ใน 1..365 แล้วตอบ 400 เมื่อ parse ไม่ได้ (ยืนยันด้วย `probe_days.py`) |
| L8 | `usage_ledger.py:258-263` | `record_quota_sample` อ่านไฟล์ quota ทั้งเดือนด้วย `_read_jsonl` ทุกครั้ง แค่เพื่อเทียบ row สุดท้าย | อ่านบรรทัดท้ายไฟล์ หรือ cache last-sample ใน memory |
| L9 | `lead_context.py:807-813`, `866` | non-Claude Lead: ถ้า `AGENTS.md` เป็นของ user (ไม่มี marker) `render_lead_agents_md` คืน None ⇒ block "role ที่ปิด" + "team preset" ไม่ถูกส่งเลย · และ `lead_may_implement=False` ไม่มี hard guard นอก Claude (`render_lead_settings` เขียน settings ของ Claude Code) | ของเดิมอยู่แล้ว แต่ #512 พึ่งมันเพิ่ม — อย่างน้อยบันทึกเป็น gap ของ #103 |
| L10 | `team_preset.py:143-147`, `329-362` | `solo-lead` ไม่ได้แปลว่าอยู่คนเดียวจริง: provider panes, `shell`, `critic` ไม่ถูก govern (ตั้งใจตาม docstring) — Lead ยังเปิด pane ได้ภายใต้ "ทำเอง" | ถ้าตั้งใจ ให้เขียนใน label/หน้า Settings ตอนทำ UI รอบหน้า |

---

## ตรวจแล้วไม่พบปัญหา (ระบุไว้ให้ Lead เทียบกับ pane อีกตัวได้)

- **Remote auth** — `/api/usage/history` ผ่าน `_check_bearer()` + `_check_password_gate()` ชุดเดียวกับ `/api/usage`, `/api/projects`, `/api/activity` ทุกตัว · **ไม่มี HTTP endpoint ใหม่สำหรับ team preset** (`takkub team` วิ่งบน CLI socket เดิม `cmd == "team"`, `cli_server.py:1338`)
- **ข้อมูลที่ตอบกลับมือถือ** — payload มีแค่ token/quota/account/model ไม่มี credential/plan blob · ฝั่ง PWA เรนเดอร์ทุกค่าผ่าน `textContent` (`app.js:3141-3155`, `3170-3181`, `3193-3201`) ไม่มี `innerHTML` ที่รับข้อมูลจาก server ⇒ ชื่อ model/account แปลกๆ ไม่กลายเป็น XSS
- **PWA cache** — `sw.js` bump `takkub-remote-shell-v37 → v38` มาพร้อมการแก้ `app.js` (บทเรียน 1.6.15 ที่ลืม bump)
- **credential read** — `claude_login_status` เรียกแค่ `_normalize_credentials` + `_read_keychain_credentials` (read-only) ไม่มี refresh/persist · เก็บแค่ `bool(access_token)` ไม่ log token ที่ไหน · **named profile บน macOS ไม่ยืม Keychain ของ default จริง** (gate ด้วย `is_default` ที่ `accounts_adapter.py:137`) · `.claude.json` มี size cap 8 MB
- **macOS branch** — `theme_settings.detect_system_variant` ใช้ `creationflags=SUBPROCESS_NO_WINDOW` ซึ่ง `_win_console.py:38` นิยามเป็น `getattr(subprocess,"CREATE_NO_WINDOW",0)` = **0 นอก Windows** ⇒ `creationflags=0` ถูกต้องบน POSIX ไม่ raise · มี `encoding="utf-8"` + `errors="replace"` + `timeout=3` · ฝั่ง Windows จับ `OSError` ครอบ key ที่ไม่มี
- **theme plumbing** — `apply_variant` rebind ผ่าน `globals().update()` และมีเทสยืนยัน key parity (`tests/test_cockpit_theme.py:241`) · grep ทั้ง `src/` **ไม่มี `from .cockpit_theme import X`** เลย (กับดักที่ docstring เตือนไว้เอง) · `app.py:244` bind variant ก่อน `from .main_window import MainWindow` จริง
- **kimi isolation (#103)** — key ของ `_PROVIDER_HOME_SUBDIRS` = codex/opencode/kimi เท่านั้น (ไม่มี claude) ⇒ เปลี่ยน `("codex","opencode")` เป็น `config.isolated_providers()` ใน `spawn_engine.py:2035` และ `doctor.py:3126` **เท่ากับของเดิม + kimi** ไม่มีผลข้างเคียง · ฝั่งอ่าน (`kimi_helper.kimi_share_dir`) กับฝั่งเขียน (`pane_env.inject_provider_home_env`) resolve ผ่าน `config.provider_home_dir` ตัวเดียวกัน — ไม่เกิด silent-blank-phone
- **enforcement choke points (#510/#512)** — `assign()` เช็คทั้ง `is_role_enabled` + `can_spawn` **ก่อน** แตก branch `pane`/`subagent` (`orchestrator.py:2331-2363`) · `cli_server` มี pre-check แบบ synchronous ก่อนตอบ ack (`:624-671`) · `pipeline_executor._spawn_one` เรียก `self.spawn()` ตรง ไม่ผ่าน assign — และมีเช็คของตัวเองครบทั้งสองชั้น (`:319-360`) · suffix `#N` ถูก strip ทั้งใน `is_role_enabled` และ `can_spawn`
- **`note_manual_roles_change`** — ดูเหมือน orphan แต่ cross-check แล้ว **มี caller จริง** ที่ `settings_window.py:1381` (กติกา "toggle มือ → custom" ต่อสายแล้ว)
- **`team_preset._save_raw`** — เขียน `.tmp` แล้ว `replace()` = atomic
- **`_v1_only_write_report`** ต่อเข้า `migrate validate` เท่านั้น ไม่แตะ `MigrationEngine` เอง (boot-time auto-migrate gate ไม่เปลี่ยนพฤติกรรม)

---

## เทสที่รันเอง

`608 passed, 1 skipped` — ไฟล์เทสใหม่/ที่แตะทั้งหมดในรอบนี้:

```
tests/test_usage_ledger.py test_remote_api.py test_team_preset.py
test_core_storage_v1_only_write.py test_theme_settings.py test_accounts_adapter.py   → 183 passed
tests/test_cockpit_theme.py test_settings_theme_toggle.py test_settings_usage.py
test_status_header_plan_badge.py test_team_preset_orchestrator.py test_cli_usage.py
test_pipeline_executor.py test_routing_planner.py test_remote_http_server.py
test_remote_pwa_usage_history.py test_provider_isolation.py test_no_hardcoded_ui_colors.py → 425 passed, 1 skipped
```

**เทสผ่านหมดแต่ไม่ครอบ H1–H5 เลย** — ไม่มีเทสไหนยิง handler จริงแล้วยืนยันว่าไม่มีการเขียน/ลบไฟล์, ไม่มีเทส rollup ข้ามเดือนหลัง prune, ไม่มีเทส main-thread responsiveness, ไม่มีเทส refresh ซ้อน · เทส `test_settings_usage.py` ยืนยันแค่ว่า `import_all` ไม่ถูกเรียกตอน construct (ซึ่งจริง) แต่ `query_usage` + `rtk gain` ถูกเรียก

### 2 เรื่องที่ต้องบอก Lead ก่อนส่งงานให้ qa

1. **รัน pytest ใน repo นี้ตอนนี้พังที่ conftest guard** — `site-packages` มี `agent_takkub` 1.0.60 (non-editable) บังอยู่ → `RuntimeError: agent_takkub imported from …site-packages…, expected …/src` · workaround ที่ใช้จริง: `PYTHONPATH=<repo>/src` นำหน้า (ห้าม `pip install -e` ตามกติกา #202)
2. **rtk บิดผล pytest จนอ่านผิดได้** — `python -m pytest` ที่ล้มที่ conftest ถูก rtk สรุปเป็น `Pytest: No tests collected` **พร้อม exit code 0** และ tee log ที่มันชี้ไปเป็นไฟล์เก่า · ต้องใช้ `rtk proxy python -m pytest …` ถึงจะเห็นผลจริง — ถ้า qa รันแบบปกติแล้วเห็น exit 0 อาจเข้าใจผิดว่าเขียว

---

## หลักฐาน (อยู่นอก repo ตามกติกา)

`$TAKKUB_ARTIFACTS_DIR/reviewer/` = `runtime/exports/2026-09-07/agent-takkub/reviewer/`

| ไฟล์ | พิสูจน์อะไร |
|---|---|
| `probe_usage.py` | H1 — traversal เขียน `daily.json` + ลบ `2000-01.jsonl` นอก ledger |
| `probe_rollup.py` | H4 — daily.json 10 turns/1000 → 1 turn/100 หลัง prune |
| `probe_days.py` | L7 — `days=999999999` ValueError, `-1`/`abc` เงียบเป็น 7 |
| `probe_dlg.py` | H3 — dialog ที่ parent แล้ว 5/5 ตัวยังไม่ถูกลบ |
| `pytest_targeted.log` | หลักฐานว่า rtk รายงาน "No tests collected" + exit 0 (ข้อ 2 ข้างบน) |

`rtk gain` timing (H2) วัดสดในเทิร์นนี้: `473 / 465 / 462 ms`, exe = `C:\Users\monch\bin\rtk.EXE`

---

## สรุป

แก้ H1–H6 ก่อน push · H1/H2/H4 อยู่ใน #507 ทั้งหมด (remote endpoint + ledger) ⇒ ถ้าอยากปล่อยรอบนี้เร็ว ทางที่แคบที่สุดคือ **ปิด `/api/usage/history` ไว้ก่อน** แล้วปล่อยส่วน desktop ที่แก้ H3/H4/H5 แล้วออกไป — แต่ H4 (ตัวเลขย้อนหลังหาย) แก้ยังไงก็ต้องแก้ เพราะขัดกับ session goal "ตัวเลขจริงจาก provider เท่านั้น · ไม่ลบข้อมูล" โดยตรง

review นี้ไม่ได้รับรองด่าน `v1_only_write=0 + drift=0 ครบ 1 สัปดาห์` — ดู H6 ว่าทำไมสัญญาณปัจจุบันยังรับรองไม่ได้ · ไม่ได้แก้ไฟล์ใดๆ ใน repo และไม่ได้ commit/push
