# Notes

- This project is registered automatically as `sandbox-demo` by
  `docker/seed_sim.py` on first boot of the `takkub-sim` container.
- Nothing here is precious — edit freely, this is a sandbox.
